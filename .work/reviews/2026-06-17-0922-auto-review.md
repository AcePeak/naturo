# Orc autonomous review — 2026-06-17 09:22 local (01:22Z)

**Verdict: quiet/healthy. MCP silent-failure loop completed a clean Dev→QA lap; Orc drove the product by filing a systematic contract issue. No human intervention, no CI block.**

## State at cycle start
- HEAD `0eff973` (PR #955, fixes #954). `develop` CI **GREEN** — Build & Test + CodeQL both success.
- **No open PRs.** `status:done` queue **empty**.
- `status:in-progress`: **#956** (active in-flight Dev work, created 00:44Z, updated 01:09Z, no branch pushed — Rule 4, left untouched).
- needs:ace queue: **#935 / #915 / #914 / #860 / #842** (unchanged).

## What moved since 07:24
- **#954** (MCP `capture_window` silently ignored `window_title`, captured foreground with `success:true`): Dev fix PR **#955** merged (`0eff973`), branch deleted (only `develop`+`main` remain), then **QA verified+closed** at 00:42Z. Full lifecycle clean — no Orc post-merge handoff required.
- **#956** (MCP `create_snapshot` bundles foreground screenshot with a *different* window's element tree, `success:true`): QA-filed sibling, Dev picked up (`status:in-progress`).

## Step 3 — product drive (the point of the cycle)
Investigated the #954/#956 pattern instead of treating them as isolated bugs. Confirmed it is a **systemic silent-failure class**: MCP tools accept a window selector, fail to resolve it, and silently proceed against the foreground/default window. Grep + read of `naturo/mcp/_inspect.py` found **more unfixed instances** (`set_element_value`, `toggle_element`, +2 handlers at ~L397/~L428) that swallow `backend._resolve_hwnd` failure at `logger.debug` level and then act on `target_hwnd == 0` (foreground).

→ Filed **#957** (P1, `bug`+`silent-failure`+`test`+`from:orc`, milestone v0.3.4): a shared `_require_hwnd` helper that returns `WINDOW_NOT_FOUND` instead of falling back to foreground, plus a **self-maintaining contract test** that enumerates every MCP tool with a window selector and asserts a bogus selector yields `success:false` (not `success:true`). Same self-maintaining-contract spirit as #912 (desktop-guard enumeration). Headless-runnable (no #863 input gate). Scoped to **not** overlap #956 (one-issue-one-PR).

## Steps skipped / unchanged
- **Step 3.5 (weekly competitiveness):** not due (baseline 2026-06-16, <7d).
- **needs:ace:** no new human-only decision item this cycle (#957 is Dev work). #915 increasingly safe to close — QA verified+closed #954 this very lap, further proving auth durability.

## Guardrails honored
No push to main. No issue closed. No in-flight Dev tree touched (#956). CI green → normal operation. Only operational files (STATE.md, NEEDS-ACE.md, this review) pushed to develop with `[skip ci]`.
