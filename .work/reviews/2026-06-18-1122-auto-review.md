# Orc autonomous review — 2026-06-18 11:22

**Verdict:** quiet/healthy loop; **one stuck team Dev PR diagnosed and dispatched.** `develop` not red.
No new human-only item. Weekly competitiveness not due (<7d since 06-16 baseline).

## PR sweep
- **PR #995** (`fix/issue-867-suggester-hidden-commands` → `develop`, author AcePeak/team Dev, auto-merge
  SQUASH enabled 03:16Z) — `BLOCKED` on **genuine red CI**. The in-flight #867 pickup from prior cycles.
  - **CI Gate FAILED.** 4 new tests in `tests/test_fuzzy_group.py` fail on **Ubuntu 3.12 + macOS 3.12**:
    `TestFuzzyGroup::test_hidden_command_not_suggested` and
    `TestRealCliHiddenSuggestions::test_hidden_top_level_command_not_suggested[screenshot-snapshot / snapshots-snapshot / hotke-hotkey]`.
    All fail the same way — the hidden command is still suggested:
    `Error: No such command 'interna'. Did you mean 'internal'?`
  - **Root cause (verified):** CI runners resolve **click 8.4.1**; this desktop has **click 8.3.1**.
    click ≥8.4 added a **native** command typo-suggester to `click.Group.resolve_command` that does **not**
    honour `hidden=True`. PR #995's `FuzzyGroup._suggestable_commands` filters hidden commands only in its own
    `difflib` path; when it finds no visible match it falls through to `super().resolve_command()`, and on
    click 8.4.1 that native path re-suggests the hidden command. On click 8.3.1 the base resolver has no
    command-level "Did you mean" (confirmed locally: a plain `click.Group` emits only `No such command 'interna'.`),
    so the fix looks complete on the Windows desktop — the "green on Windows, red on Linux/macOS" pattern the
    playbook calls out.
  - **Action:** Dev-fixable, not human-only. Per orch-review Step 1, **dispatched a Dev fix via a precise
    diagnostic comment** on PR #995 (root cause + fix direction: `FuzzyGroup` must own the not-found path —
    when `cmd is None`, `ctx.fail(...)` with a suggestion drawn only from `_suggestable_commands`, instead of
    delegating to `super().resolve_command()`; reproduce against click 8.4.1 on Linux, not the 8.3.1 desktop).
    Did **not** merge (red), did **not** touch the branch (Rule 4), auto-merge correctly held by the gate.
- No other open PRs. Branches: `develop`, `main`, `fix/issue-867-suggester-hidden-commands` (open PR — fine).

## Issue lifecycle
- `status:in-progress` = **#867** (active; PR #995 held by red CI, updated 03:16Z → not stale/abandoned). Left.
- `status:done` = **#972** (input-content guard, code-verified; close = human security sign-off, queued). No QA
  pickup yet — already tracked, no action.
- No issue closed (Rule 1 — no merged commit this cycle). No abandoned `status:in-progress` to unlabel.

## Drive the product (Step 3)
- No new issue filed. The substantive work this cycle was the #995 red-CI diagnosis + dispatch (real Step-1
  work, not idle). Backlog is sharp and correctly prioritized (recognition doc/proof arm complete; distribution
  next — #926 `.mcpb` P1/pickable, #922 registries P1, #930 hero demo). Recognition hardening remaining is
  env-blocked (#932 Java JAB no JDK; #934 SAP no install). A duplicate gap issue would be noise (Rule 9).
- **Class observation (not filed — folded into the #995 comment + STATE):** the desktop's pinned `click 8.3.1`
  masks `click 8.4.x` behavior that CI exercises, an env-honesty gap akin to #910/#969. The correct remedy is
  the #995 fix being click-version-robust (own the not-found path), so no standalone issue is warranted yet;
  revisit if the click-version divergence recurs as a class.

## needs:ace queue (Step 4)
- Unchanged: **#975 / #972 / #969 / #935 / #915 / #914** (+ infra #860 / #842). No new human-only item
  (the PR #995 red CI is Dev-fixable, dispatched). NEEDS-ACE.md header refreshed; decision table unchanged.

## Weekly competitiveness (Step 3.5)
- **Not due** — baseline row 2026-06-16, today 2026-06-18 (<7 days). Skipped.

## CI / ship-gate
- `develop`: last code commit `142bfe5` **Build & Test + CodeQL success** (HEAD `5d92fcb` = orc `[skip ci]`)
  → **not red**. The red is **PR-branch-only** (#995) → no STOP.
- v0.3.2 ship-gate **FULLY MET** — cutting/tagging the release (#914) is Ace's call (Rule 2).
