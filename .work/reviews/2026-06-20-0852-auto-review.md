# Orc autonomous review — 2026-06-20 08:52Z

**Verdict: quiet/healthy. develop NOT red. No new human-only item (queue unchanged). One team-Dev PR
landed; one Step 3.6 team-evolution change shipped.**

## Step 0 — setup
- `git config` Orc identity; `git fetch origin -p`; `git checkout develop`; `git pull --ff-only`.
- Authoritative `gh api .../branches` = **develop + main only** → Rule 14 clean.

## Step 1 — PR sweep
- **Team-Dev PR #1072** (`fix: honor find --image --screenshot for offline matching`, fixes #1070 + #1067,
  base `develop`, auto-merge SQUASH armed). First CI run **red on Linux/macOS only**:
  `test_screenshot_rejects_window_targeting_flags` → `PLATFORM_ERROR` (expected `INVALID_INPUT`);
  `test_screenshot_file_not_found` → `PLATFORM_ERROR` (expected `FILE_NOT_FOUND`). Windows DLL tests passed.
  - Root cause (read the failing log + diff): `_find_with_image` ran `_platform_supports_gui()` **before** the
    `--screenshot` argument validation, so on a non-GUI CI runner the gate fired first and changed the
    bad-input → error-code contract. Offline matching needs no live capture, so the gate should not apply.
  - Fix `b2b3c8c` (already on the branch this cycle): removed the early unconditional gate; validation now runs
    first, and the GUI gate applies only on the live-capture path (`elif screenshot is None`). Verified the
    diff is correct against both failing assertions before letting it land. **CI re-ran green → auto-merge
    landed it** (develop `91ce240`; source branch auto-deleted → Rule 14).
  - **Handoff:** flipped **#1070** `status:in-progress`→`status:done`; marked **#1067** `status:done`. Neither
    closed (Rule 1 — awaiting QA `verified`).
- **Community PR #1055** (base `main`, UNSTABLE) — unchanged; already queued **needs:ace #1057**. Human-only
  (community-PR disposition) → not merged/commented/closed.

## Step 2 — issue health
- `status:in-progress` = empty after the #1070 flip → no in-flight/abandoned work.
- `status:done` (open, awaiting QA) = **#1070**, **#1067** (this cycle) + **#972** (human security sign-off,
  queued). Nothing to close (Rule 1).
- QA confirmed healthy — verified+closed #1061 @16:42Z (live Calculator, read-only). Reinforces that #915's
  "QA down ~5 days" era is over (still recommended for closure in NEEDS-ACE).

## Step 3 — drive the product
- Standing #1 (recognition supremacy): the find-engine recognition feature `find --image --screenshot`
  (#1070/#1067 — offline template matching) is now correct + headless-capable and landed on develop.
- No new gap sharp enough to file this cycle beyond the existing tracked work (Rule 9 — no churn).

## Step 3.5 — competitiveness
- **NOT due** (tracker baseline 2026-06-16; today 06-20 = 4d < 7).

## Step 3.6 — evolve the team
- **Evidence:** PR #1072 was opened with auto-merge but went Windows-green / Linux+macOS-CI-red because a
  platform/capability gate (`_platform_supports_gui()`) was ordered **ahead of** argument validation, making
  the bad-input → error-code contract platform-dependent. A new class — distinct from the error-attribution
  row (which `try`/`except` owns a code) and the cross-platform-*collection* break (2026-06-16).
- **Change:** added a **Platform-invariant validation order** rule to `dev-cycle.md` Step 3 self-review —
  argument validation (+ its code) must precede any platform/capability gate so a given bad input yields the
  same code on every OS; skip the gate on no-live-capture paths; pin with a test that does NOT mock the
  platform. Logged in `agents/EVOLUTION.md`. Builds on (does not undo) the parity / error-attribution /
  option-coverage rows.

## Step 4 — needs:ace
- No new human-only item. Queue unchanged: **#1057, #975, #972, #969, #935, #915, #914, #897.**
- `NEEDS-ACE.md` header refreshed.

## Step 5 — state
- `STATE.md` header refreshed; this review written; operational files committed `[skip ci]` and pushed to
  develop (NOT main). v0.3.2 ship-gate unchanged — FULLY MET, release is Ace's call (#914).
