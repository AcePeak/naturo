# Daily Review — 2026-07-04 15:02 (+0800) — delta

> Short delta after the prior entry (labelled "1730" but actually committed 12:05 +0800, i.e. ~3h ago —
> the HHmm label ran ahead of commit time; this 15:02 entry is genuinely *later* despite the smaller number).
> `agents/GOAL.md` remains the single source of truth; STATE.md structural body is superseded.

## Summary
- **develop CI GREEN.** Since the last review, **#1268** finished merging and **#1269** landed —
  `fix(mcp): auto-JAB fallback in see_ui_tree` (merged 06:58 UTC, green).
- **The proven moat is now wired into the default agent path.** D1's real matrix showed Java/Swing
  **46 vs pywinauto 6** is naturo's demonstrable raw-count moat. #1269 makes `see_ui_tree` auto-fall-back
  to JAB — so an agent gets the Java moat *without a screenshot*, by default. Engineering kept advancing the
  *actually-proven* moat (pillar B / agent-fit) while the public-claim gate waits on Ace.
- **D1 is unchanged at ≈90% — the only remaining gate is Ace's positioning decision** (criterion #5),
  already escalated at the 1730 review. Nothing new to escalate; not re-escalating.

## D1 done-criteria scorecard (unchanged since 1730)
| # | Criterion | State |
|---|-----------|-------|
| 1 | Runnable harness `benchmarks/competitive/` | ✅ met (#1245) |
| 2 | `docs/COMPETITIVE.md` matrix from real runs | ✅ met (#1264) |
| 3 | Honest, not cherry-picked | ✅ met (Electron/Excel shown as non-wins) |
| 4 | Matrix-logic test, pytest 0, Linux-collectable | ✅ met (9/9) |
| 5 | README/positioning "beats X" Ace-gated | ⏸ **the remaining gate** — Ace decision |
| 6 | Merged to develop, CI green | ✅ met (#1264) |

## Milestone Progress
| Milestone | State | Health |
|-----------|-------|--------|
| M1–M4 (engineering) | DONE (2026-07-01) | ✅ complete |
| **D1** (prove #1 / competitive matrix) | ≈90% | ⏸ **at-gate** — one Ace positioning call |
| D2 (make moat visible / README) | not started | blocked on D1 #5 |

## Actions Taken
- Commented on **#1255** (Office via App Paths): it CONFLICTS specifically because merged **#1261**
  (`launch a full path passed as name directly`) already rewrote the same files (`naturo/process.py` +
  `tests/test_process.py`). Asked Dev to rebase **and verify #1261 didn't already subsume the fix** — trim
  the diff to only what's still needed rather than rebasing a possibly-redundant change.
- Verified develop head green after #1269; confirmed the JAB-moat delivery landed.
- Did **not** re-nag #1203 (commented 07-03; moat-critical status unchanged).

## Open PRs (2 — both need a Dev rebase)
- **#1203** (surface JAB element states) — **RED**, diverged (behind 76). Ubuntu 3.9/3.13 + macOS 3.13 tests
  FAIL, CI Gate FAIL. **Highest-value eng PR in flight** (Java/JAB is the proven headline moat). Needs
  rebase + green.
- **#1255** (Office via App Paths) — **CONFLICTING/DIRTY** (behind 15). Conflicts with merged #1261 (see
  action above). Auto-merge on; can't apply until rebased + conflict-free.

## Top 3 Priorities (next 24h)
1. **Ace:** the single D1→D2 unblocker — approve/adjust narrowing the public claim to Java(JAB)/SAP/deep-CEF
   as recognition *quality* and reframing Electron/Excel `✗`→`~` in `docs/COMPETITIVE.md` (self-consistency).
2. **Dev:** rebase **#1203** green — hardening JAB state surfacing is moat-critical now that Java is the
   proven #1 area.
3. **Dev:** rebase **#1255** *after* checking #1261 overlap; trim to non-redundant diff only.

## Risks
- **Positioning contradiction is public and static.** `docs/COMPETITIVE.md`'s top capability-claim table still
  marks Electron/CEF + Excel COM as rival-`✗`, contradicting the measured matrix in the same doc. Every day it
  sits is a day the public doc is internally inconsistent. Mitigation: the crisp Ace decision above (one round
  fixes it).
- **Both open PRs are stale/diverged.** Auto-merge is on but neither can apply. If Dev doesn't rebase, the
  proven-moat hardening (#1203) stalls. Mitigation: flagged; not yet abandoned.
- **Next honest metric identified, not tracked:** *meaningful-interactive-element* count (raw count ≠ agent
  usefulness) — the data itself named it; may honestly recover Electron/Excel cells. Captured here + STATE.md,
  a D1-follow/D2-strengthening `/goal` slice.
