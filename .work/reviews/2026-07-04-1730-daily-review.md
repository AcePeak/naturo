# Daily Review — 2026-07-04 17:30 (delta)

> Delta review. The earlier 2026-07-04 review escalated "D1 env-blocked (Windows run)". **That escalation
> is now resolved** — a real Windows-desktop run landed hours later. This entry records the state change
> and re-points the one remaining D1 gate.

## Summary
- **D1 is unblocked and data-complete.** PR **#1264** (`e22e606`, merged, CI green) published the coverage
  matrix **from a real desktop run** (2026-07-04, JDK 21) with the UIA-only rival **pywinauto actually
  running** (its comtypes gen-cache had to be un-blocked first, #1262/#1263 — otherwise it was silently
  scored `blocked`, which would have flattered naturo). Criteria #1–#4 and #6 are met.
- **The measured numbers are honest and they narrow the story:** Java/Swing **46 vs pywinauto 6** is the
  demonstrable raw-count moat (UIA is Swing-blind → JAB is where a UIA-only tool returns ~nothing).
  Electron/Chromium (85 vs **113**) and Excel COM (1177 vs **1307**) are **NOT** raw-count wins — a UIA-only
  rival is competitive or higher there. This is the whole point of running the real rival vs a proxy.
- **Only gate left on D1 is Ace's, and it's positioning, not infra** (see Escalation).

## D1 done-criteria scorecard (updated — was ≈50% env-blocked; now data-complete)
| # | Criterion | State |
|---|-----------|-------|
| 1 | Runnable harness `benchmarks/competitive/` (naturo vs pip-installable OSS rivals) | ✅ met (#1245) |
| 2 | `docs/COMPETITIVE.md` matrix published **from real runs** | ✅ **met now (#1264)** |
| 3 | Honest, not cherry-picked (naturo's non-wins shown) | ✅ **met now** — Electron/Excel shown as non-wins |
| 4 | Test pins matrix logic; `pytest` exits 0; Linux-collectable | ✅ met (9/9) |
| 5 | README/positioning "beats X" opened auto-merge OFF (Ace-gated) | ⏸ **the remaining gate** (see below) |
| 6 | Merged to `develop`, CI green | ✅ met (#1264 merged green) |

**D1 ≈ 90%** — build + prove-with-real-numbers halves both done. Remaining is one Ace positioning decision.

## Escalation to Ace — one crisp decision unblocks D1 #5 and all of D2
The measured run created an **internal contradiction inside the published `docs/COMPETITIVE.md`**:
the top *capability-claim* table still marks **Electron/CEF and Excel COM as rival-`✗`**, but the measured
pywinauto run contradicts that (rival is competitive/higher on raw count). #1264 correctly did **not**
rewrite the claim unilaterally — narrowing a public claim is an Ace-gated positioning call. Deciding it is
now the single thing gating D1 → D2.

**Recommended decision (approve/adjust):** narrow the public claim to what the data proves —
**Java (JAB/Swing) + SAP GUI + *deep* CEF (post-navigation DOM via CDP)** as the moat, framed as recognition
**quality/depth**, not raw element presence. Reframe the Electron/Excel rows from `✗` to `~` (rival sees the
UIA-exposed surface; naturo adds depth), keeping the metric caveat (recognized elements vs raw UIA
`descendants()`). This makes the doc self-consistent and keeps the headline honest and still #1 where it
counts. On approval, one `/goal` round edits the claim table + stages the D2 README headline (auto-merge OFF).

## Next-round engineering task the data itself identified (not yet tracked)
The matrix flags that **raw element count ≠ agent usefulness** — an *actionable / meaningful-interactive-element*
metric is the right refinement and may change the Electron/Excel picture in naturo's favor honestly. This is
a concrete D1-follow / D2-strengthening slice for the next `/goal` round. (Not filed against the dormant
legacy issue tracker; captured here + in STATE.md.)

## Health check
- **develop CI**: green (last completed runs #1265–#1267 all SUCCESS; #1268 in progress at review time).
- **Open PRs (2)**:
  - **#1203** (surface JAB element states) — **BLOCKED/RED** (Ubuntu 3.9/3.13 + macOS 3.13 test FAILUREs,
    CI Gate FAILURE). **Priority just rose:** Java/JAB is now the *proven* headline moat, so hardening JAB
    state surfacing is the highest-value engineering PR in flight. Needs a Dev rebase + green (commented
    07-03; not re-nagging, but flagged as moat-critical here).
  - **#1255** (launch Office via App Paths registry) — **DIRTY** (merge conflict); auto-merge is on but
    can't apply. Needs a rebase onto develop.
- **Runners**: still no self-hosted Windows runner registered — but D1's #2/#3 got done via a real desktop
  session anyway (option 1 from the earlier escalation happened). Standing Windows access remains the
  structural risk for future desktop-verified rounds.

## Actions taken
- Refreshed `agents/STATE.md` top line (was "D1 ≈ 50%, env-blocked, escalated" — now "D1 data-complete,
  only Ace positioning gate remains").
- Recorded the COMPETITIVE.md internal contradiction + the recommended narrowing as the crisp Ace decision.

## Top 3 priorities (next 24h)
1. **Ace: make the positioning call** (narrow claim to Java/SAP/deep-CEF; reframe Electron/Excel `✗`→`~`).
   That closes D1 and opens D2. Everything else on D1 is done.
2. **Rebase #1203** (moat-critical JAB states) to green, and **rebase #1255** (DIRTY) — both auto-merge on.
3. **Next `/goal` round**: the *meaningful-interactive-element* metric refinement (honest strengthening of
   the Electron/Excel cells), then stage the D2 README headline (auto-merge OFF).

## Risks
- **Honesty vs. hype tension is now live and correctly handled** — the measured data undercuts part of the
  old capability claim; keep the public headline to the Java/SAP/deep-CEF moat the numbers actually support.
- **Windows-desktop access is opportunistic, not standing** — this round got one; future desktop-verified
  rounds (metric refinement re-run, D2 proof) will re-block without a registered runner.
- **Public-claim discipline** — D1 #5 + all of D2 are Ace-facing; README/PyPI/"we beat X" stay auto-merge OFF.

**[Orc-Mycelium]**
