# Daily Review — 2026-07-20 07:00

## Summary
- **The loop is dead, not slow.** 7 days, zero human/agent commits; the only repo activity is dependabot.
- **Blocked on:** #1168 — no persistent scheduler. Ace-gated, not code-gated; I have no code-side lever.
- **Next:** one host-level cron entry restores forward motion. Nothing else in the backlog moves without it.

## Milestone Progress

| Milestone | Open | Health |
|-----------|------|--------|
| v0.3.2 | 21 | **blocked** — 3 untouched P0s (#1274, #1291, #1293), ship-gate #914 open |
| v0.3.3 | 11 | blocked (downstream of v0.3.2) |
| v0.3.4 | 25 | not started |
| v0.5.0 | 4 | not started |
| backlog | 40 | — |

## Findings

### 1. Freeze is total (3rd recurrence of #1168)
`CronList` empty. Last merge #1290 @ 07-13T04:11Z. 0 feature branches, 0 non-dependabot PRs,
Dev-Sirius queue idle since 2026-04-05. Escalated on #1168 with a narrowed ask: one host-level
cron entry, not the full scheduler design.

### 2. #1274 mask still in place — today's green checks carry no information
`build.yml:153` still has `continue-on-error: true`. #1290 fixed the *stub* (63→24 failures);
it never removed the mask. The 24 remaining failures (#1293) are invisible to every gate.
Commented on #1274 to prevent the 07-13 headline being read as a close-out.

### 3. NEW #1295 — dependabot targets `main` only; develop's CI is silently aging
No `target-branch` in `.github/dependabot.yml`, so every bump lands on `main` while all work
happens on `develop`. Measured drift: checkout v7/**v6**, setup-python v7/**v6**, codecov
v7/**v6**, gh-release v3/**v2**, pillow `<13.0`/**`<12.0`**. `main` is ahead 7 / **behind 946**.
Same defect class as #1273: a signal that cannot report its own failure.

Non-obvious detail captured in the issue: dependabot reads its config from the **default branch
only**, so the fix must land on `main` — patching `dependabot.yml` on `develop` is a no-op.

### 4. User impact — 946 commits unreleased
PyPI latest is 0.3.2 = `main` = 946 commits behind `develop`. Everything built since the v0.3.2
cut is invisible to users. `develop`'s `version.py` still reads 0.3.2 — no bump staged.

## Actions Taken
- Created #1295: dependabot/develop CI drift (P1, tech-debt, silent-failure)
- Escalated #1168 with 7-day evidence + narrowed ask
- Commented #1274: mask still present, stays P0
- Did **not** patch `dependabot.yml` on develop — would have been a silent no-op (see #1295)

## Top 3 Priorities (next 24h)
1. **#1168** — one host-level cron entry. Everything below is downstream of this.
2. **#1274** — drop `continue-on-error` from the Windows job; the signal is still masked.
3. **#1295** — land `target-branch: develop` on `main`, forward-port the 5 stale pins.

## Risks
- **This report is near-identical to 07-13's.** Two consecutive reviews with no code movement means
  the review cadence is now producing evidence nobody can act on. If #1168 is not resolvable,
  the honest call is to pause the daily loop rather than accumulate identical reports.
- **Trust in green CI.** #1274 unfixed means every "verified" claim on this repo is suspect
  (see the `ci-verified-means-ci-green` rule). Do not accept green as evidence until it lands.
- **Release drift compounds.** 946 commits unreleased and growing; a v0.3.3 cut from `develop`
  will be an enormous, unverified delta against what users actually run.
