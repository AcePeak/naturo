# Daily Review — 2026-07-25 17:30

## Summary
- Broke the review-cycle stalemate: instead of an 8th NO-CHANGE report, landed a real **P0 fix** — PR #1303 for #1291 (silent foreground-fallback in `type_text`/`press_key`).
- Blocked: #1303 is BLOCKED only on CI-in-progress (auto-merge enabled); the workflow-scope wall (#1302) still holds #1298/#1299.
- Next: confirm #1303 goes CI-green + merges (do NOT call it landed until then); more of the 63-failure CI set (#1274) is fixable the same way.

## What changed since 12:01 review
Nothing merged; develop HEAD was still the 12:01 review commit. #1298/#1299 unchanged (BLOCKED, 07-23). needs:ace still 9. So rather than re-report a dead loop, I did engineering: picked the highest-value non-Ace-gated item and fixed it.

## Action taken — #1291 (P0, Never-Lie)
- **Root cause (proven, not guessed):** `type_text`/`press_key` focused the target inside `try/except: pass`. An unresolvable `window_title`/`hwnd` was swallowed → keystrokes land in whatever window is focused → tool still returns `success:true`. Wrong-window write on the two primary input tools. The self-maintaining #957 guard `test_unresolvable_window_title_fails_loudly` was already **red** for both (masked pre-#1273 by `-x` + `continue-on-error`).
- **Fix:** route both through `require_hwnd()` (the shared #957 loud-resolution path) before focusing → `WINDOW_NOT_FOUND` envelope on a miss. 12 lines, no new `_SEMANTIC_EXCEPTIONS`.
- **Verified locally:** guard flips green for both tools; `test_mcp_window_selector_contract_957.py` + `test_mcp_input.py` = 49 passed (was 2 failing); ruff clean; existing focus+type tests preserved.
- **Shipped:** branch `fix/1291-input-loud-window-resolution` → PR #1303 → auto-merge (squash) enabled. Source file, so it clears the #1302 workflow-scope wall. Labeled #1291 in-progress + commented root cause.

## Note (pre-existing, out of scope)
`test_safe_input_guard.py::TestMcpTypeGuard::test_allows_dangerous_when_disabled` fails identically on clean develop (MagicMock `set_focused_element_value` returns truthy → value-pattern rung short-circuits before `backend.type_text`). One of the #1274 63-failure set — a stale test, not a product bug. Not touched here.

## Milestone Progress
| Milestone | Health |
|-----------|--------|
| CI-failure cleanup (#1274) | active — #1291 now has a fix in flight; ~62 failures remain |
| Dependabot pins (#1295/#1302) | blocked — workflow-scope wall, Ace-gated |

## Top 3 Priorities (next 24h)
1. Confirm #1303 CI-green + merged; then Dev closes #1291.
2. Triage the next tractable items in the #1274 63-failure set (same silent-fallback / stale-test patterns) — landable without Ace.
3. #1302/#1298/#1299 remain Ace-gated (workflow scope on the loop's token) — no re-proving needed.

## Risks
- **Don't declare #1303 landed on the local pass** ([[ci-verified-means-ci-green]]): require green matrix + `mergedAt` non-null.
- The 63-failure CI set means some Ubuntu/macOS lanes may carry pre-existing red; watch that #1303's *own* new-green (the #957 guard) isn't lost in noise.

**[Orc-Mycelium]**
