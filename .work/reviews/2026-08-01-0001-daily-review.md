# Daily Review — 2026-08-01 00:01

## Summary
- **Loop STILL FROZEN** — last merge into develop is #1309 (30-Jul 14:31 UTC, ~1.5d ago), all recent merges AcePeak-manual; automated loop dead since #1168 (no persistent scheduler).
- **MOVEMENT on `software-support`** — 6 green commits (07-31 13:14–15:53 UTC) of real MOAT/agent-fit feature work; its prior RED CI was a FALSE ALARM and is now GREEN, exactly as predicted last review.
- What's next: unblock the loop (#1168 needs:ace) and eventually land `software-support` (active author, no PR yet).

## Health Check
| Signal | State |
|--------|-------|
| Loop liveness | **FROZEN** — 0 loop merges; last develop merge #1309 30-Jul-14:31 UTC (~35h) |
| CI (develop) | green |
| CI (software-support) | **GREEN** — 6/6 recent runs pass (prior RED = confirmed false alarm, cleared by forward/rebase) |
| #1274 Windows mask | **VERIFIED UNCHANGED** — `continue-on-error: true` at build.yml:153 |
| needs:ace | **9** (unchanged) |
| Open PRs | #1298 (dependabot gh-release 2→3) — token-walled (#1302, no `workflow` scope) |
| New issues | none since #1302 (07-22); all blockers already filed + Ace-gated |

## `software-support` branch (the movement)
Raw Ace-manual branch, **ahead:19 behind:5, green CI, NO PR opened**. Active author committing directly.
Real capability-moat + agent-fit work landing here, not the loop:
- `feat(input): postmessage input mode` — WM_* PostMessage delivery for headless/RDP/`tscon`-redirected sessions where SendInput is silently dropped; also bypasses UIPI when elevated (drives Qt single-HWND / security-suite windows). Verified end-to-end on 腾讯电脑管家 (Qt 5.15, elevated, headless). +37 unit tests.
- `feat(see): composable recognition-technique flags + --fast/--deep` presets
- `fix(cascade): --ocr suppresses shallow-tree AI-vision auto-fallback`
- `feat(capture): visibility-first — screen BitBlt for unoccluded windows` + `fix(capture): raise occluded window and BitBlt instead of blank`
- `feat(click): --offset DX DY` relative to a resolved ref/point
- `docs(see): sweep --cascade/--fill-gaps` references

**NOT landed** — active author, no PR, behind:5 (needs rebase to pick up #1305–#1309), Windows write-paths (postmessage/BitBlt) want on-desktop verify. Correctly directional toward the north-star MOAT; leave it to the author.

## Actions Taken
- None. Everything actionable is Ace-gated (needs:ace=9) or an active author's in-flight branch. No orphan branches needing a PR, no cheap loop fixes to land, no new gaps to file.

## Top 3 Priorities (next 24h)
1. **#1168** — stand up a persistent scheduler so Dev/QA crons survive without a live Orch session; the loop has been frozen ~5d. (needs:ace)
2. **Land `software-support`** once the author rebases onto develop (clears behind:5) and Windows write-paths are desktop-verified — this is direct MOAT progress (headless input, visibility-first capture, composable recognition flags).
3. **#1274** — drop the Windows-mask `continue-on-error` and make the job blocking; the mask still hides a structurally-unfailable check. (needs:ace)

## Risks
- **Loop still dead** — all forward progress is Ace's hands; the moment Ace stops, movement stops. #1168 is the single unblock.
- **`software-support` drift** — behind:5 and growing; the longer it runs unrebased/unmerged the harder the eventual land. Green CI is reassuring but the postmessage/BitBlt Windows write-paths are not covered by the masked CI job — need real-desktop verification before merge.
- **#1298 token-walled** — dependabot workflow PRs can't merge via loop token (#1302); a low-value but permanent papercut until token scope or main-targeting (#1295) is fixed.
