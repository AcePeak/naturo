# Daily Review — 2026-08-01 15:02 (+08 / 07:00 UTC)

## Summary
- Automated loop STILL FROZEN: last real develop merge #1309 was 2026-07-30 14:31 UTC (~40.5h ago); #1168 (no persistent scheduler) is `needs:ace`.
- MOVEMENT (non-loop): `software-support` branch got 3+ Ace-manual commits today (cascade Phase 2/3 — Unified Element Tree UIA+MSAA+JAB merge, runaway-traversal backstop); ahead:73 behind:7, NO PR, active author.
- Its RED CI is a REAL mypy `call-overload` (unchanged from prior review): `_detached_stdio` dict unpacked into `Popen` — branch-local, author-fixable, NOT a false alarm.

## Milestone Progress
| Area | State | Health |
|------|-------|--------|
| Automated loop | last develop merge #1309 ~40.5h ago; #1168 `needs:ace` | blocked (Ace-gated) |
| software-support (manual) | ahead:73 behind:7, active author, no PR, RED CI | at-risk (real mypy fail) |
| needs:ace queue | 9 issues, untouched | blocked (Ace-gated) |
| CI mask #1274 | build.yml:153 `continue-on-error: true` unchanged | tracked |

## Findings

### software-support RED CI = REAL, unchanged
`naturo/process.py:432/454/479` — `subprocess.Popen([...] , **_detached_stdio)`.
`_detached_stdio` (defined line 417 as a dict literal) is inferred `dict[str, int]`; mypy rejects unpacking it into `Popen` kwargs (`No overload variant of "Popen" matches argument types "list[str]", "dict[str, int]"`).
- Fix is cheap (annotate `_detached_stdio` with a proper type / `dict[str, Any]`, or a TypedDict), BUT branch is an **actively-authored manual branch** (commits every ~30 min today) with **no PR** — do NOT push into it mid-flight. Author will see the red X and fix.
- Also note: `pyproject.toml [mypy] python_version = 3.9` triggers a mypy warning ("must be 3.10 or higher") — pre-existing, separate cleanup.

### Token wall confirmed on BOTH open PRs
- #1310 (`webbrain/issue-1302`→main): BLOCKED / `action_required`, edits `.github/workflows/*` → loop token lacks `workflow` scope (#1302). Ace-gated.
- #1298 (dependabot `action-gh-release` 2→3 →develop): edits `.github/workflows/build.yml` → same `workflow`-scope wall. Cannot be merged by the loop.

### #1274 mask — VERIFIED UNCHANGED
`build.yml:153` still `continue-on-error: true` on the windows-latest desktop-tests job. Structural mask intact; no regression, no fix landed.

## Actions Taken
- None mergeable/pushable this cycle: every open PR is `workflow`-scope-walled (Ace-gated); software-support fix must not be pushed into an active author branch; all 9 `needs:ace` are Ace-gated; #1168 (loop unfreeze) is `needs:ace`.
- Verified (not re-filed) the software-support mypy failure is real and branch-local; no new issue created (active author owns the branch).

## Top 3 Priorities (next 24h)
1. **Ace: unfreeze the loop** — #1168 (persistent scheduler). ~40.5h since last automated develop merge.
2. **Ace: grant loop token `workflow` scope OR self-merge #1310 + #1298** — both dependabot/ops PRs are permanently stuck otherwise (#1302).
3. **software-support author: fix the `_detached_stdio` Popen typing** (process.py:417/432/454/479) before landing; Windows write-paths still need runner verify.

## Risks
- Loop freeze is now multi-day; all forward motion is manual Ace commits. Mitigation is entirely Ace-side (scheduler + token scope) — Orc has no unblocked lever this cycle.
- software-support carries 73 commits with no PR and red CI — growing merge/review debt the longer it stays a private branch.
