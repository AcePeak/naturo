# Daily Review — 2026-08-02 00:00 (local +8) / 2026-08-01 16:00 UTC

## Summary
- Automated loop **STILL FROZEN** — last real develop merge is #1309 (30-Jul 14:31Z), now ~49.5h with zero loop merges; root cause #1168 (no persistent scheduler) still `needs:ace`.
- **MOVEMENT (non-loop):** `software-support` grew to **ahead:103 behind:8** (+30 commits since last review, a batch of CLI/MCP refactor extractions) and its **RED CI is now GREEN** — the prior-review real mypy call-overload (`_detached_stdio` unpacked into `Popen`) is **FIXED by the branch author**; 3 consecutive Build&Test successes. Still **NO PR** (active author), NOT landed.
- Both open PRs remain **workflow-scope-walled** (loop token, #1302): #1310 (→main, BLOCKED) and #1298 (edits build.yml, BLOCKED/MERGEABLE).

## Loop Liveness (the health metric, not CI)
| Signal | Value |
|--------|-------|
| Last real develop merge | #1309 @ 2026-07-30 14:31Z |
| Time since | ~49.5h |
| Loop merges this window | 0 |
| Blocker | #1168 `needs:ace` (session-only crons; no persistent scheduler) — no progress since 07-20 |

## Milestone / Issue Health
| Metric | Value |
|--------|-------|
| Total open issues | 30 |
| `needs:ace` | 9 (#1302, #1168, #1136, #1105, #1057, #969, #935, #914, #897) |
| New issues (last 3d) | 0 |

## Verifications This Review
- **#1274 mask VERIFIED UNCHANGED** — build.yml:153 `continue-on-error: true` on desktop-tests still present (structurally-can't-fail check).
- **software-support CI green is REAL** — mypy runs in build.yml (`mypy naturo/`, line 83); the `Popen(**_detached_stdio)` call still exists but now type-checks; prior RED was genuine and is now resolved by the author.
- **#1298 confirmed workflow-walled** — sole file is `.github/workflows/build.yml`; MERGEABLE but BLOCKED; loop gh token lacks `workflow` scope (#1302).

## Actions Taken
- None on GitHub — all blockers are Ace-gated (#1168 scheduler, #1302 token scope) or belong to an active human/agent author (software-support). No new issues warranted; no stale in-progress labels to clear.

## Top 3 Priorities (next 24h)
1. **#1168** — pick a persistent scheduler so the loop stops freezing whenever no Orch session is alive. This is THE blocker; everything downstream is idle without it.
2. **#1302** — grant the loop gh token `workflow` scope (or route workflow-file PRs to a human), unblocking #1298 and #1310.
3. Land `software-support` once the author opens a PR — CI is now green; it needs a PR + Windows write-path desktop verify + the #1274 mask lifted before any "landed" claim.

## Risks
- **Loop death normalized:** 49.5h frozen with green CI + few open PRs reads as "healthy" but is DEAD. Health = time-since-last-merge, not CI color. Mitigation: keep #1168 at the top until scheduled.
- **software-support divergence:** behind:8 and growing at ahead:103 — the longer it sits without a PR, the harder the eventual rebase/merge. Mitigation: encourage a PR now that CI is green.
