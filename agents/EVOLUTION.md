# EVOLUTION — Dev & QA team self-improvement ledger

The Orchestrator updates this **every cycle** (Step 3.6 of `agents/local/orch-review.md`). Each row is one
concrete improvement to *how Dev or QA operates* (their cycle prompts / SOUL / RULES), driven by observed
evidence — or an explicit "no change — no new evidence" line. The goal: a reader should see Dev and QA
measurably sharpening over time. Read the recent rows before adding one so you build on them, not undo them.

| Date | Target | Observed weakness (evidence) | Change implemented | Expected effect |
|------|--------|------------------------------|--------------------|-----------------|
| 2026-06-16 | Dev + Orch | Dev marked issues `status:done` on PR-open; a cross-platform/collection break (#936) only surfaced on Linux CI → false "done" on a red PR | `dev-cycle.md`/`orch-review.md`: never `status:done` until CI green/MERGED; Orch flips done on merge + fixes red-CI PRs | No premature done; red PRs get fixed, not abandoned |
| 2026-06-17 | QA | A LIVE R-SEC-012 reproduction typed `$(rm -rf /)` / `$(echo INJECTED)` into a real window (focus-race hazard); injection payloads were hardcoded in QA_AGENT.md 第七轮 | `qa-cycle.md` + `QA_AGENT.md`: injection/sanitization tests are argv/pytest-only, NEVER via live `naturo type`; benign-content-only rule; code guard backstop | No command-like keystrokes on the live desktop; injection safety is a pytest tripwire |
| 2026-06-20 | Dev | Dev burned hourly cycles on small additive v0.3.4 bugs while the v0.3.2 recognition moat (#932) sat untouched — 避实就虚 | `dev-cycle.md`: **HARDEST-FIRST** core trait — pick the hardest release-blocking task of the current milestone first; decompose huge items into one-cycle slices; log WHY if skipping a hard one | Hard problems get attacked head-on → the version becomes a controllable release |
| 2026-06-20 | Orch (meta) | Orch did pure PR-triage for days and never autonomously evolved Dev/QA — `orch-review.md` had no such mandate (Ace flagged it) | Added **Step 3.6 — Evolve the team** (every cycle) + this ledger | The loop now self-improves its own agents continuously, trackably |
| 2026-06-20 | Dev | HARDEST-FIRST was in place, yet the 05:26Z Dev cycle still skipped the #1 moat task #932 citing "no JDK/SAP toolchain" — an **inherited/assumed** env block, while SPRINT FOCUS + commit e9ac590 already stated JDK 21 + JAB were provisioned (#932 unblocked); the 14:07Z cycle then picked #932 up. "Env-blocked" had become the new 避实就虚 loophole | `dev-cycle.md` HARDEST-FIRST: an env/toolchain block must be **proven this cycle** — run the actual probe (`java -version`/import/etc.) and paste cmd+output; never inherit a "blocked" note from prior STATE/log | Dev can no longer dodge the hardest task on a stale env assumption; skips require a live, evidenced probe |
| 2026-06-20 | Dev | The #871 window-targeting "harmonization" lane (#1037/#1053/#1056) aligned *which* flags each command accepts but not their *resolution semantics* — `--app` matches process-name in `see`/`capture`/`list windows` yet title-only in `app focus/move/…`. QA filed #1058 (13:40Z) then #1065 (14:42Z) — two `--app` parity bugs in back-to-back cycles, both escaping Dev self-review | `dev-cycle.md` Step 3 self-review: harmonization/family work must assert a shared flag *resolves the same way everywhere* as the gold-standard command (`see`/`capture`), pinned by a **cross-command parity test** — not just that the flag is accepted | "Harmonization" PRs stop shipping surface-only parity; semantic drift in shared flags is caught in self-review/test, not by QA two cycles later |
| 2026-06-20 (16:22) | Dev | `find` shipped **twice** with a mis-attributed error CODE that QA only caught after merge: #1047 (missing window/app → `UNKNOWN_ERROR` from a broad `except` swallowing the dead `WINDOW_NOT_FOUND` branch) and now #1067 (bad/non-image template → `CAPTURE_FAILED` because `Image.open(template)` sat inside the **same** `try` as the screen capture, so it inherited the capture-failure label). A recurring error-attribution weakness — distinct failure sources folded into one `try`/broad-`except` → wrong code → user debugs the wrong subsystem — not covered by item-4's vague "helpful errors?" | `dev-cycle.md` Step 3 self-review item 4: added an **Error attribution** rule — each distinct failure source gets its own `try`/`except` so the code names the real culprit (never inherit a sibling op's code via a shared `try`/broad `except`), pinned by a test driving each source to its own code | Error codes name the true culprit at ship time; this class stops escaping Dev self-review and reaching QA one feature later |
| 2026-06-20 (15:22) | — | no change — no new evidence. The freshest operating evidence (#1058/#1065 `--app` parity) was already addressed by the prior cycle's cross-command parity-test rule (added ~1h earlier). The two QA cycles since (14:42 filed #1065; 15:07 exemplary — recognition moat verified end-to-end, 0 new bugs, correctly avoided duplicate filings of #865/#1054) and the clean, root-cause-deep, end-to-end-verified Dev #932 land surfaced no new recurring operating weakness. Avoided churning the prompts per Step 3.6 | — | — |
| 2026-06-20 (08:52) | Dev | The fix for #1070 (PR #1072) was opened with auto-merge, but Linux/macOS CI went red: `test_screenshot_rejects_window_targeting_flags` and `test_screenshot_file_not_found` got `PLATFORM_ERROR` instead of `INVALID_INPUT`/`FILE_NOT_FOUND`. Root cause: `_find_with_image` ran the `_platform_supports_gui()` gate **before** the `--screenshot` argument validation, so on a non-GUI platform the bad-input → error-code contract changed (gate fired first). It passed on the Windows desktop and only broke on CI. A new class: an **env/platform gate ordered ahead of input validation makes the error-code contract platform-dependent** — distinct from the error-attribution row (which `try`/`except` owns a code) and the cross-platform-*collection* break (2026-06-16) | `dev-cycle.md` Step 3 self-review: added a **Platform-invariant validation order** rule — argument validation (+ its code) must precede any platform/capability gate so a bad input yields the same code on every OS; skip the gate on no-live-capture paths; pin with a test that does NOT mock the platform. Builds on (does not undo) the parity / error-attribution / option-coverage rows | Bad-input → error-code contracts hold identically across OSes; this Windows-green/CI-red class is caught in Dev self-review, not by a red auto-merge PR |
| 2026-06-20 (08:26) | Dev | The #1066 `find --image` feature was reported "verified end-to-end on a real 1920×1080 desktop", yet QA filed #1070 right after: `--image` **silently ignored** the documented `--screenshot` option and captured the live screen, returning `score 1.0` at the **wrong** coordinates (silent-correctness failure on the headline recognition feature). Dev's verification only walked the live-capture path it naturally defaulted to — the non-default `--screenshot` path was never exercised, so a whole documented option went unhonored + untested. Distinct from the prior error-attribution row (#1067/#1047 = wrong error *code*); this is wrong *output* from an unhandled option | `dev-cycle.md` Step 3 self-review item 4: added an **Option coverage** rule — a new mode of a multi-mode command must explicitly honor or explicitly reject every documented option it could touch (never silent fallback → confidently-wrong output), and the end-to-end check must walk the **non-default** option path, pinned by a test. Builds on (does not undo) the parity + error-attribution rows | New modes stop shipping with silently-ignored documented options; the "verified end-to-end" claim now requires exercising non-default option paths, catching silent-wrong-output before QA |
| 2026-06-20 (09:22) | — | no change — no new evidence. Two substantive rows shipped <1h earlier this morning (08:26 Option coverage; 08:52 Platform-invariant validation order). The Dev/QA cycles since were both clean: Dev #1062 (browser migration fixtures, `f56a760`) ran HARDEST-FIRST with live env probes, pre-empted 2 typeshed mypy findings before commit, all-green auto-merge — exemplary; QA #1070/#1067 returned PASS×2 with the exact black-screenshot silent-correctness repro and correctly avoided a duplicate filing (Rule 9). No new recurring *operating* weakness surfaced, and the freshest rules haven't had a cycle to be exercised yet — adding a 3rd change now would over-fit / churn the prompts, which Step 3.6 explicitly forbids | — | — |
| 2026-06-20 (18:22) | — | no change — no new evidence. The two operating signals since the 09:52Z cycle were both **exemplary, not weaknesses**: Dev (18:25Z) — while *scoping* #1063 (rpa-client equivalence) it smoke-tested the browser stack and **discovered + fixed a real deeper blocker** (#1075: `browser launch` omitted `--remote-allow-origins` → Chrome 111+ rejected naturo's own CDP WebSocket with 403, breaking every post-launch browser cmd), shipped #1076 with a launch-arg test, all-green auto-merge — the opposite of 避实就虚; QA (18:10Z) verified+closed #1069 using the real-desktop superpower (windows-open repro of the green-CI/red-desktop JAB gate, non-vacuous pre/post proof). #1075 is a **single** product-bug incident on pre-existing code, not a recurring *operating* weakness — attaching a new self-review rule to it would over-fit, which Step 3.6 explicitly forbids. Four substantive rows shipped in the prior ~10h (08:26 Option coverage, 08:52 Platform-invariant order, 09:52/17:52 Test hermeticity) and the freshest has not yet had a Dev cycle to be exercised. Honest "no change" row to avoid churn (Step 3.6's explicit allowance). | — | — |
| 2026-06-20 (09:52) | Dev | The #1068 JAB auto-cascade test (`test_jab.py::test_auto_fallback_includes_jab`) was a **non-hermetic, green-CI/red-desktop** false-confidence gate on the v0.3.2 recognition moat: it mocked `_ensure_core`/`_resolve_hwnd`/`enumerate_child_windows` but **not** `enumerate_hybrid_tree`, which the `auto` path still calls on an empty UIA tree. Headless `enumerate_hybrid_tree(hwnd=0)` returns nothing → JAB fallback runs → passes; a real desktop with open windows returns top-level windows → fallback skipped → fails. So the test's pass/fail depended on **host desktop state**, not the code — masking regressions and reddening `@desktop` Dev/QA runs (QA filed #1069, fixed by #1074, `832a1ac`). A **new class**: a test that mocks to force a path but leaves an un-mocked env-dependent call on that path. Distinct from the 08:52 *Platform-invariant validation order* row (production gate ordering changing the error-CODE contract, pinned by a test that deliberately does **not** mock the platform) — this is the inverse: a *logic-path* test that must mock **all** host-dependent calls to be deterministic | `dev-cycle.md` Step 3 self-review item 4: added a **Test hermeticity** rule — a test that mocks to drive a specific path must neutralize **every** host/environment-dependent call that path can reach (trace the forced path post-mock; a test whose result flips with ambient desktop state is a false-confidence gate). Builds on (does not undo) the parity / error-attribution / option-coverage / platform-invariant rows | Logic-path tests stop passing in headless CI while failing on a real desktop; under-mocked env calls are caught in Dev self-review, not by QA filing a tech-debt issue one cycle later |
| 2026-06-20 (18:52) | — | no change — no new evidence. The two operating signals since the 18:22Z cycle were both **exemplary, not weaknesses**: Dev (18:48Z) shipped the first slice of umbrella #766 (PR #1078, `7d2b969`) — HARDEST-FIRST with live env probes (proved #1063/#763/#1060/#932/#934 blocks *this cycle*, didn't inherit), smoke-FIRST before building, **hermetic** tests (isolated `tempfile.mkdtemp` user_data_dir + free TCP port + explicit terminate/kill teardown, 0 stray chrome verified) — directly applying the freshest Test-hermeticity rule — and correctly did **not** auto-close the multi-fixture umbrella (used "part of #766", removed status:in-progress, returned to pool with a remaining-fixture list); QA (18:42Z) verified+closed #1075 with the real-desktop superpower (live Chrome 111+ CDP round-trip the argv asserts can't catch + a **non-vacuous inverse proof** launching Chrome without the flag to reproduce the exact 403, plus full taskkill cleanup). No recurring *operating* weakness surfaced; five+ substantive rows shipped in the prior ~12h and the Test-hermeticity rule was just exercised cleanly by #1078 — adding a rule on exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. | — | — |
| 2026-06-20 (19:22) | — | no change — existing gate already covers it (not a rule gap). Team-Dev PR **#1081** (iframe click/hover, fixes #1080) enabled auto-merge but went red on required Ubuntu 3.9/3.12 + macOS 3.9/3.12 Python Tests: the updated mock stubs in `tests/test_browser_element.py`/`test_browser.py` are too short for the new multi-call `_get_click_point` → deterministic `StopIteration` (`_element.py:272`, `next(effect)`) + `RuntimeError: Cannot determine element position` (`:113`). These are **mock-based, platform-independent** failures that `dev-cycle.md` Step 3.3's own gate (`python -m pytest tests/ -x -q`) would have caught locally → an **adherence miss, not a missing rule**; a new "run your tests" rule would be redundant + churn (Step 3.6 forbids). Orc surfaced the root cause via a PR comment (per the 2026-06-16 red-PR rule); **Dev fixed the stale mocks and auto-merge landed it green (`13a8c54`) the same cycle** — confirming the gap was *gate adherence*, not the rules. **Flag to watch:** if a *second* team PR enables auto-merge while its own modified test module is red, strengthen Step 4 to require pasting the modified-module `pytest` summary in the PR report (make "I ran the tests" auditable). | — | — |
| 2026-06-20 (19:52) | — | no change — no new *agent operating* weakness. The two signals since the 19:22Z cycle: **QA (19:42Z)** verified+closed #1080 on the real desktop (live CDP nested-iframe click round-trip + top-level regression + pinned `@desktop` test) and, beyond the verdict, **surfaced a genuine never-lie footgun** as a lateral observation (hidden/zero-area element → `getBoundingClientRect` all-zeros → click silently dispatched at viewport (0,0) instead of raising) without over-filing on the unattended cycle — exemplary judgment; Orc filed it this cycle as **#1083** after confirming it in `_element.py:381-400`. **Dev (19:37Z)** did not run — `cycle ERROR: loop-state.log in use by another process` (Dev + QA runners both started 19:37:02 and contended on the shared append log → Dev lost the race, skipped the slot). That is an **infra/runner concurrency failure, not agent behavior** — same uncoordinated-runners family as ops issue **#935**, where it was recorded as fresh evidence (human-only/harness → stays queued). Attaching a Dev/QA self-review rule to an exemplary QA cycle + a harness file-lock would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row; the 19:22 flag-to-watch (2nd red auto-merge module) has not recurred. | — | — |
| 2026-06-20 (20:22) | QA | **Recurring class: QA's own measurement harness produces false signals it must catch before they become false verdicts/bugs.** Three distinct manifestations now on record: (1) the `naturo-qa` editable install resolving `import naturo` to a **sibling** worktree's stale code — a documented FALSE FAIL at 16:40Z (#963) and re-hit at 19:42Z (probe imported `naturo-qa-mariana`, pre-#1076 code; QA re-ran with PYTHONPATH to test the right code); (2) **20:20Z** a cp936 console decoding naturo's correct UTF-8 as mojibake + a lone surrogate `\udc98` that looked like a **P0 Unicode-corruption bug** — a near-miss filing QA caught only by tracing to OS ground truth; (3) **20:20Z** a `naturo … \| head` pipe reporting `head`'s exit code, making error paths look like exit 0 (retracted). QA caught all three, but only by ad-hoc instinct — `qa-cycle.md` had no standing rule to **rule out the harness first**, so each was a near-miss rather than a checklist step. Not over-fit to one incident: a real recurring class (harness/pipe/locale/editable-install lies) spanning multiple cycles, distinct from the existing rules (input-safety, hermeticity which is a *Dev test* rule) | `qa-cycle.md` Step 2: added item 4 — **rule out your own measurement harness before trusting a surprising defect**: re-run on the cleanest path (no pipe; redirect to file + strict-UTF-8 decode; confirm `naturo.__file__` resolves under *this* worktree) and trust OS ground-truth over the harness's rendering before filing/FAILing; if the signal vanishes on the clean path it was the harness | False bugs/verdicts from QA's own tooling (pipe exit codes, console locale, sibling-worktree imports) are caught by a standing checklist step, not ad-hoc instinct — fewer near-miss P0 misfilings and stale-code false FAILs |
| 2026-06-20 (12:52Z) | — | no change — no new evidence. The two operating signals since the 20:22Z cycle were both **exemplary, not weaknesses**: **Dev (20:50Z)** shipped the #766 infinite-scroll Before/After equivalence slice (PR #1085, `b7a488d`, all required CI green incl. cross-platform Py3.9–3.13 + DLL + CodeQL) — ran HARDEST-FIRST with **live env probes proven this cycle** (`java -version`=JDK 21, JAB env confirmed; correctly fell to the next-hardest *actionable* slice because the input-injection moat slices are blocked by the active input-safety freeze #975 and OCR by needs:ace #1077, not skipped on a stale assumption), wrote a **hermetic** CDP/headless test, and **found + fixed a real fixture bug** (`infinite-scroll.html` bare `var status` aliasing `window.status` → `setAttribute` threw → scroll listener never attached → fixture silently stuck at 6 cards) with empirical proof, and correctly kept umbrella #766 OPEN (used "part of", removed status:in-progress, listed remaining fixtures). **QA (20:50Z)** ran a clean exploratory cycle, filed one genuine round-trip bug (#1084) with a proven repro, and **applied the freshly-added 20:22Z harness rule directly** — tracing a scary cp936 mojibake / lone-surrogate to its own console decode and a `\| head` exit-code artifact to OS ground truth, filing **no** false bug. The 20:22Z QA-harness rule was just added <1h earlier and was exercised cleanly this very cycle; adding another change on top of two exemplary cycles would over-fit / churn the prompts, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) has not recurred — #1085 landed all-green. | — | — |
| 2026-06-20 (21:22Z) | Dev | **New recurring class: error codes emitted as bare string literals, not registered in the `ErrorCode` enum / `_RECOVERY_HINTS`, so the envelope's category/recoverable/hint silently degrade to `unknown`/`false`/none — breaking the agent self-correction contract while the envelope *shape* still looks fine.** QA filed **#1086** (verified in code this cycle): the eN snapshot-ref path emits bare `"REF_NOT_FOUND"` at 6 interaction callsites (`_click.py:210`, `_common.py:657`, `_mouse.py:269`/`:318`, `_press.py:210`, `_type.py:246`) — absent from `errors.py` (only `STALE_SNAPSHOT_CACHE` is registered) AND `error_helpers.py::_RECOVERY_HINTS` → `category_for_code('REF_NOT_FOUND')=='unknown'`, recoverable:false, no hint; the identical condition on `get`/`set` correctly yields the registered `STALE_SNAPSHOT_CACHE` (session/recoverable/hinted). Not a one-off: it is the still-open eN-resolve leg of the #877/#884/#1004/#1001 contract family — **#1004 fixed only the backend `ElementNotFoundError` path and left the sibling callsites leaking** (partial-fix pattern). Distinct from the existing **Error attribution** row (that owns *which* code via try/except; here the code is descriptively named but *unregistered*, so the derived metadata defaults wrong) and from envelope-*shape* checks (#1001 only asserts shape) | `dev-cycle.md` Step 3 self-review item 4: added an **Error-code registration** rule — never emit a raw string as a code; use an `ErrorCode` member and, for a new one, register it in the enum + category map + `_RECOVERY_HINTS` in the same diff; when fixing a taxonomy/recovery bug, sweep **every** sibling callsite emitting that condition (not just the reported path); pin with a test asserting *category + recoverable*, not just envelope shape. Builds on (does not undo) the error-attribution / parity / option-coverage / platform-order / hermeticity rows | New/touched error codes carry correct category+recoverable+hint at ship time; taxonomy fixes stop being partial (one path fixed, siblings leaking); the `unknown`/`recoverable:false` self-correction-contract break is caught in Dev self-review, not by QA one cycle later |
| 2026-06-20 (14:24Z) | — | no change — no new evidence. The only operating signal since the 13:55Z cycle is **Dev landing PR #1091** (`image-captcha click-offset` Before/After equivalence, **part of #766**, `a14a46d`, all-green auto-merge SQUASH) — which again **found + fixed a dead fixture** (`captcha-image.html`). That is the **third consecutive** #766 slice to surface+repair a broken fixture (#1085 infinite-scroll `var status` aliasing `window.status`; #1090 dead `tabs.html`; now #1091 dead captcha image) — but this is **exemplary operating behaviour, not a weakness**: Dev catches and fixes the dead fixture with empirical proof rather than papering over a vacuously-passing test, and correctly keeps umbrella #766 OPEN ("part of", `closingIssues` empty). No recurring *operating* weakness surfaced; the freshest rule (13:22Z Error-code registration) was added <2h earlier and hasn't yet had a distinct new test — attaching a rule to an exemplary cycle would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) has not recurred — #1091 landed all-green. | — | — |
| 2026-06-20 (13:52Z) | — | no change — no new evidence of a NEW operating weakness; the only recurring class this cycle is **already covered by the rule added ~30 min earlier (13:22Z, Error-code registration)**. The two signals since the 13:22Z cycle were both **exemplary**: **Dev (~13:5xZ)** landed PR **#1090** (tab-management Before/After equivalence, part of #766, `bcda034`) — all-green auto-merge, **found + fixed a dead `tabs.html` fixture**, correctly kept umbrella #766 OPEN ("part of", no auto-close), and while *writing* the network-interception slice **caught a never-lie doc bug** and filed it cleanly (**#1088**, two scoped options) rather than papering over it — the opposite of 避实就虚. **QA (~13:5xZ)** filed **#1089** (wait appear-mode timeout omits the standard `error` block) with a proven root cause (`wait_cmd.py:173-194`) and the precise contract insight that it **slips the #1001 auto-enumeration contract because it returns `success:false` *without raising***. #1089 is the same **error-envelope-completeness** family the 13:22Z Error-code-registration rule just addressed (that rule already pushes Dev to attach a registered code + pin *category+recoverable*, not just shape); attaching a second envelope rule 30 min later would over-fit / churn, which Step 3.6 explicitly forbids. #1089 is logged as **reinforcing evidence** for the 13:22Z rule, not a new gap. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) has not recurred — #1090 landed all-green. | — | — |
| 2026-06-20 (14:54Z) | — | no rule change — **flag to watch (first instance).** New *operating* observation, not a product bug: Dev landed team PR **#1092** (`hover-reveal-menu` Before/After equivalence, **part of #766**, `afc6dde`, all-green auto-merge SQUASH) — exemplary slice — but the post-merge **umbrella de-claim was skipped**: the Dev cycle ended 14:44Z and #1092's auto-merge landed 14:46Z, so the usual in-cycle cleanup (remove `status:in-progress` + comment remaining rows on #766) never ran, leaving #766 `status:in-progress` with no open PR. Orc completed the handoff this cycle (removed the label + commented). This is a **structural timing gap** (auto-merge is async vs. the cycle boundary), but the **prior 3 #766 slices (#1085/#1090/#1091) handled the de-claim cleanly in-cycle** → this is the **first** manifestation. Per the project's demonstrated discipline (flag-to-watch on first instance, e.g. the 19:22Z red-auto-merge flag which then *didn't* recur; rule only on recurrence) and Step 3.6's explicit ban on over-fitting a single incident, recorded as a flag, not a rule. **If it recurs:** add a one-line `dev-cycle.md` rule — for an umbrella slice, de-claim `status:in-progress` + write the remaining-rows comment at the moment you enable auto-merge (the umbrella stays open regardless of the slice's async merge outcome), not after the merge. | — (flag only; remediated by Orc label cleanup this cycle) | — |
