# EVOLUTION — Dev & QA team self-improvement ledger

The Orchestrator updates this **every cycle** (Step 3.6 of `agents/local/orch-review.md`). Each row is one
concrete improvement to *how Dev or QA operates* (their cycle prompts / SOUL / RULES), driven by observed
evidence — or an explicit "no change — no new evidence" line. The goal: a reader should see Dev and QA
measurably sharpening over time. Read the recent rows before adding one so you build on them, not undo them.

| Date | Target | Observed weakness (evidence) | Change implemented | Expected effect |
|------|--------|------------------------------|--------------------|-----------------|
| 2026-06-16 | Dev + Orch | Dev marked issues `status:done` on PR-open; a cross-platform/collection break (#936) only surfaced on Linux CI → false "done" on a red PR | `dev-cycle.md`/`orch-review.md`: never `status:done` until CI green/MERGED; Orch flips done on merge + fixes red-CI PRs | No premature done; red PRs get fixed, not abandoned |
| 2026-06-17 | QA | A LIVE R-SEC-012 reproduction typed `$(rm -rf /)` / `$(echo INJECTED)` into a real window (focus-race hazard); injection payloads were hardcoded in QA_AGENT.md 第七轮 | `qa-cycle.md` + `QA_AGENT.md`: injection/sanitization tests are argv/pytest-only, NEVER via live `naturo type`; benign-content-only rule; code guard backstop | No command-like keystrokes on the live desktop; injection safety is a pytest tripwire |
| 2026-06-20 | Dev | Dev burned hourly cycles on small additive v0.3.4 bugs while the v0.3.2 recognition moat (#932) sat untouched — 避实就虚 | `dev-cycle.md`: **HARDEST-FIRST** core trait — pick the hardest release-blocking task of the current milestone first; decompose huge items into one-cycle slices; log WHY if skipping a hard one | Hard problems get attacked head-on → the version becomes a controllable release |
| 2026-06-20 | Orch (meta) | Orch did pure PR-triage for days and never autonomously evolved Dev/QA — `orch-review.md` had no such mandate (Ace flagged it) | Added **Step 3.6 — Evolve the team** (every cycle) + this ledger | The loop now self-improves its own agents continuously, trackably |
| 2026-06-20 | Dev | HARDEST-FIRST was in place, yet the 05:26Z Dev cycle still skipped the #1 moat task #932 citing "no JDK/SAP toolchain" — an **inherited/assumed** env block, while SPRINT FOCUS + commit e9ac590 already stated JDK 21 + JAB were provisioned (#932 unblocked); the 14:07Z cycle then picked #932 up. "Env-blocked" had become the new 避实就虚 loophole | `dev-cycle.md` HARDEST-FIRST: an env/toolchain block must be **proven this cycle** — run the actual probe (`java -version`/import/etc.) and paste cmd+output; never inherit a "blocked" note from prior STATE/log | Dev can no longer dodge the hardest task on a stale env assumption; skips require a live, evidenced probe |
| 2026-06-20 | Dev | The #871 window-targeting "harmonization" lane (#1037/#1053/#1056) aligned *which* flags each command accepts but not their *resolution semantics* — `--app` matches process-name in `see`/`capture`/`list windows` yet title-only in `app focus/move/…`. QA filed #1058 (13:40Z) then #1065 (14:42Z) — two `--app` parity bugs in back-to-back cycles, both escaping Dev self-review | `dev-cycle.md` Step 3 self-review: harmonization/family work must assert a shared flag *resolves the same way everywhere* as the gold-standard command (`see`/`capture`), pinned by a **cross-command parity test** — not just that the flag is accepted | "Harmonization" PRs stop shipping surface-only parity; semantic drift in shared flags is caught in self-review/test, not by QA two cycles later |
| 2026-06-20 (16:22) | Dev | `find` shipped **twice** with a mis-attributed error CODE that QA only caught after merge: #1047 (missing window/app → `UNKNOWN_ERROR` from a broad `except` swallowing the dead `WINDOW_NOT_FOUND` branch) and now #1067 (bad/non-image template → `CAPTURE_FAILED` because `Image.open(template)` sat inside the **same** `try` as the screen capture, so it inherited the capture-failure label). A recurring error-attribution weakness — distinct failure sources folded into one `try`/broad-`except` → wrong code → user debugs the wrong subsystem — not covered by item-4's vague "helpful errors?" | `dev-cycle.md` Step 3 self-review item 4: added an **Error attribution** rule — each distinct failure source gets its own `try`/`except` so the code names the real culprit (never inherit a sibling op's code via a shared `try`/broad `except`), pinned by a test driving each source to its own code | Error codes name the true culprit at ship time; this class stops escaping Dev self-review and reaching QA one feature later |
| 2026-06-20 (15:22) | — | no change — no new evidence. The freshest operating evidence (#1058/#1065 `--app` parity) was already addressed by the prior cycle's cross-command parity-test rule (added ~1h earlier). The two QA cycles since (14:42 filed #1065; 15:07 exemplary — recognition moat verified end-to-end, 0 new bugs, correctly avoided duplicate filings of #865/#1054) and the clean, root-cause-deep, end-to-end-verified Dev #932 land surfaced no new recurring operating weakness. Avoided churning the prompts per Step 3.6 | — | — |
| 2026-06-20 (08:52) | Dev | The fix for #1070 (PR #1072) was opened with auto-merge, but Linux/macOS CI went red: `test_screenshot_rejects_window_targeting_flags` and `test_screenshot_file_not_found` got `PLATFORM_ERROR` instead of `INVALID_INPUT`/`FILE_NOT_FOUND`. Root cause: `_find_with_image` ran the `_platform_supports_gui()` gate **before** the `--screenshot` argument validation, so on a non-GUI platform the bad-input → error-code contract changed (gate fired first). It passed on the Windows desktop and only broke on CI. A new class: an **env/platform gate ordered ahead of input validation makes the error-code contract platform-dependent** — distinct from the error-attribution row (which `try`/`except` owns a code) and the cross-platform-*collection* break (2026-06-16) | `dev-cycle.md` Step 3 self-review: added a **Platform-invariant validation order** rule — argument validation (+ its code) must precede any platform/capability gate so a bad input yields the same code on every OS; skip the gate on no-live-capture paths; pin with a test that does NOT mock the platform. Builds on (does not undo) the parity / error-attribution / option-coverage rows | Bad-input → error-code contracts hold identically across OSes; this Windows-green/CI-red class is caught in Dev self-review, not by a red auto-merge PR |
| 2026-06-20 (08:26) | Dev | The #1066 `find --image` feature was reported "verified end-to-end on a real 1920×1080 desktop", yet QA filed #1070 right after: `--image` **silently ignored** the documented `--screenshot` option and captured the live screen, returning `score 1.0` at the **wrong** coordinates (silent-correctness failure on the headline recognition feature). Dev's verification only walked the live-capture path it naturally defaulted to — the non-default `--screenshot` path was never exercised, so a whole documented option went unhonored + untested. Distinct from the prior error-attribution row (#1067/#1047 = wrong error *code*); this is wrong *output* from an unhandled option | `dev-cycle.md` Step 3 self-review item 4: added an **Option coverage** rule — a new mode of a multi-mode command must explicitly honor or explicitly reject every documented option it could touch (never silent fallback → confidently-wrong output), and the end-to-end check must walk the **non-default** option path, pinned by a test. Builds on (does not undo) the parity + error-attribution rows | New modes stop shipping with silently-ignored documented options; the "verified end-to-end" claim now requires exercising non-default option paths, catching silent-wrong-output before QA |
| 2026-06-20 (09:22) | — | no change — no new evidence. Two substantive rows shipped <1h earlier this morning (08:26 Option coverage; 08:52 Platform-invariant validation order). The Dev/QA cycles since were both clean: Dev #1062 (browser migration fixtures, `f56a760`) ran HARDEST-FIRST with live env probes, pre-empted 2 typeshed mypy findings before commit, all-green auto-merge — exemplary; QA #1070/#1067 returned PASS×2 with the exact black-screenshot silent-correctness repro and correctly avoided a duplicate filing (Rule 9). No new recurring *operating* weakness surfaced, and the freshest rules haven't had a cycle to be exercised yet — adding a 3rd change now would over-fit / churn the prompts, which Step 3.6 explicitly forbids | — | — |
| 2026-06-20 (18:22) | — | no change — no new evidence. The two operating signals since the 09:52Z cycle were both **exemplary, not weaknesses**: Dev (18:25Z) — while *scoping* #1063 (rpa-client equivalence) it smoke-tested the browser stack and **discovered + fixed a real deeper blocker** (#1075: `browser launch` omitted `--remote-allow-origins` → Chrome 111+ rejected naturo's own CDP WebSocket with 403, breaking every post-launch browser cmd), shipped #1076 with a launch-arg test, all-green auto-merge — the opposite of 避实就虚; QA (18:10Z) verified+closed #1069 using the real-desktop superpower (windows-open repro of the green-CI/red-desktop JAB gate, non-vacuous pre/post proof). #1075 is a **single** product-bug incident on pre-existing code, not a recurring *operating* weakness — attaching a new self-review rule to it would over-fit, which Step 3.6 explicitly forbids. Four substantive rows shipped in the prior ~10h (08:26 Option coverage, 08:52 Platform-invariant order, 09:52/17:52 Test hermeticity) and the freshest has not yet had a Dev cycle to be exercised. Honest "no change" row to avoid churn (Step 3.6's explicit allowance). | — | — |
| 2026-06-20 (09:52) | Dev | The #1068 JAB auto-cascade test (`test_jab.py::test_auto_fallback_includes_jab`) was a **non-hermetic, green-CI/red-desktop** false-confidence gate on the v0.3.2 recognition moat: it mocked `_ensure_core`/`_resolve_hwnd`/`enumerate_child_windows` but **not** `enumerate_hybrid_tree`, which the `auto` path still calls on an empty UIA tree. Headless `enumerate_hybrid_tree(hwnd=0)` returns nothing → JAB fallback runs → passes; a real desktop with open windows returns top-level windows → fallback skipped → fails. So the test's pass/fail depended on **host desktop state**, not the code — masking regressions and reddening `@desktop` Dev/QA runs (QA filed #1069, fixed by #1074, `832a1ac`). A **new class**: a test that mocks to force a path but leaves an un-mocked env-dependent call on that path. Distinct from the 08:52 *Platform-invariant validation order* row (production gate ordering changing the error-CODE contract, pinned by a test that deliberately does **not** mock the platform) — this is the inverse: a *logic-path* test that must mock **all** host-dependent calls to be deterministic | `dev-cycle.md` Step 3 self-review item 4: added a **Test hermeticity** rule — a test that mocks to drive a specific path must neutralize **every** host/environment-dependent call that path can reach (trace the forced path post-mock; a test whose result flips with ambient desktop state is a false-confidence gate). Builds on (does not undo) the parity / error-attribution / option-coverage / platform-invariant rows | Logic-path tests stop passing in headless CI while failing on a real desktop; under-mocked env calls are caught in Dev self-review, not by QA filing a tech-debt issue one cycle later |
| 2026-06-20 (18:52) | — | no change — no new evidence. The two operating signals since the 18:22Z cycle were both **exemplary, not weaknesses**: Dev (18:48Z) shipped the first slice of umbrella #766 (PR #1078, `7d2b969`) — HARDEST-FIRST with live env probes (proved #1063/#763/#1060/#932/#934 blocks *this cycle*, didn't inherit), smoke-FIRST before building, **hermetic** tests (isolated `tempfile.mkdtemp` user_data_dir + free TCP port + explicit terminate/kill teardown, 0 stray chrome verified) — directly applying the freshest Test-hermeticity rule — and correctly did **not** auto-close the multi-fixture umbrella (used "part of #766", removed status:in-progress, returned to pool with a remaining-fixture list); QA (18:42Z) verified+closed #1075 with the real-desktop superpower (live Chrome 111+ CDP round-trip the argv asserts can't catch + a **non-vacuous inverse proof** launching Chrome without the flag to reproduce the exact 403, plus full taskkill cleanup). No recurring *operating* weakness surfaced; five+ substantive rows shipped in the prior ~12h and the Test-hermeticity rule was just exercised cleanly by #1078 — adding a rule on exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. | — | — |
| 2026-06-20 (19:22) | — | no change — existing gate already covers it (not a rule gap). Team-Dev PR **#1081** (iframe click/hover, fixes #1080) enabled auto-merge but went red on required Ubuntu 3.9/3.12 + macOS 3.9/3.12 Python Tests: the updated mock stubs in `tests/test_browser_element.py`/`test_browser.py` are too short for the new multi-call `_get_click_point` → deterministic `StopIteration` (`_element.py:272`, `next(effect)`) + `RuntimeError: Cannot determine element position` (`:113`). These are **mock-based, platform-independent** failures that `dev-cycle.md` Step 3.3's own gate (`python -m pytest tests/ -x -q`) would have caught locally → an **adherence miss, not a missing rule**; a new "run your tests" rule would be redundant + churn (Step 3.6 forbids). Orc surfaced the root cause via a PR comment (per the 2026-06-16 red-PR rule); **Dev fixed the stale mocks and auto-merge landed it green (`13a8c54`) the same cycle** — confirming the gap was *gate adherence*, not the rules. **Flag to watch:** if a *second* team PR enables auto-merge while its own modified test module is red, strengthen Step 4 to require pasting the modified-module `pytest` summary in the PR report (make "I ran the tests" auditable). | — | — |
| 2026-06-20 (19:52) | — | no change — no new *agent operating* weakness. The two signals since the 19:22Z cycle: **QA (19:42Z)** verified+closed #1080 on the real desktop (live CDP nested-iframe click round-trip + top-level regression + pinned `@desktop` test) and, beyond the verdict, **surfaced a genuine never-lie footgun** as a lateral observation (hidden/zero-area element → `getBoundingClientRect` all-zeros → click silently dispatched at viewport (0,0) instead of raising) without over-filing on the unattended cycle — exemplary judgment; Orc filed it this cycle as **#1083** after confirming it in `_element.py:381-400`. **Dev (19:37Z)** did not run — `cycle ERROR: loop-state.log in use by another process` (Dev + QA runners both started 19:37:02 and contended on the shared append log → Dev lost the race, skipped the slot). That is an **infra/runner concurrency failure, not agent behavior** — same uncoordinated-runners family as ops issue **#935**, where it was recorded as fresh evidence (human-only/harness → stays queued). Attaching a Dev/QA self-review rule to an exemplary QA cycle + a harness file-lock would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row; the 19:22 flag-to-watch (2nd red auto-merge module) has not recurred. | — | — |
| 2026-06-20 (20:22) | QA | **Recurring class: QA's own measurement harness produces false signals it must catch before they become false verdicts/bugs.** Three distinct manifestations now on record: (1) the `naturo-qa` editable install resolving `import naturo` to a **sibling** worktree's stale code — a documented FALSE FAIL at 16:40Z (#963) and re-hit at 19:42Z (probe imported `naturo-qa-mariana`, pre-#1076 code; QA re-ran with PYTHONPATH to test the right code); (2) **20:20Z** a cp936 console decoding naturo's correct UTF-8 as mojibake + a lone surrogate `\udc98` that looked like a **P0 Unicode-corruption bug** — a near-miss filing QA caught only by tracing to OS ground truth; (3) **20:20Z** a `naturo … \| head` pipe reporting `head`'s exit code, making error paths look like exit 0 (retracted). QA caught all three, but only by ad-hoc instinct — `qa-cycle.md` had no standing rule to **rule out the harness first**, so each was a near-miss rather than a checklist step. Not over-fit to one incident: a real recurring class (harness/pipe/locale/editable-install lies) spanning multiple cycles, distinct from the existing rules (input-safety, hermeticity which is a *Dev test* rule) | `qa-cycle.md` Step 2: added item 4 — **rule out your own measurement harness before trusting a surprising defect**: re-run on the cleanest path (no pipe; redirect to file + strict-UTF-8 decode; confirm `naturo.__file__` resolves under *this* worktree) and trust OS ground-truth over the harness's rendering before filing/FAILing; if the signal vanishes on the clean path it was the harness | False bugs/verdicts from QA's own tooling (pipe exit codes, console locale, sibling-worktree imports) are caught by a standing checklist step, not ad-hoc instinct — fewer near-miss P0 misfilings and stale-code false FAILs |
| 2026-06-20 (12:52Z) | — | no change — no new evidence. The two operating signals since the 20:22Z cycle were both **exemplary, not weaknesses**: **Dev (20:50Z)** shipped the #766 infinite-scroll Before/After equivalence slice (PR #1085, `b7a488d`, all required CI green incl. cross-platform Py3.9–3.13 + DLL + CodeQL) — ran HARDEST-FIRST with **live env probes proven this cycle** (`java -version`=JDK 21, JAB env confirmed; correctly fell to the next-hardest *actionable* slice because the input-injection moat slices are blocked by the active input-safety freeze #975 and OCR by needs:ace #1077, not skipped on a stale assumption), wrote a **hermetic** CDP/headless test, and **found + fixed a real fixture bug** (`infinite-scroll.html` bare `var status` aliasing `window.status` → `setAttribute` threw → scroll listener never attached → fixture silently stuck at 6 cards) with empirical proof, and correctly kept umbrella #766 OPEN (used "part of", removed status:in-progress, listed remaining fixtures). **QA (20:50Z)** ran a clean exploratory cycle, filed one genuine round-trip bug (#1084) with a proven repro, and **applied the freshly-added 20:22Z harness rule directly** — tracing a scary cp936 mojibake / lone-surrogate to its own console decode and a `\| head` exit-code artifact to OS ground truth, filing **no** false bug. The 20:22Z QA-harness rule was just added <1h earlier and was exercised cleanly this very cycle; adding another change on top of two exemplary cycles would over-fit / churn the prompts, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) has not recurred — #1085 landed all-green. | — | — |
| 2026-06-20 (21:22Z) | Dev | **New recurring class: error codes emitted as bare string literals, not registered in the `ErrorCode` enum / `_RECOVERY_HINTS`, so the envelope's category/recoverable/hint silently degrade to `unknown`/`false`/none — breaking the agent self-correction contract while the envelope *shape* still looks fine.** QA filed **#1086** (verified in code this cycle): the eN snapshot-ref path emits bare `"REF_NOT_FOUND"` at 6 interaction callsites (`_click.py:210`, `_common.py:657`, `_mouse.py:269`/`:318`, `_press.py:210`, `_type.py:246`) — absent from `errors.py` (only `STALE_SNAPSHOT_CACHE` is registered) AND `error_helpers.py::_RECOVERY_HINTS` → `category_for_code('REF_NOT_FOUND')=='unknown'`, recoverable:false, no hint; the identical condition on `get`/`set` correctly yields the registered `STALE_SNAPSHOT_CACHE` (session/recoverable/hinted). Not a one-off: it is the still-open eN-resolve leg of the #877/#884/#1004/#1001 contract family — **#1004 fixed only the backend `ElementNotFoundError` path and left the sibling callsites leaking** (partial-fix pattern). Distinct from the existing **Error attribution** row (that owns *which* code via try/except; here the code is descriptively named but *unregistered*, so the derived metadata defaults wrong) and from envelope-*shape* checks (#1001 only asserts shape) | `dev-cycle.md` Step 3 self-review item 4: added an **Error-code registration** rule — never emit a raw string as a code; use an `ErrorCode` member and, for a new one, register it in the enum + category map + `_RECOVERY_HINTS` in the same diff; when fixing a taxonomy/recovery bug, sweep **every** sibling callsite emitting that condition (not just the reported path); pin with a test asserting *category + recoverable*, not just envelope shape. Builds on (does not undo) the error-attribution / parity / option-coverage / platform-order / hermeticity rows | New/touched error codes carry correct category+recoverable+hint at ship time; taxonomy fixes stop being partial (one path fixed, siblings leaking); the `unknown`/`recoverable:false` self-correction-contract break is caught in Dev self-review, not by QA one cycle later |
| 2026-06-20 (14:24Z) | — | no change — no new evidence. The only operating signal since the 13:55Z cycle is **Dev landing PR #1091** (`image-captcha click-offset` Before/After equivalence, **part of #766**, `a14a46d`, all-green auto-merge SQUASH) — which again **found + fixed a dead fixture** (`captcha-image.html`). That is the **third consecutive** #766 slice to surface+repair a broken fixture (#1085 infinite-scroll `var status` aliasing `window.status`; #1090 dead `tabs.html`; now #1091 dead captcha image) — but this is **exemplary operating behaviour, not a weakness**: Dev catches and fixes the dead fixture with empirical proof rather than papering over a vacuously-passing test, and correctly keeps umbrella #766 OPEN ("part of", `closingIssues` empty). No recurring *operating* weakness surfaced; the freshest rule (13:22Z Error-code registration) was added <2h earlier and hasn't yet had a distinct new test — attaching a rule to an exemplary cycle would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) has not recurred — #1091 landed all-green. | — | — |
| 2026-06-20 (13:52Z) | — | no change — no new evidence of a NEW operating weakness; the only recurring class this cycle is **already covered by the rule added ~30 min earlier (13:22Z, Error-code registration)**. The two signals since the 13:22Z cycle were both **exemplary**: **Dev (~13:5xZ)** landed PR **#1090** (tab-management Before/After equivalence, part of #766, `bcda034`) — all-green auto-merge, **found + fixed a dead `tabs.html` fixture**, correctly kept umbrella #766 OPEN ("part of", no auto-close), and while *writing* the network-interception slice **caught a never-lie doc bug** and filed it cleanly (**#1088**, two scoped options) rather than papering over it — the opposite of 避实就虚. **QA (~13:5xZ)** filed **#1089** (wait appear-mode timeout omits the standard `error` block) with a proven root cause (`wait_cmd.py:173-194`) and the precise contract insight that it **slips the #1001 auto-enumeration contract because it returns `success:false` *without raising***. #1089 is the same **error-envelope-completeness** family the 13:22Z Error-code-registration rule just addressed (that rule already pushes Dev to attach a registered code + pin *category+recoverable*, not just shape); attaching a second envelope rule 30 min later would over-fit / churn, which Step 3.6 explicitly forbids. #1089 is logged as **reinforcing evidence** for the 13:22Z rule, not a new gap. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) has not recurred — #1090 landed all-green. | — | — |
| 2026-06-20 (14:54Z) | — | no rule change — **flag to watch (first instance).** New *operating* observation, not a product bug: Dev landed team PR **#1092** (`hover-reveal-menu` Before/After equivalence, **part of #766**, `afc6dde`, all-green auto-merge SQUASH) — exemplary slice — but the post-merge **umbrella de-claim was skipped**: the Dev cycle ended 14:44Z and #1092's auto-merge landed 14:46Z, so the usual in-cycle cleanup (remove `status:in-progress` + comment remaining rows on #766) never ran, leaving #766 `status:in-progress` with no open PR. Orc completed the handoff this cycle (removed the label + commented). This is a **structural timing gap** (auto-merge is async vs. the cycle boundary), but the **prior 3 #766 slices (#1085/#1090/#1091) handled the de-claim cleanly in-cycle** → this is the **first** manifestation. Per the project's demonstrated discipline (flag-to-watch on first instance, e.g. the 19:22Z red-auto-merge flag which then *didn't* recur; rule only on recurrence) and Step 3.6's explicit ban on over-fitting a single incident, recorded as a flag, not a rule. **If it recurs:** add a one-line `dev-cycle.md` rule — for an umbrella slice, de-claim `status:in-progress` + write the remaining-rows comment at the moment you enable auto-merge (the umbrella stays open regardless of the slice's async merge outcome), not after the merge. | — (flag only; remediated by Orc label cleanup this cycle) | — |
| 2026-06-20 (15:26Z) | — | no change — no new evidence. The only **completed** operating signal since the 14:54Z cycle is an **exemplary** QA cycle (15:12Z): a clean read-only L1 envelope sweep that found **NO NEW BUG** and correctly **ruled out 3 harness lies** (cp936 mojibake / phantom missing-hwnd / `\| head` exit-code artifact) by tracing to OS ground truth — directly applying the 20:22Z QA-harness rule, filing nothing false. The other signal, Dev's **#1059** cycle, is **still in flight** (dev.lock from 15:07Z; `naturo-dev` on `feat/issue-1059-click-image` with uncommitted work) → outcome not yet assessable. Investigated whether #1059's 15:14Z re-claim was a **"merged-but-left-without-status:done → re-pickable" weakness** (PR #1066 merged `find --image` at 07:34Z; `status:in-progress` was removed 07:33Z and `status:done` never set; the issue then sat OPEN+unlabeled until re-claimed 15:14Z) — **ruled OUT as a weakness:** #1059's body explicitly lists the **`naturo click --image` shortcut** in its acceptance and PR #1066 landed only the `find --image` half, so the issue **correctly stayed OPEN** with unfinished scope and is being legitimately finished now (normal multi-PR progress on one issue, cf. the #766 umbrella slices) — not a limbo/abandonment bug. Recorded the reasoning here so a future cycle doesn't mis-flag the same pattern. Today's substantive self-review rules (error-attribution / option-coverage / platform-order / hermeticity / error-code-registration) plus the 20:22Z QA-harness rule were all added within the last day and have not yet had a distinct new failing test — attaching another change on top of an exemplary QA cycle would over-fit / churn the prompts, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22 flag-to-watch (a 2nd team PR auto-merging with its own modified test module red) and the 14:54 flag-to-watch (umbrella de-claim missed due to async auto-merge) have **not** recurred. | — | — |
| 2026-06-20 (15:52Z) | — | no rule change — **flag from 14:54Z RESOLVED (second instance, confirmed benign).** Team-Dev landed PR **#1093** (`naturo click --image` shortcut, *fixes #1059*, `f04b0d8`, all-green) merged 15:33:59Z — ~2 min **after** the Dev cycle's boundary (cycle END 15:32Z), so the in-cycle post-merge label transition (`status:in-progress` → `status:done`) was skipped, exactly the structural gap flagged at 14:54Z for the #1092 umbrella de-claim. Orc completed the handoff this cycle (flipped #1059 → `status:done`, left open for QA, Rule 1). **Why no Dev rule (the 14:54Z flag proposed "de-claim/flip at auto-merge-enable time"):** that would directly **conflict** with the deliberate design — `orch-review.md` Step 1 keeps Dev at `status:in-progress` until merge *precisely to prevent a premature `status:done` on a PR whose CI then fails*; the **orch flip-on-merge IS the intended absorber**, and across both instances (#1092 @14:54Z, #1059 @15:52Z) it caught the gap within the same cycle with zero rot. The pattern is real but benign-by-design → resolving the flag (so a future cycle does not implement the conflicting rule), not escalating it. | Stops the loop from over-fitting a rule that would re-introduce false-`status:done`-on-red; the auto-merge-async handoff is a known, orch-absorbed boundary, not a Dev weakness |
| 2026-06-20 (16:26Z) | — | no change — no new evidence. The two completed operating signals since the 15:52Z cycle were both **exemplary, not weaknesses**: **QA (00:15Z)** verified+closed **#1088** (doc-vs-code consistency — non-existent `browser listen`/`page.listen` surface confirmed gone, documented CLI + `NetworkMonitor` Python surface all resolve) and **#1059** (`find --image` runtime-verified live+offline incl. threshold boundary + no-false-positive; `click --image` code-verified, physical SendInput **correctly deferred** per input-safety because the live desktop had admin terminals), and **applied the 20:22Z QA-harness rule** — traced a cp936 CJK-title mojibake to a console-decode artifact (UTF-16 compare matched correctly) and filed **no** false bug. **Dev (00:18Z)** shipped **#1082** (never-lie doc fix: iframe guide promised a stateful `browser frame`/`page.in_frame()`/`find(all_frames=True)` surface that does not exist → rewrote to the shipped `BrowserPage.frame`/`BrowserFrame` API), ran **HARDEST-FIRST with env blocks PROVEN this cycle** (cited the live `~/.naturo/safe-input.lock` 06-21 00:07 + open #863/#975/#1077 — directly honoring the 06-20 "prove the env block this cycle, never inherit" rule), wrote a **code-grounded hermetic** test (derives real subcommands from the Click tree at runtime) RED→GREEN, all-green auto-merge #1095, and set `status:done` **in-cycle**. The freshest self-review rules (error-attribution / option-coverage / platform-order / hermeticity / error-code-registration + the 20:22Z QA-harness rule) are all <1 day old and were just exercised cleanly here — attaching another change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row. The **19:22Z** flag (2nd team PR auto-merging with its own modified test module red) and the **14:54Z** flag (umbrella de-claim missed on async auto-merge — resolved benign 15:52Z) have **not** recurred: #1095 landed all-green and Dev set `status:done` in-cycle (no async-boundary handoff owed). | — | — |
| 2026-06-20 (16:52Z) | — | no rule change — **exemplary Dev cycle + first-instance infra/process blocker (flag to watch).** The 00:37Z Dev cycle attacked the v0.3.2 recognition moat (Standing #1) HARDEST-FIRST on the provisioned OpenJDK 21 + JAB desktop and filed **P0 #1096**: naturo's `core/src/jab.cpp::jab_ensure_init()` fires `Windows_run()` **once**, pumps a fixed `JAB_INIT_SETTLE_MS=1000` ms, then caches `initialized=true` permanently — so the **async** JAB↔JVM discovery handshake never completes on a loaded desktop (`isJavaWindow`→false forever, `naturo_jab_get_element_tree` rc=-6, cascade gets **no** `jab` provider). Proven by a decisive **same-process A/B**: a 2nd direct `Windows_run()` on the identical loaded module attaches in ~0.4 s; bare-name vs full-path DLL load ruled out. Consequence: the published `docs/RECOGNITION.md` JAB "+40" headline + matrix ✅ + the cited `test_jab_recognition_932.py` **do not reproduce** — a never-lie/moat-credibility defect. Dev **directly applied the 06-20 proven-env-block rule** (probed the runtime JAB env live AND proved the build toolchain absent live — `cl`/`cmake`/`msbuild`/`vswhere` not found) and **correctly filed-not-shipped** (shipping an unbuilt/unverified native moat change would violate verify-end-to-end / never-lie) — the opposite of 避实就虚. QA (00:45Z) verified+closed #1082 cleanly (doc-vs-code). The genuinely new thing this cycle is a **blocker, not an agent weakness**: native-core moat fixes have **no local build/verify path** — build is CI-only, JAB-verify is local-desktop-only, neither environment can do both. That is **infra/process = human-only → queued as needs:ace #1097** (recommended resolution: CI-build-artifact → local-desktop-verify, no spend). Also filed **#1098** (interim never-lie doc caveat on the unreproducible RECOGNITION.md JAB row, Dev-actionable headless). Per the over-fit ban + flag-on-first-instance discipline, recorded as a flag, **not** a dev-cycle rule. **If the build/verify gap recurs or stalls #1096:** escalate #1097 and consider a standing dev-cycle "native change ⇒ CI-build-artifact + local-desktop-verify" workflow rule. | — (flag + needs:ace #1097; no prompt change) | — |
| 2026-06-20 (17:31Z) | — | no change — **reinforcing evidence for the fresh platform-order rule, not a new weakness.** The two completed signals since 16:52Z were both sound. **Dev (01:25Z)** landed team PR **#1099** (`fix #1086` — eN stale-ref error envelopes now use the registered `STALE_SNAPSHOT_CACHE` code with `context={"ref":...}` instead of bare `REF_NOT_FOUND`/category:unknown across click/type/press/move/drag/scroll + `capture --element`; TDD RED 7/7→GREEN; all-green SQUASH auto-merge `3d30438`, `status:done` in-cycle, branch auto-deleted Rule 14) **and self-caught a CROSS-PLATFORM break** — its first CI run was RED on Linux/macOS only (`capture --element` returned `PLATFORM_ERROR` because capture crops *after* the GUI gate), fixed on the 2nd run by giving capture its own GUI-mocked test so the 6 interaction commands stay platform-invariant. This is the **exact #1070/#1072 platform-order class already covered by `dev-cycle.md:121-127`** (validation/error-code before the platform gate; pin with a non-mocking test — #1070 cited as evidence), a rule **<1 day old**; #1086's first-red is its first real post-adoption exercise and Dev self-corrected to green within the cycle → logged as **reinforcing evidence**, not a new gap. Attaching a second platform rule one day after the first would over-fit a single nuanced incident (capture legitimately needs the GUI, unlike the pure interaction commands), which Step 3.6 explicitly forbids. **QA (01:20Z)** ran an exemplary read-only envelope sweep — NO false bug, ruled out a CJK-encoding harness artifact per the 20:22Z QA-harness rule, and filed **#1101** (9 `ErrorCode` members missing from `_ERROR_CATEGORIES`→category:"unknown") with a root cause for why `test_error_envelope_884` misses it. **If first-CI-red on the platform-order class recurs on a *3rd* post-rule PR,** sharpen `dev-cycle.md:121-127` to require an explicit pre-push mental run of the Linux/macOS lane for any command touching a GUI/capability gate. | — | — |
| 2026-06-20 (18:24Z) | — | no change — no new **agent operating** weakness. The completed Dev signal since 17:52Z was **exemplary**: **Dev (18:18Z)** shipped **#1101** (PR #1103, `a52c953`, all-green SQUASH auto-merge, `status:done` in-cycle, branch auto-deleted) — ran **HARDEST-FIRST** and **re-proved the C++ toolchain absent THIS cycle** (`cl`/`cmake`/`msbuild`/`vswhere`/`ninja` all not found → native moat #1096/#932 unbuildable/unverifiable locally, correctly **filed-not-shipped** per never-lie, tracked needs:ace #1097 — honoring the 06-20 proven-env-block rule, not inheriting a stale note), then fell to the **next-hardest actionable** task (#1101: 9/34 `ErrorCode` members missing from `_ERROR_CATEGORIES` → category:"unknown") and pinned it with a **hermetic drift-guard** test (RED 28-fail → GREEN 53-pass) asserting *every* code maps non-unknown — directly applying the 13:22Z Error-code-registration + Test-hermeticity rules. The only non-exemplary signal is **QA's 02:07Z(+0800) cycle ERROR** (`naturo-loop-state.log` "in use by another process" → QA lost the shared-append-log race vs. a concurrent runner and **skipped its slot**, no work lost). That is the **#935-family uncoordinated-runners harness/infra issue** — the **same root** as the 19:52Z Dev log-lock instance recorded then as #935-family — **not** an agent operating weakness → stays queued (human-only/harness), no Dev/QA rule. The freshest self-review rules (error-attribution / option-coverage / platform-order / hermeticity / error-code-registration + the 20:22Z QA-harness rule) are all <1 day old and were just exercised cleanly by #1101 — attaching a change on top of an exemplary Dev cycle + a harness file-lock would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row. The 17:31Z platform-order escalation trigger did **not** fire (#1103 all-green, no first-red); the 19:22Z / 14:54Z flags did not recur. | — | — |
| 2026-06-21 (08:22Z) | — | **No change — no new evidence.** The two completed signals since the 07:52Z cycle were both **exemplary, not weaknesses**. **Dev (#1119, *fixes #1119*, PR #1122 merged `12d7d9c` all-green @00:18Z)** attacked the P1 never-lie `browser screenshot` 100%-crash HARDEST-FIRST — re-proved the native-moat block **this cycle** (`java`/`javac` present = JDK 21, but `where cmake`/`cl`/`msbuild` all NOT FOUND → #1096/#932 unbuildable locally, correctly stays needs:ace #1097; click/type-via-JAB frozen under #863/#975; #1060 OCR → #1077 — honoring the proven-env-block rule, not inheriting), then fell to the hardest *actionable* item; root-caused `_page.py:485` unpacking the CDP param dict (`send("Page.captureScreenshot", **params)`) into `CDPClient.send(method, params=None)` (positional) → `TypeError`, fixed it **1-line positionally with NO public-API/CLI change** (`--auto` correct under the public-API hold), ran a **lateral grep** (`\.send\([^)]*\*\*`) proving line 485 the sole victim, added **2 hermetic regressions driving a real `send(method, params=None)` stub** (the prior bare-MagicMock tests swallowed kwargs → why it slipped) PROVEN red-on-old/green-on-fix + an end-to-end PNG magic-byte check on real headless Chrome, and **filed the lateral option-coverage gap (#1123, `--selector` silently ignored) as a separate atomic issue rather than bundling** — directly applying the Option-coverage + Error-attribution + Test-hermeticity rules. **QA (#1115, 08:13Z)** verified+closed the migration-guide ms→s doc fix with the correct docs-only method (merged-commit ancestry per Rule 1 + content grep [0 stray ≥1000 literals; Before-snippets untouched] + seconds ground-truth in `waits.py`/`_page.py` + non-vacuous runtime-derived guard), and — applying the **20:22Z QA-harness rule** — **declined to fuzz the browser/runtime surface while a concurrent Dev cycle ran** (08:07Z, contamination risk) rather than filing false positives, confirmed `import naturo` resolves under its own worktree (no #969 stale-egg), left #972 human-only-queued, used zero intrusive input. The freshest operating rules (#969 stale-egg guard <1d; public-API hold / QA-harness / error-code-registration / option-coverage all <2d) were just exercised cleanly here → attaching a change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. The 19:22Z / 14:54Z watch-flags did **not** recur (#1122 landed all-green; #1119 is a `fixes` to non-default `develop` so Orc owed + did the flip-on-merge handoff, no async de-claim gap). | — | — |
| 2026-06-20 (17:52Z) | — | no change — no new evidence. The two completed signals since 17:31Z were both **exemplary, not weaknesses**: **QA (01:45Z)** verified+closed **#1086** (eN stale-ref envelope) — live-ran all 8 eN ref-not-found paths on the real CLI with an empty snapshot cache (refs never resolve → **zero input dispatched**, honoring the input-safety freeze), confirmed byte-for-byte `STALE_SNAPSHOT_CACHE`/category:session/recoverable:true parity with get/set, **ruled out two harness artifacts** (a `set --value` arg-syntax `UNKNOWN_OPTION`; a `type/press --app NoSuchApp` ordering short-circuit) per the 20:22Z QA-harness rule, filed no false bug, closed with a merged commit (Rule 1). **Dev (01:46Z)** shipped **#1098** (RECOGNITION.md JAB never-lie caveat) HARDEST-FIRST — re-proved the C++ toolchain block **this cycle** (`cl`/`cmake`/`msbuild`/`vswhere`/`ninja` all absent — never inherited), correctly filed-not-shipped the native #1096 fix, **swept ALL JAB claim sites** (matrix cell, legend, footnote, withdrawn benchmark numbers, narrative, per-framework how-to — not just the one row), pinned it with a hermetic non-desktop test referencing #1096 + an "under repair" marker so the withdrawn number can't silently return, all-green auto-merge #1102, `status:done` in-cycle. The freshest self-review rules (error-attribution / option-coverage / platform-order / hermeticity / error-code-registration + the 20:22Z QA-harness rule) are all <1 day old and were just exercised cleanly here — adding a change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Flags: the 17:31Z platform-order escalation trigger (3rd post-rule first-CI-red) did **not** fire (#1102 all-green, no first-red); the 19:22Z and 14:54Z flags also did not recur. Honest "no change" row to avoid churn (Step 3.6's explicit allowance). | — | — |
| 2026-06-20 (18:52Z) | Dev | **Public-API change auto-merged unattended.** Team-Dev (18:50Z) shipped **#1104** (part of #766, merged `41b81ad`, all-green SQUASH `--auto`) which **added new public surface** — `BrowserPage.set_download_dir()`/`wait_for_download()` + a `DownloadResult` dataclass **exported from `naturo.browser`**. Dev *correctly identified* it as public-API and *flagged it for sign-off in the PR body* — yet still enabled `--auto`, so the surface landed on `develop` with no human gate. The human-only public-API guardrail lives in `RULES.md` + `orch-review.md` but **`dev-cycle.md` had no corresponding instruction**, so Dev's only lever was a flag-in-the-body, not a hold. This is a structural guardrail hole (Dev-side), not a stylistic nit: it landed an irreversible-on-`develop` public contract that is explicitly Ace's call (the "guide already promised it" defense is itself a judgment that should be gated — shrinking the doc is a valid alternative). First instance, but a guardrail-class gap → surgical fix, not over-fit. | **`dev-cycle.md` Step 4:** added a **Public-API guardrail** — if a PR adds/alters public API (new exported symbol / public method / CLI flag-or-contract), open it **without `--auto`**, comment "auto-merge OFF — needs Ace sign-off", leave the issue `status:in-progress`, and let orch queue it `needs:ace`; this holds **even when the symbol is already in a committed doc**. Filed `needs:ace` **#1105** to sign off (or revert) the surface that already landed. | Dev stops auto-landing public API unattended; public-surface changes are held for Ace sign-off (the human-only guardrail now has a Dev-side lever, not just an orch/RULES statement). |
| 2026-06-20 (19:22Z) | — | **No change — no new evidence.** Dev (#1107, JS-eval equivalence row, *part of #766*) was exemplary and gave the **public-API hold rule its first post-adoption exercise**: it correctly determined the PR adds *no* public surface (pure test over existing `page.evaluate`) so `--auto` was permitted per `dev-cycle.md` Step 4 — i.e. the <1-day-old rule fired correctly and did not over-trigger. It also ran the HARDEST-FIRST native-toolchain probe LIVE (`where cl cmake ninja msbuild` → all absent → #1096 stays #1097-gated, not silently skipped) and **filed never-lie doc gap #1106** instead of silently implementing undocumented public API. QA (03:12+0800) filed no false bug, ruled out gbk/`/tmp` harness artifacts, re-verified the #1050 fix live. Recurring migration-guide doc-drift (#1104→#1107→#1106 all found documented-but-unimplemented surfaces) is a **product** issue Dev is correctly catching+filing, not an agent operating weakness. A second guardrail tweak one cycle after the public-API hold would over-fit (Step 3.6 forbids). | none (ledger line only) | Avoids churn; records that the public-API hold's first exercise was clean (reinforcing, not a new gap) so a future cycle doesn't re-litigate it |
| 2026-06-21 (06:22Z) | — | no change — no new evidence. The two signals since the 05:52Z cycle were both **exemplary**: **Dev #1117 (06:19Z)** shipped the #766 Anti-Detection Before/After equivalence row HARDEST-FIRST — moat blocks **re-proven this cycle** (Java 21 present but #1096 needs an absent MSVC toolchain → build-blocked #1097; #932 action slices frozen under input-safety #863/#975; #1060 OCR → #1077), then ran a genuine **never-lie** empirical stealth probe and, finding naturo's default CDP launch already passes all 4 stealth vectors with zero config, **filed no fabricated doc-gap**; the test reads the raw `navigator` fingerprint (not the fixture's self-report) and requires a measurable stealth-patch delta so a no-op fails (hermetic); test-only → `--auto` correct; kept #766 open. It also **caught the #969 stale-egg trap** mid-cycle (a probe imported sibling `naturo-qa-mariana` pre-#1075 code) and forced `PYTHONPATH` to its own worktree — directly applying the 20:22Z QA-harness "rule out your tooling first" discipline, the Dev side of it. **QA** verified+closed **#1112** with the merged commit (Rule 1), left **#972** as needs:ace (human-only security), simulated no intrusive input. The freshest self-review rules (HARDEST-FIRST proven-env-block, public-API hold, error-code-registration, hermeticity, QA-harness) are all <2d old and were just exercised cleanly here — attaching a change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22 / 14:54 flags-to-watch have not recurred (#1117 landed clean, a #766 "part of" with no async de-claim owed). | — | — |
| 2026-06-20 (19:55Z) | — | **No change — no new evidence.** The two completed signals since 19:22Z were both **exemplary**: **Dev (#1108, *fixes #1063*)** attacked a never-lie correctness bug HARDEST-FIRST — element-level `find`/`find_all` for XPath/text resolved against the whole document, returning the first card's title for every card; it scoped the search to the parent node via CDP `Runtime.callFunctionOn`, **proved the guard both ways** (restoring the document-scoped behaviour fails the new desktop equivalence test) + hermetic unit tests, and kept the fix off the public surface so SQUASH `--auto` was correct under the public-API hold. **QA (19:40Z)** ran a clean read-only exploratory cycle — no false bug, deferred the human-only #972 security close to the queue, ruled out gbk-console/harness artifacts rather than filing noise. The freshest rules (public-API hold, platform-order, hermeticity) are all <2 days old and exercised correctly; a tweak now would over-fit a single cycle (Step 3.6 forbids). | — | — |
| 2026-06-20 (20:24Z) | — | **No change — no new evidence.** Both signals since 19:55Z were **exemplary**. **Dev (#1109, *fixes #1106*)**: HARDEST-FIRST — re-proved the C++ toolchain absent (`where cmake/cl/msbuild` all missing → P0 JAB moat #1096 genuinely blocked, tracked needs:ace #1097), fell to the hardest *actionable* never-lie item; the migration guide advertised `browser cookies`/`BrowserPage.cookies` + JS-click (`browser click --js`/`click(js=True)`) that don't exist; **derived the real surface at runtime**, added "🚧 Not yet implemented" + #1106 caveats to all 6 guide regions (caveat-not-prune per orch triage), offered the working `eval`/`page.evaluate` alternative, and shipped a **bidirectional hermetic guard** (asserts the 4 APIs still absent + every fictional region carries #1106); docs+test off the public surface → `--auto` correct. **QA (04:10Z)**: verified+closed #1063 with genuine headless-Chrome evidence (offline xhs fixture, 4 distinct per-card titles), code-level never-lie confirm; 03:40Z clean read-only exploratory, 0 false bugs. The migration-guide doc-drift recurrence (#1098→#1104→#1106) was **re-evaluated** and remains a *product* class Dev catches correctly via never-lie — it is exactly what umbrella #766 sweeps row-by-row — **not an operating weakness**; a guardrail tweak ~1.5 days after the public-API hold would over-fit (Step 3.6 forbids). | — | — |
| 2026-06-20 (20:52Z) | — | **No change — no new evidence.** Both signals since 20:24Z were **exemplary, not weaknesses**. **Dev (#1110, 04:48Z)** added a #766 Scroll equivalence row (`scroll_by`/`scroll_to_element`) HARDEST-FIRST: re-proved the native-moat block *this cycle* (toolchain `where cmake/cl/...` absent), chose the hardest *actionable* row, wrote a hermetic offline fixture (`scroll.html`) with a never-lie both-ways assertion against live `scrollY`/`getBoundingClientRect` (no-op stays at 0; `block:center` not top-aligned), touched zero `naturo/` code so `--auto` was correct off the public surface, and did **not** hijack the in-progress umbrella (used "part of #766", #1107 precedent). **QA (04:10Z)** verified+closed #1063 with genuine headless-Chrome evidence and ruled out the #969 harness-artifact class without filing noise. Freshest operating rules (<2d) were exercised cleanly; a tweak on exemplary cycles would over-fit (Step 3.6 forbids). Honest no-change row to avoid churn. | — | — |
| 2026-06-21 (05:22Z) | — | **No change — no new evidence.** Both signals since 20:52Z were **exemplary, not weaknesses**. **Dev (#1113, 05:07Z, *part of #766*)** proved the migration guide's **Waiting** equivalence row HARDEST-FIRST and, doing so, exposed + fixed a real never-lie bug: `wait_for(state=visible/hidden)` decided visibility via `_get_click_point()`, whose `getBoundingClientRect` fallback returns a valid all-zeros point for an unrendered node, so `--state hidden` never fired for a `display:none`/zero-area element (the Dianping spinner-gone pattern) and `--state visible` wrongly passed. Fix added private `_is_displayed()` (connected ∧ not `display:none`/`visibility:hidden|collapse` ∧ non-zero box); **click path untouched (does not preempt #1083)**; **no new public symbol/flag** → `--auto` correct under the public-API hold; never-lie precondition + blocked-duration + post-state assertions on real headless Chrome; updated the affected units; and it **filed the adjacent never-lie doc gap #1112 rather than papering over it** (`wait_for_eval`/`wait_for_navigation(url_contains)`/`wait_for_network_idle(time=)` + `wait --load/--eval/--navigate` don't match the shipped surface). **QA (#1111, 05:13Z)** found a real P1 never-lie bug — `naturo type` is silently dropped from `record` though replay supports it — with **3 independent evidence lines** (code: `_type.py` never calls `_record_action`; control: `move` records step_count 1; runtime: foreground-confirmed benign `QA_PROBE` into own Notepad → step_count 0), cleaned up its 4 probe recordings, and routed input safely (foreground pid confirmed, guard armed). The 19:22Z **watch-flag** (a *second* auto-merge team PR red in its own modified test module) did **not** trigger: #1113's modified units passed locally (91) and its Lint&Type is green. Freshest operating rules <2d old, exercised cleanly → a tweak would over-fit (Step 3.6 forbids churn). | — | — |
| 2026-06-21 (05:52Z) | — | **No change — no new evidence.** Both signals since 05:22Z were **exemplary, not weaknesses**. **Dev (#1116, *fixes #1112*, merged `55fa4bc` 21:52Z)** closed the never-lie doc gap the prior cycle triaged: the migration guide's **Waiting** section advertised `wait_for_eval`/`wait_for_navigation(url_contains)`/`wait_for_network_idle(time=)` + `wait --load/--eval/--navigate` flags that don't match the shipped surface — Dev **corrected the doc to the real API** (docs + a `test_migration_guide_wait_surface_doc_1112.py` guard, +195/−19), **no public surface** → SQUASH `--auto` correct under the public-API hold; landed all-green, branch auto-deleted (Rule 14). **QA (#1114, 05:41Z)** filed a genuine P2 never-lie bug — a failed `capture --region`/`--element` (zero/neg or off-screen crop) **still writes the full uncropped screenshot to the user's `-o` path** while reporting `success:false`/exit 1 — with a **pinpointed root cause** (`_capture.py` L130 writes the full image up front; crop validated only after at L285 which raises, leaving the file; L287 overwrites only on success), a clean rm-before/file-after repro on both `-j` and human paths, suggested unlink-on-failure fix + regression-test note, did a **lateral record-coverage audit** that correctly found only `type` missing (= already-filed #1111, no duplicate), ruled out its own tester-typo (`capture --out` vs `-o`) as not-a-bug, deleted all scratch artifacts, and simulated no input. Both cycles directly exercised the freshest rules cleanly (Dev: public-API hold + never-lie doc-fix-not-paper-over; QA: harness-rule-out + no-duplicate-filing). The 19:22Z watch-flag did **not** recur (#1116 all-green). Freshest operating rules <2d old, exercised cleanly → a tweak would over-fit (Step 3.6 forbids churn). | — | — |
| 2026-06-21 (06:52Z) | Dev | **#969 stale-egg trap recurred — Dev had to hand-improvise the PYTHONPATH workaround a SECOND consecutive cycle, and `dev-cycle.md` carried no guard for it.** The editable install can resolve `import naturo` to a *sibling* worktree's stale code (#969, needs:ace/open). **Dev #1117 (06:19Z)** hit it (a probe imported `naturo-qa-mariana` pre-#1075 code → 403 CDP handshake) and forced `PYTHONPATH=naturo-dev`; **Dev #1118 (06:50Z)** hit it **again** and again forced `PYTHONPATH=naturo-dev` for all probes + pytest. Two cycles in a row improvising the same workaround = a **recurring operating friction**, not a one-off: a Dev who *forgets* it would green-light a fix while exercising stale sibling code (a silent false-confidence gate — the same class the 20:22Z **QA-harness "rule out your tooling first"** rule already guards, but **only `qa-cycle.md:62` had it; `dev-cycle.md` had none**, even though Dev runs probes/pytest against runtime code every cycle). Crosses the evidence bar (≥2 cited cycles); surgical; mirrors an existing QA guard → not over-fit. | **`dev-cycle.md` Step 0:** added a #969 stale-egg guard to the setup block — print `naturo.__file__`, and if it resolves outside this worktree, force `PYTHONPATH=$(pwd)` on **every** probe + pytest run until #969 lands; states the risk (green-lighting a fix on stale sibling code). | Dev no longer relies on remembering an ad-hoc workaround; the #969 false-confidence path now has a Dev-side guard symmetric to QA's, so a fix is always exercised against this worktree's code until #969 is fixed for real. |
| 2026-06-21 (07:25Z) | — | **No change — no new evidence.** The only completed signal since the 06:52Z cycle was **exemplary, not a weakness**: **QA (07:07–07:14Z)** filed a genuine **P1 #1119** (`naturo browser screenshot` crashes 100% — `_page.py:485` does `send("Page.captureScreenshot", **params)` but `cdp.py:232 send(method, params=None)` takes the dict positionally → `TypeError: unexpected kwarg 'format'`; both default + `--full-page`, no file written, leaks a raw traceback even with `-j`) with root cause + 1-line fix + regression-test + lateral-grep note, and — directly exercising the **20:22Z QA-harness "rule out your tooling first" rule** — **caught and discarded two harness artifacts** (the `\| head` exit-0 trap; an intermittent CDP handshake-500 / `No such target` traced to a *concurrent Dev browser-pytest* Chrome load → did **NOT** file the contaminated-env noise), physically verified DOM state (not stdout), left #972 queued (human-only security), used no intrusive input (CDP-dispatch only). It also confirmed the **#969 stale-egg guard added to `dev-cycle.md` last cycle is being honored** ("import naturo resolves under naturo-qa, no #969 stale-egg"). The **Dev (07:07Z) cycle is still running** (#1115 timeout ms-vs-seconds doc fix) → not a completed signal this cycle. The freshest operating rules (#969 stale-egg guard <1d, public-API hold, QA-harness, error-code-registration all <2d) were just exercised cleanly → attaching a change on top of an exemplary QA cycle would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (08:52Z) | — | **No change — no new evidence.** The two completed signals since the 08:22Z cycle were both **exemplary, not weaknesses**. **Dev (#1125, *part of #766*, merged `2069b49` all-green @00:48Z)** added the migration-guide **Screenshots** Before/After equivalence row HARDEST-FIRST — re-proved the native-moat block **this cycle** (`java`/`javac`=JDK 21 present but `where cmake`/`cl`/`msbuild` all NOT FOUND → #1096/#932 unbuildable locally, stays needs:ace #1097; input-via-JAB frozen under #863/#975; #1060 OCR → #1077 — honoring the proven-env-block rule, not inheriting), fell to the hardest *actionable* slice (completing the section whose 100%-crash #1119 was just fixed in #1122), wrote a **dependency-free hermetic** test (`_png_dimensions` parses PNG sig+IHDR, no Pillow → CI-collectable) with **non-vacuous never-lie** assertions (full_page PNG strictly taller than viewport ≈ content height; a silent viewport re-capture fails) on real headless Chrome, test-only → `--auto` correct, kept umbrella #766 OPEN ("part of #766", no auto-close/relabel) — directly applying the Test-hermeticity + Option-coverage rules. **QA (08:42Z)** verified+closed **#1119** with the real-desktop superpower (live headless Chrome: TypeError gone, default + `--full-page` write valid `\x89PNG` PNGs, params positional `_page.py:485`; cited merged `12d7d9c` per Rule 1) and, thinking laterally, filed a **genuine NEW P1 #1124** (first `browser screenshot` after `launch` reliably times out ~30s/no-file across 5 fresh launches, then OK — headless first-frame-readiness gap, distinct from the #1119 param fix) after **ruling out the harness** (clean redirected exit codes, separate process/conn per launch, import under its own worktree, no #969 stale-egg) per the 20:22Z QA-harness rule, did not block closing #1119 on it, used no intrusive input, left #972 human-only-queued. The freshest operating rules (#969 stale-egg guard <1d; public-API hold / QA-harness / error-code-registration / option-coverage / hermeticity all <2d) were just exercised cleanly → attaching a change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. The 19:22Z / 14:54Z watch-flags did **not** recur (#1125 all-green; a "part of #766" with no async de-claim owed). | — | — |
| 2026-06-21 (07:52Z) | — | **No change — no new evidence.** The two completed signals since the 07:25Z cycle were both **exemplary, not weaknesses**. **Dev (#1120, *fixes #1115*, merged `6297fac` all-green)** corrected the migration guide's browser timeouts **ms→s** (the guide passed `--timeout 10000`/`timeout=10000` but the shipped CLI/SDK surface is **seconds** → a copied example waits ~2.8 h) and ran it cleanly: **independently re-verified in code** that every browser `--timeout` is `type=float default=30.0 help="…seconds"` and the SDK `wait_*` use `deadline = monotonic()+timeout` (never-lie verify, not inherited), swept the guide (10000→10/5000→5/30000→30/60000→60), left the Selenium/DrissionPage *Before* snippets untouched, added a **hermetic** regression test that **derives the real unit at runtime** from the `--timeout` help + `BrowserPage` docstrings and scans for stray ≥1000 literals so it can't recur, docs+test only (no public surface) → `--auto` correct, kept #1115 `status:in-progress` for the orch flip-on-merge, and **correctly resumed the prior 07:07Z cycle's half-done uncommitted #1115 work** rather than discarding it (the "never leave half-done / resume" net working as designed). **QA (#1121, 07:42Z)** filed a genuine **P2** never-lie bug — `naturo diff --snapshot A --snapshot B` is the *first* documented diff example yet `diff_cmd.py:113-128` is a hard-coded "(not yet implemented)" placeholder — with a feasibility proof (`Snapshot.ui_map` does store the tree: 56 elements on a Calculator snap → rebuild → existing `diff_trees()`) + a lateral `--app`-alias note, **applied the 20:22Z QA-harness rule** (re-decoded a gbk `open()` mojibake as strict UTF-8 before trusting it), **avoided the contaminated browser surface** (Dev's concurrent 07:37Z cycle) per the same rule, simulated no intrusive input, left #972 queued (human-only). The freshest operating rules (#969 stale-egg guard <1d; public-API hold / QA-harness / error-code-registration all <2d) were just exercised cleanly → attaching a change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. **First-instance neutral note (no rule):** the 07:07Z Dev cycle ended with #1115 uncommitted (no commit/PR) — cause unattributable (likely a budget/time cutoff at the ~30 min runner cadence) and **recovered cleanly** by #1120, so the safety net worked; recorded only so a future cycle can detect recurrence. The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (09:22Z) | — | **No change — no new evidence.** No work cycle has *completed* since the 08:52Z review (≈30 min): the 09:07Z Dev and QA cycles are still in-flight (loop-state.log shows their `cycle START` with no END; no new team-Dev PR and no new issue above #1124 has appeared). With no completed Dev/QA signal to assess, there is no new operating evidence this cycle, and the freshest self-review rules (#969 stale-egg guard <1d; public-API hold / QA-harness / error-code-registration / option-coverage / hermeticity all <2d) were already exercised cleanly by the 08:22Z/08:52Z exemplary cycles. Fabricating a rule on zero new evidence is exactly the over-fit Step 3.6 forbids → honest "no change" row to avoid churn. The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (09:52Z) | — | **No change — no new evidence.** No work cycle has *completed* since the 08:52Z review: the 09:07Z and 09:37Z Dev/QA cycles are both still in-flight (loop-state.log shows their `cycle START` with no `cycle END`); no new team-Dev PR (the only open PR is the long-standing community #1055) and no new issue above #1124 has appeared. With zero completed Dev/QA signal to assess, there is no new operating evidence, and the freshest self-review rules (#969 stale-egg guard <2d; public-API hold / QA-harness / error-code-registration / option-coverage / hermeticity all <3d) were already exercised cleanly by the 08:22Z/08:52Z exemplary cycles. Inventing a rule on zero evidence is the over-fit Step 3.6 explicitly forbids → honest "no change" row to avoid churn. The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (10:22Z) | — | **No change — no new evidence.** The two signals that *completed* since the 09:52Z review were both **exemplary, not weaknesses**: **Dev (10:20Z)** landed team-Dev **#1126** (`test: prove Page Navigation Before/After equivalence row`, **part of #766**, merged `953b0c8` all-green @02:18Z) — ran **HARDEST-FIRST with the native-moat block re-PROVEN this cycle** (`javac` 21 present but `cl`/`cmake`/`cargo` ABSENT → #1096/#932 unbuildable locally, correctly stays needs:ace #1097; #863 SendInput input-frozen #975; #1060 OCR → #1077 — honoring the proven-env-block rule, never inheriting), fell to the hardest *fully-shipped headless-safe no-new-public-API* slice, wrote a **non-vacuous hermetic** CDP test (`navigate()` auto-wait `readyState complete`; `reload()` never-lie via a live DOM-mutation-discarded proof that a no-op reload would fail; `back()/forward()` history walk), test-only → SQUASH `--auto` correct under the public-API hold, branch auto-deleted, and correctly kept umbrella **#766 OPEN** ("part of", not a `fixes`) → no orch handoff owed. **QA (10:11Z)** ran a clean exploratory cycle (queue exhausted: only `status:done` = #972 human-only, untouched) — L0/L1/L3 envelope + real-desktop capture sweep found **NO new bug**, mapped every observed gap to an already-OPEN issue (#1089/#865/#1054) filing zero duplicates (Rule 9), and **ruled out 2 of its own harness errors before filing** (phantom `see --screenshot` flag; known #865 bare-tree) per the 20:22Z QA-harness rule, zero intrusive input. The freshest self-review rules were just exercised cleanly by two exemplary cycles in a row → attaching a rule now would over-fit, which Step 3.6 explicitly forbids → honest "no change" row to avoid churn. The 19:22Z / 14:54Z watch-flags did not recur (#1126 all-green; "part of #766" → no async de-claim/flip handoff owed). | — | — |
| 2026-06-21 (10:52Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 10:22Z review was **QA-Mariana (10:37→10:41Z)**, and it was **exemplary, not a weakness**: exploratory PASS with 0 new bugs, every observed gap mapped to an already-OPEN issue (#1089 wait-timeout error block, #865 `see -j` envelope, #1054 `get/set -j` envelope, #1084 `process_name` round-trip — **no duplicates filed**), and — exactly the discipline the 06-20 20:22Z rule asks for — it **ruled out 4 of its own false alarms before filing** (a 300-char output-truncation artifact misread as "record list missing count"; `list apps`→windows is intentional #98 design; `category` is populated only on UNKNOWN_OPTION not "always unknown"; CJK "mojibake" was its own cp936 console, clean under strict UTF-8). Zero intrusive input (read-only probes only, focus safety respected with live concurrent agents present). The 10:37Z Dev cycle is still **in-flight** (loop-state.log shows `cycle START` with no `cycle END`) → no completed Dev signal to assess. The freshest dev/qa-cycle rules (06-20/06-21) are being exercised cleanly; manufacturing a new rule on a single exemplary signal is precisely the over-fit Step 3.6 forbids. Recorded as no-change, consistent with the 09:22→10:22Z rows (not oscillating). |
| 2026-06-21 (11:22Z) | — | **No change — no new evidence.** No Dev/QA *work* cycle has produced a signal since the 10:52Z review: the **11:07Z Dev and QA cycles both exited 1 having done zero work**, killed by a **transient API rate-limit** (`qa-last.out`/`dev-last.out` both end on *"API Error: Server is temporarily limiting requests (not your usage limit) · Rate limited"*). This is **infra noise, not a Dev/QA operating weakness** — and distinct from the #915 `403 Request not allowed` auth-down class (that was a credential failure; this is a brief server-side throttle that self-clears next round). There is no agent *behavior* to correct: the cycles never reached Step 0. Manufacturing a process rule from a one-round throttle would be the over-fit Step 3.6 forbids; the right channel for repeated infra flakiness is the #917 watchdog, not a SOUL/cycle edit. **Watch-flag:** if rate-limited empty cycles recur in a sustained burst (≠ one round), it becomes a runner-resilience item (retry/backoff in `runner.ps1`), surfaced to Ace — not a team-evolution row. Freshest dev/qa rules (06-20/06-21) were exercised cleanly by the 10:11Z/10:41Z exemplary QA cycles → no change. | — | — |
| 2026-06-21 (11:52Z) | — | **No change — no new evidence.** The two signals that *completed* since the 11:22Z review were both **exemplary, not weaknesses**. **Dev #1127 (11:42Z, *part of #766*, merged `5a0328a` all-green @03:45Z)** added the migration-guide Chrome **profile user-data-dir persistence** equivalence row HARDEST-FIRST — re-proved the native-moat block **this cycle** (`java`/`javac` 21 present but `where cmake`/`cl`/`ninja` ALL absent → #1096 JAB / #932 / #920 unbuildable locally, stays needs:ace #1097; #972/#975 input-frozen; #1077 OCR), fell to the hardest *actionable* row, and notably **adopted 131 lines of complete in-flight test work the rate-limited 11:07Z cycle abandoned** (preserved via stash, replayed onto a fresh branch off origin/develop — the 07:52Z "never leave half-done / resume" safety net working **again**, and the 11:22Z rate-limit watch-flag thereby *resolved*: that empty cycle's work was recovered, not lost). The test is non-vacuous never-lie (fresh-dir control sees no `localStorage` marker; graceful CDP `Browser.close`+process-wait to avoid Windows flush-flakiness) with an honest scope note (`ProfileManager` surface unimplemented — asserts only the shipped `user_data_dir` mechanism, not faked); test-only → SQUASH `--auto` correct under the public-API hold; #969 stale-egg ruled out (import under own worktree); kept umbrella #766 OPEN ("part of", no auto-close/relabel); branch auto-deleted (Rule 14). **QA (11:38Z)** ran a clean exploratory PASS — 0 new bugs, **cross-checked L3 DPI/capture dims with PIL (OS ground-truth, not naturo stdout)** per the 20:22Z QA-harness rule, mapped every gap to an already-OPEN issue (#1089/#865/#1054/#1084) filing zero duplicates (Rule 9), zero intrusive input, left #972 human-only-queued. The freshest self-review rules (#969 stale-egg guard / public-API hold / QA-harness / hermeticity / option-coverage, all <2–3d) were just exercised cleanly by two exemplary cycles in a row → attaching a rule now would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn (consistent with the 09:22→11:22Z rows, not oscillating). The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (12:22Z) | — | **No change — no new evidence.** The two signals that *completed* since the 11:52Z review were both **clean/exemplary, not weaknesses**. **Dev (04:20Z, #1128, *part of #766*, merged `a543925` all-green @04:18Z)** added the migration-guide **Multi-Account concurrent-profile isolation** equivalence row HARDEST-FIRST — re-probed the native moat first (`cl`/`cmake`/`msbuild`/`vswhere` ALL absent → #1096 JAB / #932 / #920 unbuildable locally, stays needs:ace #1097; #1060 OCR blocked on #1077; JDK21 present but #932 depends on #1096), fell to the hardest *actionable* row, kept umbrella **#766** OPEN ("part of", no auto-close/relabel), gate clean (ruff PASS / mypy unaffected tests-only / pytest x3 non-flaky / full collect PASS), test-only → SQUASH `--auto` correct under the public-API hold, branch auto-deleted (Rule 14). **QA (12:09Z)** ran a clean exploratory PASS — queue exhausted of QA-verifiable work (#972 human-only), so a **fresh-angle** `--json` envelope + exit-code sweep (surfaces prior cycles didn't re-probe); envelope CONSISTENT; investigated one candidate (`list apps` per-window rows incl. same-PID dups) and **ruled it out before filing** as intentional #98 design (STATE.md:72 / README:297 / CHANGELOG) → 0 dupes, 0 new bug; zero intrusive input; #969 stale-egg ruled out (import under own worktree). The freshest self-review rules (error-code registration 06-20 21:22Z / #969 stale-egg guard / public-API hold / QA-harness PIL cross-check — all <2–3d) were just exercised cleanly by two exemplary cycles back-to-back → attaching a rule now would over-fit, which Step 3.6 explicitly forbids. The intra-day no-change streak sits inside one quiet day where all hard work is correctly blocked on needs:ace (#1097/#1077/#975/#972) — not the multi-day PR-triage drift Step 3.6 warns against. Honest "no change" to avoid churn (consistent with the 09:22Z→11:52Z rows, not oscillating). | — | — |
| 2026-06-21 (12:52Z) | — | **No change — no new evidence.** The two signals that *completed* since the 12:22Z review were both **exemplary, not weaknesses**. **Dev #1129 (04:50Z, *fixes #1083*, merged `7809f7b` all-green)** ran HARDEST-FIRST — re-proved the native moat env-blocked this cycle (`where cl`/`where cmake` not found → #1096 JAB / #932 / #920 unbuildable locally, stays needs:ace #1097; #1060 OCR → #1077; #766 remaining rows frozen on #863/#975/#1106) and fell to the hardest *actionable* slice, the earliest-next-milestone v0.3.3 **never-lie bug #1083**: `browser click/hover` on a zero-area / `display:none` element silently dispatched `Input.dispatchMouseEvent` at viewport (0,0) and reported success. Fix: `_get_click_point()` returns None on the zero-area `getBoundingClientRect` fallback so click()/hover() raise loudly; the non-zero-box-no-quad path is unchanged (**no #1080 regression**). Proper **TDD** — 3 hermetic CDP-mocked tests proven to FAIL on pre-fix code (returned `(0.0, 0.0)`) → PASS after; gate clean (ruff PASS / mypy only the pre-existing mcp py3.9 match-stmt error / `test_browser_element.py` 36 PASS + 193 broader browser units); **public-API none** (internal bugfix, never-lie contract already documented) → SQUASH `--auto` correct under the public-API hold; branch auto-deleted (Rule 14). This is the **first non-test-only Dev PR in this quiet run** — correct HARDEST-FIRST behavior once #766's actionable rows ran out, not avoidance (避实就虚). **QA (12:43Z)** ran a clean exploratory PASS — 0 new bugs; swept `--json` error envelopes (get/scroll/set/menu-inspect/tray/drag/find all well-formed, real exit=1 re-checked **without** the pipe trap); investigated `highlight` no-target (`success:true hwnd:0`) as a possible silent no-op then **ruled it out with a 168KB visual-PROOF PNG** (foreground window elements correctly boxed/labelled, `hwnd:0` = documented `_resolve_hwnd` foreground sentinel) per the SOUL "screenshot-verify, no stdout-only false positive" rule; cross-checked capture dims against the physical PNG; left #972 correctly needs:ace-queued; zero intrusive input. The freshest self-review rules (#969 stale-egg guard / public-API hold / QA-harness / error-code-registration / hermeticity, all <2–3d) were just exercised cleanly by two exemplary cycles back-to-back → attaching a rule now would over-fit, which Step 3.6 explicitly forbids. The intra-day no-change streak sits inside one quiet day where all remaining hard work is correctly blocked on needs:ace (#1097/#1077/#975/#972) and Dev/QA are demonstrably sharp (a real never-lie bug shipped + a visual-proof false-positive avoided) — not the multi-day PR-triage drift Step 3.6 warns against. Honest "no change" to avoid churn (consistent with the 09:22Z→12:22Z rows, not oscillating). The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (13:22Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 12:52Z review was **QA-Mariana (05:10Z)**, and it was **exemplary, not a weakness**: it verified+closed the v0.3.3 never-lie bug **#1083** (`browser click/hover` on a zero-area/`display:none` element no longer dispatches at viewport (0,0)+reports success) on the **real-desktop superpower** — live headless Chrome with an offline fixture pinning both a `display:none` target and a real button at viewport (0,0): `click "#hidden"` → exit 1 `success:false "Cannot determine element position for click"` (raises loudly), the (0,0) corner button's `textContent` **unchanged after the failed click** proving the event was never dispatched at origin, a `click "#visible"` regression check (no #1080 regression), and the 3 pinned hermetic CDP tests pass. It cited the merged commit `7809f7b` per Rule 1, **re-checked the true exit code WITHOUT the pipe trap** (avoided the `$?`-of-tail footgun) on both plain and `--json` paths, used **no intrusive input** (CDP-dispatch only, no SendInput), and left #972 correctly needs:ace-queued (human-only security). The 13:07Z Dev cycle is still **in-flight** (loop-state.log shows `cycle START` with no `cycle END`; no new team-Dev PR and no new issue has appeared) → no completed Dev signal to assess. The freshest self-review rules (#969 stale-egg guard / public-API hold / QA-harness / error-code-registration / hermeticity, all <2–3d) were just exercised cleanly → attaching a rule on top of a single exemplary signal is precisely the over-fit Step 3.6 forbids. The intra-day no-change streak sits inside one quiet day where all remaining hard work is correctly blocked on needs:ace (#1097/#1077/#975/#972) and Dev/QA are demonstrably sharp (a real never-lie bug shipped + verified end-to-end this very window) — not the multi-day PR-triage drift Step 3.6 warns against. Honest "no change" to avoid churn (consistent with the 09:22Z→12:52Z rows, not oscillating). The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (14:22Z) | — | **No change — no new evidence.** No Dev/QA *work* cycle has produced a signal since the 13:22Z review. The **13:37Z Dev and QA cycles both exited 1 with zero work** (`dev-last.out`/`qa-last.out` END at +4s of START), and the **13:52Z Orc review itself also exited 1 at +4s** — the same **transient API rate-limit** burst already characterized at 11:22Z (a server-side throttle that self-clears, *not* the #915 `403 Request not allowed` auth-down class). The **14:07Z Dev+QA cycles never even logged a `cycle START`** (only the proxy/input-guard preamble lines appear, no START/END) → they died in the same throttle before reaching Step 0. There is therefore **no agent *behavior* to assess** this window: no cycle reached its work phase, no new team-Dev PR, no new issue above #1124. Per the 11:22Z precedent this is **infra noise, not a Dev/QA operating weakness** — manufacturing a process rule from a rate-limit burst is exactly the over-fit Step 3.6 forbids; repeated infra flakiness is the #917 watchdog's lane, not a SOUL/cycle edit. **Watch-flag (carried, not new):** if rate-limited empty cycles recur as a *sustained* burst (≠ a couple of rounds) it becomes a runner-resilience item (retry/backoff in `runner.ps1`) surfaced to Ace. The freshest operating rules (#969 stale-egg guard / public-API hold / QA-harness / error-code-registration / hermeticity, all <2–3d) were exercised cleanly by the recent exemplary cycles → honest "no change" to avoid churn (consistent with the 09:22Z→13:22Z rows, not oscillating). | — | — |
| 2026-06-21 (14:52Z) | — | **No change — no new evidence.** The completed signal since the 14:22Z review was **exemplary, not a weakness**: **QA (14:37→14:39Z)** ran a clean exploratory PASS — 0 new bugs, every observed gap mapped to an already-OPEN issue (#1089 `wait` timeout error block / #865 `see -j` envelope / #1054 `get/set -j` envelope) so **zero duplicates** (Rule 9), correct exit codes (1 runtime / 2 usage), left #972 human-only-queued, **zero intrusive input**. Separately, **Dev (14:37Z)** opened team-Dev **#1130** (`attach standard error block to wait predicate-timeout -j output`, **fixes #1089** — the long-tracked never-lie envelope sibling of #884/#895/#1001) with auto-merge SQUASH on, CI in progress — a real source bugfix picked HARDEST-FIRST while the native moat stays build-blocked (#1096/#1097); its cycle block is **not yet logged** (still finishing), so it is a *fresh/in-flight* signal, not a recorded weakness either. The freshest operating rules (#969 stale-egg guard / public-API hold / QA-harness / error-code-registration / hermeticity, all <2–3d) were just exercised cleanly → attaching a rule on top of an exemplary QA cycle + a healthy in-flight Dev fix is precisely the over-fit Step 3.6 forbids → honest "no change" row to avoid churn (consistent with the 09:22Z→14:22Z rows, not oscillating). The 14:22Z rate-limit watch-flag did **not** recur this window — both 14:37Z cycles reached their work phase (QA produced a verdict; Dev produced a PR). | — | — |
| 2026-06-21 (15:22Z) | — | **No change — no new evidence.** Both signals that *completed* since the 14:52Z review were **exemplary, not weaknesses.** **Dev #1089 (cycle END 14:57Z, *fixes #1089*, merged `8388bf8` Build C++ Core + Python 3.9–3.13 Ubuntu/macOS + CodeQL all-green)** attacked the never-lie `wait` predicate-timeout `-j` error envelope HARDEST-FIRST — re-proved the native-core moat block **fresh this cycle** (`where cl`/`cmake`/`nmake` ALL absent, JDK21 present → #1096/#932 unbuildable locally → correctly stays needs:ace #1097; #1060 OCR → #1077; #766 Ace-owned), then fell to the hardest *actionable* item, **TDD'd 7 failing tests → fix → green**, and **factored `build_error_object()` out of `json_error` as a single-source envelope shape** (extending the #1001 contract test to the wait-timeout path with hermetic mocks) — a real reuse/never-lie improvement with **no public surface → `--auto` correct**, branch deleted (Rule 14). **QA-Mariana #1089 (15:09Z)** verified+closed via the clean direct-`python -m naturo` no-pipe strict-UTF8 path (window/element timeout → standard block code=TIMEOUT/category=automation/recoverable; success path keeps the #895 shape, block attached-not-replacing), cited merged `8388bf8` per Rule 1, left #972 human-only-queued, **0 intrusive input** (JSON-envelope only, live machine untouched) — and **honestly self-corrected an inaccurate expected-behavior claim in its OWN earlier issue-body** (the non-existent `--element X --gone Y` mutual-exclusion check; element→gone→window elif precedence silently picks element-mode) rather than filing a false positive: exactly the never-lie discipline the harness rewards. No recurring operating weakness surfaced; the freshest rules (Option-coverage / Error-attribution / Test-hermeticity / QA-harness, all <2–3d) are being exercised cleanly, so a new rule now would over-fit a clean run (Step 3.6 forbids). Consistent with the 10:52Z→14:52Z streak. |
| 2026-06-21 (15:52Z) | — | no change — no new evidence. Completed signals since 15:22Z were both **exemplary, not weaknesses**: **QA (15:37–15:40Z)** ran an exploratory PASS (0 new bugs), chased the non-JSON-vs-`-j` usage-error exit-code divergence to root cause and confirmed it **intentional & documented** (`_emit_usage_error_json`, same axis as #866/#872) → filed nothing, and ran a real silent-failure check (parsed the captured PNG IHDR = claimed dims, no fabricated success). **Dev #1111 (07:45Z)** shipped the never-lie record/type fix all-green (TDD'd 5 hermetic tests) and **recovered a prior cycle's orphaned-but-correct in-flight #1111 work** left uncommitted+unlogged by the concurrent 15:07–15:16 dev cycle — that orphaning is the **#935** concurrency symptom (already queued needs:ace, runner/infra, human-only), and the recovery itself ("never leave an issue half-done") was exemplary. The freshest self-review rules are all <2–3d old and were just exercised cleanly here → adding a change on top would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn. | — | — |
| 2026-06-21 (16:22Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 15:52Z review was **QA-Mariana (08:11Z)**, and it was **exemplary, not a weakness**: it verified+closed the never-lie record/type fix **#1111** through the **real recording pipeline** (driving the actual `type_cmd`, asserting on data artifacts, 5/5 pass) with a **non-vacuous negative control** (3 fail on pre-fix code), and correctly **abstained from physical SendInput** because the foreground window was the Claude terminal and focus was unconfirmable per the focus-safety rule (physical typing was already confirmed in the original report, untouched by the fix) — no main push, zero production-code changes, #972 left human-only-queued. The **08:07Z Dev cycle is still in-flight** on the fresh actionable bug **#1065** (`app …--app` matching window-title not process-name; loop-state.log shows its `cycle START` with no `cycle END`, no new team PR) → no completed Dev signal to assess. The freshest self-review rules (#969 stale-egg guard / public-API hold / QA-harness / error-code-registration / hermeticity, all <2–3d) were just exercised cleanly → attaching a rule on top of a single exemplary signal is precisely the over-fit Step 3.6 forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→15:52Z streak, not oscillating). The 19:22Z / 14:54Z watch-flags did not recur. | — | — |
| 2026-06-21 (16:52Z) | `dev-cycle.md` (public-API guardrail) | **Dev auto-merged a public-surface change unattended again — 2nd instance after #1104.** Team-Dev PR **#1134** (*fixes #1123*, merged `c00227e`) added a new keyword param to the exported public method `BrowserPage.screenshot(..., selector=...)` and made the inert `--selector` flag functional, with `--auto` **ON** → it landed at 08:55Z before Orc could hold it. The PR body's own reasoning — *"implements the already-committed documented contract; no doc change needed"* — is the exact inversion the guardrail warns against (it holds *even when the symbol is promised in a committed doc*). With #1104 ("flagged-but-auto-merged anyway") this is now a **recurring** miss (2 instances), clearing the don't-over-fit-one-incident bar. Root cause: Dev treats "honors an existing doc" as a *reason it's not a sign-off item*, the opposite of the rule. | Added a crisp, **mechanical** decision trigger to the guardrail: *"honors a documented contract / no doc change needed" is NOT an exemption — it is a trigger*; the test is purely whether the PR changes a public signature (new/renamed param, return type, exported symbol) or makes a previously-inert public flag actually function — if yes → auto-merge OFF regardless of doc status. Cites #1134 as the inversion case. One surgical addition, no rewrite. | Dev stops rationalizing doc-promised public-surface adds as auto-mergeable; such PRs open with `--auto` OFF and reach Ace via `needs:ace` instead of landing unattended. Measure: next public-surface PR opens auto-merge OFF (zero new unattended public-API landings). |
| 2026-06-21 (17:22Z) | — | **No change — no new evidence, and the freshest rule (16:52Z, <1 cycle old) was exercised cleanly.** Both signals that *completed* since the 16:52Z review were **exemplary, not weaknesses**. **Dev #1135 (cycle END 17:22Z, PR #1137, *fixes #1135*, merged `d7280eb` via its own auto-merge)** registered the previously-unregistered browser `SCREENSHOT_FAILED` code (→ category `automation`/`recoverable`) and split the missing-`--selector` leg to `ELEMENT_NOT_FOUND` — a never-lie taxonomy fix — and **directly exercised the brand-new public-API trigger from the 16:52Z guardrail**: it explicitly reasoned *"public-API NOT triggered — no new/renamed CLI flag, no changed signature, no inert flag made functional; registering a new ErrorCode is the dev-cycle-prescribed remediation (cf #1086/#1047/#1067)"* and kept `--auto` ON correctly (error-code registration is not a public-signature change). That is the just-tightened rule applied as designed, not a miss. Dev also re-proved the native moat build-block fresh (`where cl/cmake/msbuild/vswhere` all absent → #1096 deferred to #1097) and added a self-maintaining sweep test. **QA #1123 (verified+closed `d7280eb`'s predecessor `c00227e`)** confirmed the crop fix on real headless Chrome (300×120 cropped vs 764×485 viewport), confirmed loud-fail on ghost selector + `--selector`+`--full-page` conflict, **ruled out a transient CDP "No such target id" race as harness flake** (re-navigate+retry per qa-cycle Step 4, not a naturo bug), and abstained from intrusive input. Freshest operating rule <1 cycle old, exercised cleanly by the very next Dev cycle → a tweak now would over-fit a single incident (Step 3.6 forbids churn). | — | — |
| 2026-06-21 (17:52Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 17:22Z review was **QA-Mariana (verified+closed #1135 @09:42Z)**, and it was **exemplary, not a weakness**: it verified the browser error-code taxonomy fix end-to-end on the real desktop (`screenshot --selector '#does-not-exist' -j` → `ELEMENT_NOT_FOUND` / category `automation` / recoverable, was `SCREENSHOT_FAILED`/category `unknown`/non-recoverable), **swept the whole browser family** confirming every fallback code is now registered with a real category (no `unknown` remains), cited the merged commit `d7280eb` per Rule 1, left the human-only #972 needs:ace-queued, and used **zero intrusive input** (the JSON envelope is the ground truth for a text-contract fix). The **09:37Z Dev cycle is still in-flight** on the actionable P1 #1124 (loop-state shows `cycle START` / `status:in-progress` set @09:42Z, no `cycle END`; no new team-Dev PR, no new issue above #1136) → no completed Dev signal to assess. The freshest operating rules (16:52Z public-API trigger < 1 cycle old, plus #969 stale-egg guard / QA-harness PIL cross-check / error-code-registration / hermeticity, all < 2–3d) were just exercised cleanly → attaching a rule on top of a single exemplary signal is precisely the over-fit Step 3.6 forbids. The intra-day no-change streak sits inside one quiet day where all remaining hard work is correctly blocked on needs:ace (#1097/#1077/#975/#972) and the team is demonstrably sharp (a never-lie taxonomy fix shipped + verified end-to-end this very window). Honest "no change" to avoid churn (consistent with the 09:22Z→17:22Z rows, not oscillating). | — | — |
| 2026-06-21 (18:22Z) | `dev-cycle.md` (Step 3 gate — auditable full-suite run) | **Pre-registered watch-flag FIRED on its 2nd instance: an `--auto` PR's first CI run went red on platform-independent test failures the prescribed full local `tests/` run would have caught.** The 06-20 19:22Z row flagged exactly this and said: *"if a second team PR enables auto-merge while its own modified test module is red, strengthen Step 4 to require pasting the modified-module pytest summary (make 'I ran the tests' auditable)."* **Instance 1:** #1081 (06-20 19:22Z) auto-merged with its own modified `test_browser_element.py`/`test_browser.py` red (mock stubs too short for the new multi-call `_get_click_point`). **Instance 2:** #1124/#1138 (06-21 ~10:08Z) — Dev's own log: *"first CI run red on 3 EXISTING screenshot mocks (Page.enable shifted their send sequence/counts)… Root: I'd only run tests/browser/ locally, missing tests/ root files… Lesson reinforced: run the whole non-desktop suite, not a subdir."* Both = the same incomplete-local-run → red-first-CI class, >1 day apart, clearing the over-fit bar; both self-corrected pre-merge (no bad code reached `develop`) but each wasted a CI run and risked a red `--auto` landing. The gate (Step 3.3 line 87) already prescribes the full `python -m pytest tests/ …`; the gap is **adherence/auditability** (Dev ran a sub-folder), exactly the deficiency the pre-registered flag named. | **`dev-cycle.md` Step 3.3:** added an **auditable full-suite** rule right under the gate — run the FULL `tests/` tree (never a sub-folder like `tests/browser/`, which hides both a sibling module reddened by a production change and a partially-edited module you touched), and **paste the exact `pytest tests/ …` command + its one-line summary into the gate report** so "I ran the tests" is auditable; cites #1081 + #1124/#1138 as the two instances. One surgical addition, builds on (does not undo) the parity / error-attribution / option-coverage / platform-order / hermeticity / error-code-registration rows. | Dev stops running a sub-folder and calling it a gate; the full-suite run is auditable in the report, so the incomplete-local-run → red-first-CI class is caught before push, not on an `--auto` PR's first CI run. **Measure:** next Dev PR's gate report pastes a full `tests/` summary and its first CI run is green (no red-then-fix on platform-independent tests). |
| 2026-06-21 (19:22Z) | — | **No change — no new evidence (loop halted on an account session-usage cap, not agent behavior).** No Dev/QA *work* cycle has produced a signal since the 18:22Z review: the **18:37Z Dev+QA, 18:52Z Orc, and 19:07Z Dev+QA cycles all died** — `dev-last.out`/`qa-last.out` both end on *"You've hit your session limit · resets 7pm (Asia/Shanghai)"* and none logged a `cycle END` with work. This is a **hard account-quota cap**, distinct from both the 11:22Z/14:22Z *transient rate-limit* burst and the #915 `403 Request not allowed` *auth-down* class — and it **self-cleared at the 7pm reset** (this 19:22Z Orc cycle ran fine post-reset; `develop` green throughout, nothing lost). There is **no agent *behavior* to correct** — the cycles never reached Step 0 — so manufacturing a SOUL/cycle rule from a usage cap is exactly the over-fit Step 3.6 forbids; loop-resilience to infra outages is the #917 watchdog's lane, not a prompt edit. The freshest rule (18:22Z auditable full-suite, <1 cycle old) has not yet had a Dev cycle to be exercised. **Watch-flag (new, first instance):** a *daily* session cap that reliably halts Dev+QA+Orch around the same window each day is an Ace-only plan/scheduling matter (upgrade tier, or stagger the runner cadence so all three roles don't burn the quota together) — surfaced in `NEEDS-ACE.md` this cycle but **not yet filed** (one self-clearing ~30 min window does not clear the file-an-issue bar; escalate to a `needs:ace` issue only if it recurs as a sustained daily block on ship-gate work). Honest "no change" row to avoid churn (consistent with the 09:22Z→18:22Z streak, not oscillating). The 19:22Z / 14:54Z product watch-flags did not recur. | — | — |
| 2026-06-21 (19:52Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 19:22Z review was **QA (19:40Z)**, and it was **exemplary, not a weakness**: with no QA-verifiable `status:done` work (only #972, human-only, correctly untouched) it ran exploratory per Rule 13 and filed a **genuine new P2 #1139** — `browser launch --user-data-dir <relative>` fails opaquely (Chrome exits 21 → generic `UNKNOWN_ERROR`) while an absolute path succeeds — with the root cause pinpointed in `naturo/browser/_launcher.py::launch_chrome()` (the dir is passed to Chrome verbatim; Chrome needs an absolute path), a suggested `abspath` fix, after **ruling out its own harness** (reproduced across fresh dirs; abs-vs-rel the only variable) per the 20:22Z QA-harness rule, killed only the one Chrome it launched (user's real Chrome untouched), wrote no production code, used no intrusive input, left #972 human-only-queued. The **19:37Z Dev cycle is still in-flight** on the fresh actionable bug **#1079** (`clipboard get` mislabels image/file content as empty/`text`; `dev.lock` present, issue touched 11:44Z, no `cycle END`) → no completed Dev signal to assess. The freshest operating rules (18:22Z auditable full-suite < 2 cycles old and **not yet exercised by a completed Dev cycle**; #969 stale-egg guard / public-API trigger / QA-harness / error-code-registration / hermeticity all < 2–3d) were just exercised cleanly → attaching a rule on top of a single exemplary QA signal + a healthy in-flight Dev fix is precisely the over-fit Step 3.6 forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→19:22Z streak, not oscillating). The 19:22Z session-cap watch-flag did **not** recur (the 7pm reset held — QA/Dev/Orch all reached their work phase this window); the 19:22Z / 14:54Z product watch-flags did not recur. | — | — |
| 2026-06-21 (20:22Z) | — | **No change — no new evidence.** Both signals that *completed* since the 19:52Z review were **exemplary, not weaknesses.** **Dev (cycle END 20:01Z)** shipped the never-lie clipboard fix **#1079** via PR **#1140** (`6f45ca5`, *fixes #1079*): `get` now consults `clipboard_info()` when no text and reports the real format (image/files/empty) + truthful `has_text` in both plain and `-j` — directly on the never-lie axis (#1098/#1101/#1111), merged all-green, branch auto-deleted (Rule 14), set `status:done` and left the close to QA (Rule 1, no premature self-close). **QA (20:10–20:12Z)** then verified+closed #1079 using **PowerShell clipboard APIs as independent ground truth** (not naturo's own output) — cross-checked `get` vs `info` across all 4 states (text/empty/image/files), confirmed they now agree where pre-fix image/files reported "(empty)"/format:text, ruled out its own harness, simulated **no intrusive input** (set clipboard via PowerShell, not type/click/press), and restored state — a textbook application of the QA-harness "independent ground truth / rule out your tooling" discipline. The freshest self-review rules (18:22Z auditable full-suite, 16:52Z public-API hold, HARDEST-FIRST, hermeticity) are all <2d old and nothing this window contradicted them; attaching a change on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row. The 19:22Z session-cap watch-flag did **not** recur (post-7pm-reset held; Dev+QA+Orc all reached their work phase). | — | — |
| 2026-06-21 (20:52Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 20:22Z review was **QA-Mariana (20:39Z)**, and it was **exemplary, not a weakness**: with no QA-verifiable `status:done` work (only #972, human-only, correctly left needs:ace-queued) it ran an exploratory PASS per Rule 13 — swept JSON validity + exit codes (list screens/apps/windows, doctor, clipboard get → all exit 0, valid UTF-8) and error paths (get/click/find/wait on non-existent app/window → exit 1, `success:false`, structured envelope with code/category/context + fuzzy "Did you mean" suggestions) and filed **0 bugs**. Critically, it ran **harness discipline** to the letter (qa-cycle Step 4 / the 20:22Z QA-harness rule): it caught three "surprising defects" and **ruled every one out as its own tooling before filing** — (a) Git Bash `/tmp` vs Windows-Python `/tmp` path mismatch made valid JSON look empty, (b) a pipe+stdin gbk console threw `JSONDecodeError` on valid UTF-8, (c) the gbk console couldn't print the U+2022 bullet — re-ran on a clean path (UTF-8 file, no pipe) and the signals vanished → **no false bug**. The **20:37Z Dev cycle is still in-flight** on the fresh actionable bug **#1084** (`list windows`/`list apps` emit `process_name` as a non-round-tripping full path; loop-state shows its `cycle START` with no `cycle END`, issue touched 20:41Z, no new team-Dev PR) → no completed Dev signal to assess. The freshest operating rules (18:22Z auditable full-suite, 16:52Z public-API trigger, #969 stale-egg guard / QA-harness / error-code-registration / hermeticity, all < 2–3d) were just exercised cleanly → attaching a rule on top of a single exemplary QA signal is precisely the over-fit Step 3.6 forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→20:22Z streak, not oscillating). The 19:22Z session-cap watch-flag did **not** recur (7pm reset held). | — | — |
| 2026-06-21 (21:22Z) | — | **No change — no new evidence; the one envelope-sibling data point is being fixed at the BACKLOG level, not via a prompt edit.** The only work cycle that *completed* since the 20:52Z review was **QA-Mariana (21:12Z)** and it was **exemplary**: exploratory PASS (only `status:done` = #972, human-only, correctly left needs:ace), and it filed one genuine P2 — **#1141** (`visual compare -j`/`diff -j` omit the `success` envelope) — *after verifying the defect on the clean path first* (direct invocation, output redirected to file, strict-UTF-8 `json.load`, asserted siblings `list`/`baseline` DO carry it; ruled out pipe/locale harness artifacts per the 20:22Z QA-harness rule) and cleaned up its throwaway baseline → no false bug. The **21:07Z Dev cycle is still in-flight** (loop-state shows `cycle START` 21:07Z, no `cycle END`; #1084 last touched 12:41Z, no new team-Dev PR) → no completed Dev signal to assess. #1141 is a **direct missed sibling of CLOSED #977** (same file `visual_cmd.py`: #977 fixed `list`/`baseline`, left `compare`/`diff`) — i.e. the envelope-shape drip #865→#1054→#977→#1141. That is real evidence of a *fix-the-instance-not-the-class* pattern, **but** (a) it is a single envelope-SHAPE instance, distinct from the 06-20 21:22Z **error-code-registration** sweep rule which already prescribes sibling-sweep for error *codes*; (b) the proper remedy is structural, and I took it **this cycle at the backlog level** — filed umbrella **#1142** mandating a *one-shot repo-wide sweep of every `-j`/`--json` success path* + a *self-maintaining hermetic guard test* that fails CI if any future subcommand forgets the envelope, which ends the drip more durably than a prompt line; (c) Dev has not yet had a chance to act on #1141, so attributing an operating weakness to an in-flight/unstarted fix would over-fit, which Step 3.6 forbids. Honest "no change" row to avoid churn. **Watch-flag (pre-registered):** once #1142's guard test lands, if Dev fixes an envelope-shape bug and QA still re-files a *same-file* sibling, that is a 2nd-instance process miss → then extend the dev-cycle sibling-sweep rule to cover success-envelope *shape*, not just error-code taxonomy. | — (backlog: umbrella #1142 filed instead) | The envelope-consistency drip gets a bounded one-shot fix + a regression guard at the source, rather than another QA bug per cycle; if the guard still doesn't change Dev behavior, the watch-flag escalates it to a prompt rule with 2 cited instances. |
| 2026-06-21 (13:52Z) | QA | QA re-discovers the **same two harness artifacts every cycle** — cp936/gbk console mangling valid UTF-8 JSON, and `naturo … \| head` masking naturo's exit code with the pipe's — then correctly rules each out (no false bug). Evidence: 4 consecutive QA cycles 20:22 / 20:39 / 21:12 / 21:40Z each spent a re-run re-deriving these known lies. The 20:22Z rule (Step 2.4) is *reactive* (rule out AFTER a surprising result); the recurrence is the cost of having no *preventive* default. | `qa-cycle.md` Step 0: set `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8` up front + a "harness hygiene" note — never read naturo output through a pipe when exit code/bytes matter (redirect to file, strict-UTF-8 decode). Builds on Step 2.4, does not replace it. | QA stops burning a re-run per cycle on the same two artifacts; cp936 mojibake is forestalled, pipe-exit-code confusion avoided by default; Step 2.4 remains the safety net for genuinely *surprising* results. |
| 2026-06-21 (14:22Z) | — | **No change — no new evidence, and the freshest rule (13:52Z preventive QA-harness, <30 min old) was just exercised cleanly.** Both signals that *completed* since the 13:52Z review were **exemplary, not weaknesses**. **QA-Mariana (14:11Z)** verified+closed **#1084** (P2 — full-path `process_name` round-trips back into `--app`: count 4 vs pre-fix 0; cross-command resolver parity proven via `capture`/`see --app <fullpath>`; #789 over-match guard intact) **and #1144** (P1 — `find` strategy auto-detection: `.png`→image / `app://`+`@`→selector / text→tree; explicit `--image`/`--selector` always win) on the real desktop, cited the merged commits per Rule 1, left #972 human-only-queued, used **zero intrusive input** — and **directly applied the just-added 13:52Z rule** (`PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8` set up front, all naturo output redirected to files + strict-UTF-8 decoded, no pipe; no harness false-positive filed). **Dev (#1144 cycle)** shipped the find auto-detect slice HARDEST-FIRST — re-proved the native-moat block **this cycle** (`where cmake/cl/nmake` not found, JDK 21 present → #1096/#932 unbuildable locally, stays needs:ace #1097; #1060 OCR → #1077), reused the existing `_find_with_image`/`_find_with_selector` dispatch (no new engine code), ran the **full `tests/` suite** per the 18:22Z auditable-full-suite rule, applied the mechanical public-API guardrail (no new flag/param → `--auto` correct), all-green SQUASH, branch auto-deleted (Rule 14). The freshest operating rules (13:52Z preventive QA-harness <30 min; 18:22Z auditable full-suite; 16:52Z public-API trigger; #969 stale-egg guard / error-code-registration / hermeticity, all <2–3d) were just exercised cleanly by two exemplary cycles back-to-back → attaching a rule now would over-fit, which Step 3.6 explicitly forbids (and would risk oscillating against the 13:52Z change made <30 min earlier). Honest "no change" row to avoid churn (consistent with the 09:22Z→13:52Z streak). The 19:22Z session-cap / 19:22Z+14:54Z product watch-flags did **not** recur. | — | — |
| 2026-06-21 (14:55Z) | — | **No change — no new evidence.** The only signal that *completed* since the 14:22Z review is **QA-Mariana (14:40Z)** — exemplary, not a weakness: with no verifiable queue (only #972, human-only, correctly left) she ran exploratory testing and filed a genuine P2 (**#1146** — `find --help` documents the bare `@save-btn` saved-selector form but the resolver requires `@app/name`) with exact repro / actual / expected / **pinpointed root cause** (`_find.py:115,134` doc vs `selector_cmd.py:105` resolver), ruled out a duplicate, applied the freshest 13:52Z UTF-8/no-pipe rule cleanly (output→file, strict-UTF-8), zero intrusive input. The Dev cycle (#1141 envelope fix) is still **in-flight** (no `cycle END`) → no completed Dev behavior to assess this window. #1146 is a **single clean help-text-vs-resolver incident**; the adjacent migration-guide doc bugs (#1112/#1115) are a *different* mechanism (doc-ahead-of-surface by design in #766, already covered by the never-lie doc rules), so they are not the same class — and `dev-cycle.md` **L129–132 already mandates honoring documented options pinned by a test**. Manufacturing a new "execute every help example" rule from one incident is exactly the over-fit Step 3.6 forbids; the freshest rules (13:52Z QA-harness <1d; 18:22Z auditable full-suite; 16:52Z public-API trigger) were just exercised cleanly. Honest "no change" to avoid churn (consistent with the streak, not oscillating). | — | — |
| 2026-06-21 (15:52Z) | — | **No change — no new evidence.** Both signals that *completed* since the 15:22Z review were **exemplary, not weaknesses**, and the one notable incident is the already-queued #935 hazard handled *correctly*, not a new gap. **Dev (#1148, *fixes #1114*, cycle END 15:51Z, `5e1df10` merged all-green @15:50Z — Build C++ Core + Python-with-DLL Windows + Ubuntu/macOS ×3.9–3.13 + CodeQL + Lint all green)** attacked the deepest actionable never-lie correctness bug HARDEST-FIRST after re-proving the native-moat block **this cycle** (`where cmake/cl/nmake/msbuild` ALL not found, only java/javac=JDK21 → #1096/#932 unbuildable locally, stays needs:ace #1097; #1060 OCR→#1077; input-safety #863/#972/#975 + ship-gate #914 human-only): a failed `capture --region/--element` no longer leaves the full uncropped screenshot on the user's `-o` path (track the pre-written path only when a crop is requested; `unlink(missing_ok)` best-effort on every crop-validation `SystemExit`; `img.load()` before the off-screen check so PIL releases the Windows handle in time to unlink), **swept a latent sibling bug in the same diff** (zero-size-element message read a non-existent `element.name` field → `AttributeError` swallowed by a broad except → mis-reported `CAPTURE_ERROR`; now reads real title/label so `ZERO_SIZE_ELEMENT` surfaces — the Error-attribution rule applied proactively), ran the **full `tests/` suite** per the 18:22Z auditable rule (6702 passed; the 8 fail + 11 error all pre-existing env-only, **reproduced on pristine develop with changes stashed**), 8 hermetic backend+platform-mocked CLI tests, applied the **mechanical public-API guardrail** (no new flag/param/exported symbol, no inert-flag activation, no JSON-envelope/code change → `--auto` correct; behavior change noted in PR+CHANGELOG), set `status:done` in-cycle, branch auto-deleted (Rule 14). The genuinely new event: Dev found **orphaned uncommitted WIP for #1114 in the shared `naturo-dev` worktree** (mtime 23:15–23:19, no active dev lock besides its own, prior cycle ended 23:00) — the **#935 concurrent-worktree hazard** manifesting — and handled it the **safe** way (verified `_capture.py` byte-identical to its base `56f3d5c` on `origin/develop` = clean carry-over, confirmed the issue unassigned/not-in-progress = no collision, then adopted + rigorously self-reviewed as its own) rather than a blind `reset --hard` wipe or a blind adoption. That is **exemplary handling of an already-queued harness item (#935, human-only/runner-coordination)**, not a Dev operating weakness — recorded here as reinforcing evidence so a future cycle does not mis-flag the safe adopt-and-verify pattern as a new gap. **QA (15:40Z, PASS no-defects)** ran a clean read-only L0/L1 sweep (JSON validity + exit codes; error-path envelopes consistent; physical capture-px matches reported screen dims; regression-checked the freshest #1141/#1147 visual `-j` envelopes — identical-compare `success:true match:true`, missing-baseline `success:false` rc1), **ruled out three of its own flag-errors as harness lies** (`capture --out`→`-o`; `baseline --name`→positional; `compare --from`→`--current`) per the qa-cycle Step 2.4 discipline and filed **nothing false**, set `PYTHONUTF8=1` + confirmed `import naturo` resolves under its own worktree (no #969 stale-egg), left #972 human-only-queued, used zero intrusive input. The freshest operating rules (13:52Z preventive QA-harness, 18:22Z auditable full-suite, 16:52Z public-API trigger, #969 stale-egg guard, Error-attribution — all <1–3d) were just exercised cleanly by both cycles; attaching a rule on top of two exemplary cycles + a correctly-handled already-queued harness item would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→15:22Z streak, not oscillating). The 21:22Z envelope-sibling / 19:22Z session-cap / 14:54Z product watch-flags did **not** recur. | — | — |
| 2026-06-21 (15:22Z) | — | **No change — no new evidence.** Both signals that *completed* since the 14:55Z review were **exemplary, not weaknesses.** **Dev (#1141, cycle END 14:58Z, PR #1147 `07413f3` merged all-green)** added the `-j` success envelope to the visual comparison family and — beyond the two subcommands named in the bug (`compare`/`diff`) — **swept the same-file siblings `report`/`suite`** in one diff, exactly the sibling-sweep discipline the 21:22Z watch-flag wanted (drains umbrella #1142 rather than dripping a bug/cycle); ran the **full `tests/` suite** (6703 passed; the 10 fails reproduced on clean `develop` = pre-existing env-only, none in touched files) per the 18:22Z auditable-full-suite rule, wrote **hermetic** CLI tests (`mkdtemp` to dodge the concurrent-loop contended-`tmp_path` WinError5), and applied the **mechanical public-API guardrail** (no new flag/param/exported symbol; envelope-normalization toward the existing documented contract, same class as merged #876/#977/#980 → `--auto` correct, JSON-shape change called out in PR+CHANGELOG), set `status:done` in-cycle, branch auto-deleted (Rule 14). **QA (verified+closed #1141 @15:12Z)** confirmed all four subcommands on the clean path and caught an **initial false-FAIL as #969-class** (temp cwd made `import naturo` resolve to the stale sibling `naturo-qa-mariana`), re-ran from the worktree root — the **#969 stale-egg guard working as designed**, not a weakness (root cause = the queued infra item #969, human-only). The freshest operating rules (13:52Z preventive QA-harness, 18:22Z auditable full-suite, 16:52Z public-API trigger, #969 stale-egg guard — all <1–2d) were just exercised cleanly by both cycles; attaching a rule on top of two exemplary cycles would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→14:55Z streak, not oscillating). The 21:22Z envelope-sibling watch-flag did **not** fire (Dev swept the siblings itself); the 19:22Z session-cap / 19:22Z+14:54Z product watch-flags did not recur. | — | — |
| 2026-06-21 (16:22Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 15:52Z review was **QA-Mariana (#1114 verified+closed @00:18Z local)**, and it was **exemplary, not a weakness**: it verified the failed-crop never-lie fix on the **real desktop using live-filesystem ground truth** (the stronger signal here, since the bug *is* a file on disk) — non-positive-W region `(100,100,0,200)` and fully-off-screen `(5000,5000,100,100)` both leave **no file** (rc1, `success:false`), control valid crop produces exactly 200×150, and the malformed/short-region parse branch is also clean — plus the 8 hermetic regression tests pass, error messages echo the user's requested region (no #1050 clamped-box leak), cited merged commit `5e1df10` per Rule 1, left #972 human-only-queued, used **zero intrusive input**. Critically, its first repro run from a `.work/` subdir "reproduced" the OLD bug because `python -m naturo` there resolved to the editable install at sibling `naturo-qa-mariana` (stale pre-fix `_capture.py`) — a textbook **#969-class stale-egg** — and QA **caught it, re-ran from the worktree root, and filed NOTHING false** (a false FAIL avoided): the #969 stale-egg guard working exactly as designed (#969 = queued infra, human-only — not an agent weakness). The **Dev cycle is still in-flight on #1139** (`browser launch --user-data-dir <relative>` opaque Chrome-exit-21; loop-state shows `status:in-progress` updated 16:11Z, no `cycle END`; no new team-Dev PR) → no completed Dev signal to assess. The freshest operating rules (18:22Z auditable full-suite, 16:52Z public-API trigger, #969 stale-egg guard, 13:52Z preventive QA-harness — all <1–3d) were just exercised cleanly; attaching a rule on top of a single exemplary signal is precisely the over-fit Step 3.6 forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→15:52Z streak, not oscillating). The 21:22Z envelope-sibling / 19:22Z session-cap / 14:54Z product watch-flags did **not** recur. | — | — |
| 2026-06-21 (16:52Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 16:22Z review was **QA @00:45Z**, and it was **exemplary, not a weakness**: a lateral exploratory sweep of the `-j`/`--json` envelope + visual-regression family on the real desktop (1920×1080@96DPI) that (a) re-confirmed the freshest #1141/#1147 envelope merge healthy, (b) **ruled out a harness-lie before filing anything** (no shell pipe — redirected to file with `rc=$?`; strict UTF-8; in-tree import shadows the editable install → not stale-egg/locale), (c) **cleaned up every test baseline it created PLUS two stray prior-cycle QA baselines** so `visual list` returns count 0 for a real user, and (d) filed exactly **one genuine, well-scoped input-validation defect (#1149** — `visual --threshold` not range-validated to 0.0–1.0; out-of-range makes pixel-identical images report `match:false` → false CI regression) as normal in-authority Dev work, no false `needs:ace`. The Dev cycle @00:37 **errored on a `naturo-loop-state.log` file-lock** (orch/dev write race) — a harness-infrastructure transient **already tracked by #935**, NOT a Dev agent-behavior weakness, so nothing to encode. The freshest operating rules (Option-coverage / Error-attribution / QA-harness #969 stale-egg guard, all <1–3d) were exercised cleanly; a rule change now would over-fit a clean run (Step 3.6 forbids). Consistent with the 15:22Z→16:22Z streak. |
| 2026-06-21 (17:52Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 17:22Z review were exemplary, not weaknesses. **Dev #1149 (PR #1153, *fixes #1149*)** is the textbook execution of the freshest rules: a **hardest-first probe with PROVEN env blocks** (live `where cl`/`where cmake` absent → moat #932/#1096/#1150 deferred to the already-queued #1097, OCR #1060 to #1077, #763 correctly rejected as non-deterministic for unattended TDD), then the hardest *actionable* bug taken with **red-first TDD**, an **auditable stash-and-rerun gate** (pre-existing fails reproduced on clean develop), the **mechanical public-API check** (existing flag + private helper → `--auto` ON correctly), and **parity tech-debt filed immediately as #1154** instead of scope-creeping the atomic PR — directly rebutting the 避实就虚 weakness class the 06-20 row targeted. **QA @17:42Z (#1139 verify+close)** used **physical ground-truth** (Chrome profile dirs at resolved abs paths + CDP port 9222 open), lateral-swept all 35 `browser` subcommands, and **ruled out a pipe-lie** (`\| head` masking exit code) before filing #1152 — no false report. Freshest operating rules (<1–3d) exercised cleanly by the very next cycles → a tweak now would over-fit a single incident (Step 3.6 forbids churn). | — | — |
| 2026-06-21 (17:22Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 16:52Z review were **exemplary, not weaknesses.** **QA @01:10Z** filed a genuine, fully-root-caused **P1 #1150** — non-ASCII window titles corrupted to U+FFFD mojibake across `list windows`/`list apps`/`app windows`/`app find` (native ANSI codepage-936/GBK read path → UTF-8/replace decode = irreversible data loss) — and did it with textbook verification discipline: it took **independent OS ground truth** (ctypes `user32.GetWindowTextW`, NOT naturo's own stdout), read the corruption from **raw on-disk JSON bytes decoded strict-UTF-8** (no pipe, `rc=$?` direct), *proved* the byte-level mechanism (`GBK('无标题')=ce de b1 ea cc e2`; `.decode('utf-8','replace')` == naturo's exact output), distinguished it from the closed #894/#1025 (\uXXXX *escaping of valid data* ≠ genuine *loss*), ruled out console/locale/stale-egg harness lies, filed zero false `needs:ace`, simulated no intrusive input, and left #972 human-only-queued — applying the QA-harness "independent ground truth / rule out your tooling" rule cleanly. **Dev @01:25Z** completed the **interrupted** #1139 (`browser launch --user-data-dir <relative>` → opaque Chrome-exit-21) by **adopting the prior cycle's complete-but-uncommitted branch and rigorously self-reviewing it** rather than a blind `reset --hard` that would have discarded a finished fix+tests — i.e. the already-queued **#935 concurrent-worktree hazard navigated the *safe* adopt-and-verify way (exactly the pattern documented in the 15:52Z row), NOT a Dev operating weakness** — then resolved `--user-data-dir` via `os.path.abspath(os.path.expanduser(...))`, added 2 regression tests, recomputed the absolute-path asserts for POSIX+Windows parity, ran an **auditable gate** (ruff clean; mypy naturo clean; full `tests/ -m "not desktop"` with every failure proven pre-existing via stash-and-rerun on clean develop), applied the **mechanical public-API guardrail** (existing flag, no new/renamed signature → `--auto` correct), and opened PR #1151 auto-merge-pending. The freshest operating rules (13:52Z preventive QA-harness, 18:22Z auditable full-suite, 16:52Z public-API trigger, #969 stale-egg guard, Error-attribution — all <1–3d) were just exercised cleanly by both cycles; attaching a rule on top of two exemplary cycles + a correctly-handled already-queued harness item would over-fit, which Step 3.6 explicitly forbids. Honest "no change" row to avoid churn (consistent with the 09:22Z→16:52Z streak, not oscillating). The 21:22Z envelope-sibling / 19:22Z session-cap / 14:54Z product watch-flags did **not** recur. | — | — |
| 2026-06-21 (18:22Z) | — | **No change — no new evidence.** The only work cycle that *completed* since the 17:52Z review was **QA @02:10Z (#1149 verified+closed)**, and it was **exemplary, not a weakness**: a strictly **file-only** verification of the `visual --threshold` range-validation fix (merged `17b5274`/PR #1153, confirmed ancestor of HEAD) that (a) drove out-of-range `--threshold` (`95`/`2.0`/`-0.5`) across `diff`/`compare`/`report` **and** the suite-JSON top-level + per-test paths → every one returns the `INVALID_INPUT` envelope (exit 1), (b) confirmed the boundary/in-range matrix (`0.0`/`1.0`/`0.95`) still yields `match:true` similarity 1.0 exit 0 — i.e. the original false-FAIL repro fully resolved, (c) applied the qa-cycle Step 2.4 anti-pipe-lie discipline (output→file with `rc=$?` direct, no shell pipe; strict UTF-8; `import naturo` confirmed resolving under the `naturo-qa` worktree → no #969 stale-egg), (d) used **zero intrusive input** (two identical 1920×1080 screenshots, no SendInput), left the human-only #972 untouched/queued, and **cleaned up its own baselines/reports** under `~/.naturo/visual/`. The **Dev cycle (#1133, hermetic `test_win32_hybrid.py`) is still in-flight** (`status:in-progress`, updated 18:12Z, no `cycle END` in loop-state, no new team-Dev PR) → no completed Dev signal to assess this cycle. The freshest operating rules (18:22Z[06-20] auditable full-suite, 16:52Z public-API trigger, 13:52Z preventive QA-harness, #969 stale-egg guard, Error-attribution — all <1–3d) were just exercised cleanly; attaching a rule on top of a single exemplary signal is precisely the over-fit Step 3.6 forbids. Honest "no change" row to avoid churn (consistent with the 15:22Z→17:52Z streak, not oscillating). The 21:22Z envelope-sibling / 19:22Z session-cap / 14:54Z product watch-flags did **not** recur. | — | — |
| 2026-06-21 (18:52Z) | Dev | **Dev filed a parity tech-debt on a FALSE premise — the claimed gap was already fixed at HEAD.** While shipping #1149 (visual `--threshold` range validation), Dev filed **#1154** asserting `find/click --image --threshold` was "not validated to [0.0,1.0] (same class as #1149)". But the range guard already existed at `naturo/cli/core/_find.py:540` (`if not (0.0 <= threshold <= 1.0)`), added the day before via #1093 (`f04b0d8`, 2026-06-20) — verified this cycle by `git blame`/grep. QA spent its **02:44Z** cycle reproducing find/click `--threshold 95/-1 → INVALID_INPUT exit 1` (both `-j` and human, live and `--screenshot` paths) and posted a correcting comment; only a *cosmetic* gap is genuine (declared `type=float` not `click.FloatRange`, so help reads "FLOAT" not "FLOAT RANGE" and it exits 1 not Click's usage-exit 2). The prior 17:52Z Orc row even *praised* #1154 as exemplary parity-filing — so the loop rewarded a false-premise issue, which is why a corrective rule is worth more than a watch-flag here. This is a distinct, citable, novel weakness (not 避实就虚, not envelope-shape, not hermeticity): **assuming parity-of-absence from the bug you just fixed without grepping the sibling at HEAD.** Single instance, but the fix is one surgical, low-risk preventive line and the cost (a wasted QA cycle + a misleading open issue) is concrete. | `dev-cycle.md` Step 1 self-driven mode: before filing a *parity/sibling* tech-debt claiming a command lacks a guard/validation you added/noticed elsewhere, `grep` the sibling's current code to confirm the gap is real **at HEAD**; if only cosmetic, scope the title to that. Cites #1154/#1149/#1093. | Dev stops filing already-fixed "parity" tech-debt; QA stops burning cycles disproving false premises; the tracker stays honest. Watch-flag: if a future cycle still files a sibling tech-debt without a HEAD check, escalate (the rule isn't landing). |
| 2026-06-22 (03:22Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 18:52Z review were **exemplary, not weaknesses.** **Dev @02:51Z (#1133 → PR #1155, `9cd33ef`, merged green Linux/macOS/Windows)** is textbook execution: it made `test_win32_hybrid` hermetic, ran the full suite under a **private `--basetemp`** after correctly diagnosing the first-run 635×`WinError 5` as a *concurrent-QA-pytest basetemp race* (not its own defect — anti-false-red discipline), and filed tech-debt **#1156** that does NOT repeat the #1154 false-premise miss: it cites a *genuine* `_bulk_get_process_info` NoneType crash on non-UTF-8 subprocess output plus a real `test_app_ids`/`test_electron` hermeticity gap (family of #1100/#1133) — i.e. the freshest 18:52Z HEAD-check rule appears respected on its first opportunity. **QA @03:14Z (#1133 verified+closed)** is the matching standard: verified on the **real Windows desktop** (the env CI can't reproduce), proved the mock is *non-vacuous* (un-mocked `enumerate_hybrid_tree` returns ElementInfo on this host = the exact original failure; mocked→None), strict-UTF-8 file-redirected output (no pipe-lie), zero intrusive input, left human-only #972 untouched, cleaned its own baselines. No recurring operating weakness surfaced; the freshest rule (dev-cycle.md HEAD-check, landed 18:52Z <1d ago) has had a single clean exercise → over-fitting forbidden (Step 3.6). Honest no-change row; no prompt churn. |
| 2026-06-22 (03:52Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 03:22Z review were **exemplary, not weaknesses.** **Dev @03:37Z** shipped **PR #1157** (*fixes #1150*) attacking the P1 CJK-mojibake bug **at the correct layer** — it decodes native window titles via the OS **ANSI codepage** instead of the UTF-8-mode locale, i.e. Dev fixed it in the Python read path rather than inheriting the long-standing "native-DLL-blocked" framing carried in STATE — directly honoring the proven-env-block / right-layer discipline; the PR went MERGEABLE with the full CI matrix green and auto-merge ON. **QA @03:45Z** re-confirmed the same #1150 mojibake against independent **Win32 `GetWindowTextW` ground truth**, did **not** duplicate the already-open issue (Rule 9), and added a value note that `app focus` "Did you mean" suggestions leak the identical mojibake — textbook anti-duplicate + harness-ground-truth discipline. No recurring operating weakness surfaced; the freshest rule (dev-cycle.md HEAD-check, landed 18:52Z <1d ago) has had only clean exercises → over-fitting forbidden (Step 3.6). Honest no-change row; no prompt churn. |
| 2026-06-22 (04:22Z) | — | **No change — no new evidence.** The work cycle that *completed* since the 03:52Z review — **QA verified+closed #1150** — was **exemplary, not a weakness**: it took independent OS ground truth (`user32.GetWindowTextW`, NOT naturo stdout) across **41/41 non-ASCII titles → 0 mismatch, 0 U+FFFD**, confirmed the prior-cycle-flagged `app focus` "Did you mean" leak is now correct CJK, ran the 11 hermetic decode tests green, **cited merged commit `5226d9f` per Rule 1**, left human-only #972 needs:ace-queued, and used **zero intrusive input** (file-redirected, `rc=$?`, no pipe-lie). The Dev #1150/#1157 ship was already assessed (03:52Z row). A Dev cycle @04:07 local **errored on a `naturo-loop-state.log` file-lock** (orch/dev write race) — a harness-infra transient already tracked by **#935/#1156**, NOT a Dev agent-behavior weakness, so nothing to encode. The freshest rule (dev-cycle.md HEAD-check, landed 18:52Z <1d ago) has had only clean exercises → attaching a rule on top of an exemplary signal is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→03:52Z streak, not oscillating). | — | — |
| 2026-06-22 (04:52Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 04:22Z review were **exemplary, not weaknesses.** **Dev @04:37Z** opened **PR #1158** (*fixes #1156*) attacking the tech-debt it had itself filed in the #1133 cycle — hardening `_bulk_get_process_info` electron decode against non-UTF-8 subprocess stdout AND making `test_app_ids`/`test_electron` hermetic on a real Windows desktop (family of #1100/#1133) — i.e. a genuine hardest-first fix at the right layer, not 避实就虚; PR went MERGEABLE with auto-merge SQUASH ON and every completed check green (Ubuntu/macOS 3.9–3.13, Windows+DLL, Lint, Build C++, Version, Author), only the still-running macOS/c-cpp checks leaving it BLOCKED → landing itself. **QA @04:40Z** ran a clean read-only exploratory sweep with **zero intrusive input on the live machine** (no SendInput): --help/--version, doctor, list screens/apps/windows JSON validity, error-path envelopes (WINDOW_NOT_FOUND rc1), arg-validation matrix, plus real-machine capture ground-truth (full-screen 1920×1080 PNG signature + region boundary clamp/reject matrix) — filed nothing false, left human-only #972 needs:ace-queued. No recurring operating weakness surfaced; the freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of two exemplary cycles is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→04:22Z streak, not oscillating). | — | — |
| 2026-06-22 (05:22Z) | — | **No change — no new evidence.** The one work cycle that *completed* since the 04:52Z review — **QA @05:08Z verified+closed #1156** (the just-merged PR #1158 fix) — was **exemplary, not a weakness**: it ran the exact runtime check the offline runner cannot (live `_bulk_get_process_info` enumerated **669 processes with no crash** = the precise path that raised `AttributeError` pre-fix, plus a forced-`stdout=None` mock → empty dict, no crash), confirmed the 6 previously-red-on-Windows repro tests + 11 electron decode tests now pass deterministically, **cited merged commit `959411d` per Rule 1**, used **zero intrusive input** (subprocess-decode + pytest only), left human-only #972 needs:ace-queued, and **correctly traced a one-off pytest tmp-dir `PermissionError` to a harness artifact (Step 2.4) and did NOT file it** — directly applying the 20:22Z QA-harness rule. The Dev #1156/#1158 ship was already assessed (04:52Z row). The current Dev cycle (@05:07Z, #1100 test_verify hermeticity — same #1100/#1133 family) is **still in-flight** → not yet assessable. No recurring operating weakness surfaced; the freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of an exemplary signal is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→04:52Z streak, not oscillating). | — | — |
| 2026-06-22 (05:52Z) | — | **No change — no new evidence.** The one work cycle that *completed* since the 05:22Z review — **QA @05:40Z (cycle END 05:42)** — was **exemplary, not a weakness**: with no QA-verifiable queue (only human-only #972, correctly left parked) it ran an exploratory sweep and filed a genuine, fully-root-caused **P2 #1159** — window titles with chars outside the system ANSI codepage (emoji U+2733 ✳, cross-script) silently corrupted to `?` (0x3F) across `list windows`/`app list`/`app windows`/`app find` — taking **independent OS ground truth** (`GetWindowTextW` = ✳) and reading the corruption from **raw on-disk JSON bytes** (0x3F), correctly **distinguishing it from #1150** as a *new* class (lossy `GetWindowTextA`/`WideCharToMultiByte` at the native layer, which the #1150/#1157 Python codepage-decode band-aid cannot recover — true fix is native `GetWindowTextW`+`CP_UTF8`, gated on the already-queued native-build #1097), ruled out duplicates, filed zero false `needs:ace`, and used **zero intrusive input**. The Dev cycle (@05:37) is **still in-flight** (no `cycle END` since 04:54; #1100 unchanged at `status:in-progress`; no new team-Dev PR; develop HEAD still `46fccf5`) → no completed Dev signal to assess this window. The freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of a single exemplary signal is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→05:22Z streak, not oscillating). | — | — |
| 2026-06-22 (06:22Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 05:52Z review were **exemplary, not weaknesses.** **QA @06:12Z** had no QA-verifiable queue (#1100 had only just merged; #972 human-only, correctly left parked) so it ran a focused exploratory sweep of the freshly-armed input-content guard and filed a genuine, fully-root-caused **P2 #1160** — bare `naturo type --paste` (clipboard-only, `text=None`) issues raw `Ctrl+V` **without** consulting the guard (guard gated on `if text is not None` at `_type.py:143`, bare-paste branch 330-332 bypasses it) — the same focus-race-to-terminal threat the guard exists to stop; demonstrated with a **mocked backend** (armed guard + clipboard `$(rm -rf /)` → exit 0 / Ctrl+V issued, vs the same content via the TEXT arg → blocked exit 1), **ZERO live keystroke injection** on the real machine, confirmed MCP path unaffected, and proposed the precise fix (validate `clipboard_get()` via `unsafe_input_reason` before the bare-paste Ctrl+V). Textbook anti-false / non-intrusive discipline; a true new code path (lateral to #960/#972), not a re-file. **Dev @06:20Z** finished the highest *actionable* in-progress item (#1100 test_verify NonWindows hermeticity, #1069/#1074/#1133 family) after **re-proving the native-moat block THIS cycle** (`cmake/cl/nmake/msbuild/dotnet/gcc/clang` all absent; `java/javac` present but #932 JAB still routes through the un-buildable `naturo_core.dll` → #920/#932/#1096 stay needs:ace #1097; OCR #1060→#1077) — i.e. correctly env-bounded hardest-first, NOT 避实就虚 — with a surgical test-only fix (wrap both non-Windows asserts in `patch("platform.system", return_value="Linux")`, +11/-4 one file), an **auditable stash-and-rerun gate** (the 2 fails + tmp-dir errors proven pre-existing on clean develop), the mechanical public-API check (test-only → `--auto` correct), `status:done` set, branch auto-deleted (Rule 14); PR #1161 merged all-green `d79647f`. No recurring operating weakness surfaced; the freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of two exemplary cycles is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→05:52Z streak, not oscillating). | — | — |
| 2026-06-22 (06:52Z) | — | **No change — no new evidence.** The one work cycle that *completed* since the 06:22Z review — **QA @06:38Z verified+closed #1100** (the just-merged PR #1161 test_verify NonWindows hermeticity fix) — was **exemplary, not a weakness**: it ran the runtime check the offline runner cannot, executing both previously-Windows-red tests on the **real `win32`/py3.12.10 desktop** (both PASS; full `test_verify.py` 81 passed), **cited merged commit `d79647f` per Rule 1**, used **zero intrusive input**, and correctly left the human-only #972 needs:ace-queued (no duplicate noise). The Dev cycle (started 06:37) is **still in-flight** (no `cycle END`; no new team-Dev PR; no new remote branch) → no completed Dev signal to assess this window. The freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of a single exemplary signal is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→06:22Z streak, not oscillating). | — | — |
| 2026-06-22 (07:22Z) | — | **No change — no new evidence.** Both work cycles that *completed* since the 06:52Z review were **exemplary, not weaknesses.** **QA @07:12Z** had no QA-verifiable queue (only human-only #972, correctly left parked) so it ran a non-intrusive exploratory sweep (input guard armed, **zero SendInput**) and filed a genuine, fully-root-caused **P2 #1162** — `UNKNOWN_OPTION`/`UNKNOWN_COMMAND` under `-j` emit `category:"unknown"` while their sibling `INVALID_INPUT` emits `"validation"` (a *usage-path* lateral miss of #1101, which only covered enum members — these are bare string literals in `_emit_usage_error_json`), with repro/actual/expected/root-cause + suggested fix; critically it **investigated the `list apps` 39-CalculatorApp anomaly and RULED IT OUT as a naturo bug** (OS ground-truth = 39 real leaked Calculator PIDs from prior cycles = machine clutter) rather than filing a false defect — textbook anti-false discipline. **Dev @07:20Z** shipped **PR #1163** (*fixes #1146*): after **re-proving the native-moat block THIS cycle** (`cmake/cl/nmake/msbuild/dotnet/gcc/clang` all absent → #920/#932/#1096 stay needs:ace #1097; OCR #1060→#1077; ship-gate/#766 human/umbrella) it correctly fell to the highest *actionable* in-progress item, **adopted a near-complete prior-cycle uncommitted #1146 fix and finished it** (the safe #935 adopt-and-verify pattern, not a blind `reset --hard` that would discard finished work), corrected **every** saved-selector help example to `@app/name` (catching the 2 the prior cycle missed — the `--selector` option-help string + the `_detect_query_strategy` docstring), added a regression test that walks the command docstring **AND every option help string** (closing the option-help class the original test missed), verified the premise at HEAD (the 18:52Z HEAD-check rule, respected), ran an auditable gate, and confirmed pure docs/help-text → `--auto` correct. No recurring operating weakness surfaced; the freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of two exemplary cycles is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→06:52Z streak, not oscillating). | — | — |
| 2026-06-22 (07:52Z) | — | **No change — no new evidence.** The one work cycle that *completed* since the 07:22Z review — **QA @07:39Z verified+closed #1146** (the just-merged PR #1163 find --help saved-selector fix) — was **exemplary, not a weakness**: a clean-path verification (no pipe, JSON→file, strict UTF-8) that confirmed the merged commit `aa0f983` per Rule 1, checked `find --help` uniformly shows `@app/name` (auto-detect desc, example, `--selector` option help) with the bare `@save-btn`/`@name` shorthand gone, confirmed the behavioral expectation (`find @notepad/save-btn --json` parses → `SELECTOR_REF_ERROR` rc=1 = the issue's Expected), **lateral-grepped residual `@name` mentions and correctly ruled them non-user-facing** (internal comments / generic docstrings / design-doc regex note, absent from the `--help` surface) rather than re-opening, used **zero intrusive input** (read-only CLI + grep, input guard armed), and left the human-only #972 needs:ace-queued. The Dev cycle (started 07:37) is **still in-flight** (no `cycle END`; only #766 Ace-umbrella `status:in-progress`; no new team-Dev PR; no new remote branch) → no completed Dev signal to assess this window. The freshest rule (dev-cycle.md HEAD-check, landed 18:52Z 06-21 <1d ago) has had only clean exercises → attaching a rule on top of a single exemplary signal is the over-fit Step 3.6 forbids. Honest no-change row; no prompt churn (consistent with the 03:22Z→07:22Z streak, not oscillating). | — | — |
| 2026-06-28 (14:22Z) | — | **No change — no new evidence (and a root-cause correction).** No Dev or QA cycle has run since 2026-06-22 ~09:56 local: the loop-state log shows **Ace globally PAUSED all roles on 06-22 (`ba7d6ac`) and only RESUMED today (`d16a91e`)**, so the ~6-day "freeze" the 06-26/27/28 daily-reviews attributed to #1168 (session-only crons) was in fact an **explicit human pause**, not new Dev/QA behavior — there is literally no team work cycle in the window to assess. The last assessed cycles (06-22 Dev #1164/#1162, QA capture-correctness sweep) were already logged exemplary (hardest-first + proved native block + TDD + full suite + public-API guardrail; QA non-intrusive, filed nothing false). Encoding a rule with zero new evidence is the churn/over-fit Step 3.6 explicitly forbids. Honest no-change row; the next real evidence will come from the first post-resume Dev/QA cycles. | — | — |
| 2026-06-28 (14:52Z) | — | **No change — no new evidence.** Exactly one work cycle has *completed* since the 14:22Z review — the **first post-resume QA cycle (~14:37Z)** — and it was **exemplary, not a weakness**: it verified+closed **#1164** (`wait <huge|inf|nan>` → structured `INVALID_INPUT/category:validation`, exit 1, no traceback under `-j`) and **#1162** (`UNKNOWN_OPTION/UNKNOWN_COMMAND -j` now `category:"validation"`), in each case **citing the merged commit** (`4721b67`/`3fb7b5d`) per Rule 1, using **strict UTF-8 output-to-file (no pipe lie)**, running pure stdout JSON-contract checks with **zero intrusive input** (no type/click/press), adding regression spot-checks (`wait -3` lower-bound, `wait 0.3` normal), and correctly leaving human-only #972 in the needs:ace queue. No Dev cycle has completed in this window (no new team-Dev PR / remote branch). One exemplary signal is not evidence of a recurring operating weakness, and the freshest operating rules are all <a few days old with only clean exercises → attaching a rule here is the over-fit Step 3.6 forbids. Honest no-change row (consistent with the post-resume streak, not oscillating). | — | — |
| 2026-06-28 (15:22Z) | — | **No change — no new evidence.** Exactly one work cycle has *completed* since the 14:52Z review — the **QA cycle @15:13Z** — and it was **exemplary, not a weakness**: with no QA-verifiable queue (only human-only #972, correctly left parked) it ran a **read-only** exploratory sweep of the find engine on the real desktop (live Notepad hwnd, CJK title), confirmed the error paths + `--all`/role/`app://` selectors resolve correctly, and filed a genuine, sharply-scoped **P2 #1169** (find short-form `//Role`: role-only never matches though `role:Button`/`app://app/Button` do, and the documented "any app" desktop-wide search doesn't search without a scope flag) — citing the **#615 selector contract** as the regression baseline, explicitly **ruling out the harness**, and using **zero intrusive input**. The **Dev cycle is in-flight** (#1060 `find --ocr` → `status:in-progress` 15:14Z, no `cycle END`, no PR) → no completed Dev signal to assess. One exemplary signal is not evidence of a recurring operating weakness, and the freshest operating rules are all only cleanly exercised → attaching a rule here is the over-fit Step 3.6 explicitly forbids. Honest no-change row (consistent with the post-resume streak, not oscillating). | — | — |
| 2026-06-28 (15:52Z) | — | **No change — no new evidence.** Two work cycles *completed* since the 15:22Z review, both **exemplary, not weaknesses**: **(1) Dev** opened **PR #1170** (`find --ocr`, *fixes #1060*) full-matrix green and — crucially — **deliberately held auto-merge OFF and documented in the PR body** that it adds new public CLI/API surface (`--ocr` flag, `OCR_NOT_AVAILABLE`/`OCR_FAILED` codes, `naturo.ocr_match` module, `naturo[ocr]` extra), self-gating it as a human-only public-API sign-off. This is precisely the public-API guardrail (the rule that closed the #1136/#1105 *landed-unattended* process gap) working **as intended** — Dev caught it **before** merge this time. It also shipped 20 hermetic tests with an injectable engine (coverage without the optional dep), ran the full non-desktop suite (6772 passed), and proved `--collect-only` survives `rapidocr`/`numpy` absent (no cross-platform collection break). **(2) QA** (@15:37Z) ran a read-only exploratory cycle on find/click `--image --threshold`, **evidenced open P2 #1154 as already-resolved** (range validation landed `19c6852` 06-20, before the 06-21 issue), posted an evidence-based close-or-fold-into-#897 comment, did **not** relabel/close (no merged commit ref; not in its queue), ruled out tooling/harness first, zero intrusive input. Attaching a rule on top of two exemplary cycles is the over-fit Step 3.6 forbids; the public-API self-gate is the existing rule succeeding, not a new gap. Honest no-change row (post-resume streak holds, not oscillating). | — | — |
| 2026-06-29 (00:22Z) | — | **No change — no new evidence (two exemplary cycles; over-fit forbidden).** Two work cycles completed since the 15:52Z review, both the existing rules *succeeding*, not new operating weaknesses: **(1) Dev @16:04Z** shipped the #1169 fix as **PR #1171** and *correctly held auto-merge OFF* because it changes a public CLI default (`find --selector` backend `uia`→`auto`) — the public-API self-gate firing exactly as written; ran the full `tests/` suite auditably (6116 passed) with the pre-existing/env failures itemized; added a hermetic 4-case wiring test; and honestly deferred the HARDEST item #1096 with a logged reason. **(2) QA @16:10Z** root-caused + filed **#1172** (saved-selector "not found" leaks `KeyError` repr quotes across find/click/type) read-only, ruled out the harness (ground-truthed `str(KeyError(x))=="'x'"`), zero intrusive input, filed nothing false. No recurring weakness to encode. **Test-quality spot-check (new Step-3.6 dimension):** sampled Dev's new `test_find_selector_backend_depth_1169.py` — it asserts the forwarded `backend`/`depth` kwargs + the `auto` default, which IS the fix's behavior (not shape-only, not tautological, not over-mocked); acceptable, no tech-debt to file. **Distillation check:** the `dev-cycle.md` Step-3 self-review is at **6 bolded principles** (parity / error-attribution / error-code-registration / option-coverage / platform-validation-order / test-hermeticity) — under the ~8 threshold and each still catching a distinct failure class, so distillation is **not yet due** (watch: when it next crosses ~8 or two principles collapse into one failure mode, distill rather than add). **One thin spot handled operationally, not as a prompt edit (avoiding over-fit to a single instance):** Dev cited #1096's MSVC block as "cl not on PATH" without activating the provisioned `vcvars` env or running the build — addressed via an on-issue [Orc] nudge to PROVE the block next cycle (build + live-verify attempt, pasted output), per the existing HARDEST-FIRST "prove, don't assume" rule, rather than adding a new rule. | Honest no-change row avoids prompt-churn; #1096 reclassified honestly in GOAL.md + surfaced (not yet a hard gate); distillation flagged as approaching but not forced. |
| 2026-06-29 (00:57Z) | — | **No change — no new evidence; the freshest evidence is the existing rules SUCCEEDING on the hardest item in the milestone.** One work cycle completed since the 00:22Z review — **Dev @00:46Z merged PR #1174 (`fixes #1096`, the P0 recognition-moat JAB blocker)** — and it is the cleanest possible vindication of the operating doc, not a weakness: it directly answered the prior cycle's on-issue [Orc] nudge to "**PROVE the block, don't assert it**" (the HARDEST-FIRST "an env/toolchain block must be PROVEN, not assumed" rule) — Dev **built the native core locally** (MSVC 14.44 + CMake), surmounting the very `vcvars`-gated MSVC block two prior headless cycles deferred on; **reproduced the bug first** with the pre-fix DLL (red-first), then **live-verified on a provisioned desktop** (OpenJDK 21 + Access Bridge: UIA 6 → cascade 46, delta=40, `extra_sources={'jab':40}`); ran the **Step 3.5 fresh-context adversarial verifier** (PASS, incl. byte-identical-DLL check + adversarial bounded-cost perf: call1 5.05s one-time handshake, call2/3 0.156s, no per-query hang); republished the honest `docs/RECOGNITION.md` `+40` row and re-pinned `test_recognition_doc_982.py` never-lie *both directions*. **Test-quality audit (Step-3.6 dimension, Ace 2026-06-29):** (a) the tests #1174 added/changed are sound — the new `test_jab_attaches_to_live_jvm` runs the **real cascade unmocked** and **skips when the Swing fixture is absent** (hermetic on the right axis, not over-mocked/tautological), and the doc-pin test guards the number in both directions; (b) Dev **self-caught two latent BAD tests** the full-suite run surfaced and **filed #1175** with textbook discipline (reproduced on clean `develop` HEAD, both scoped, fix options given, correctly **not** fixed out-of-scope) — `test_cost_guardrails.py:14` `open()` w/o `encoding=` (non-hermetic: cp936 desktop `UnicodeDecodeError`, green on CI UTF-8) + `test_agent.py:400` `assert total_time > 0` (clock-resolution flake). This is the test-hermeticity discipline self-correcting, the same class as the 13:52Z preventive `PYTHONUTF8` rule but at the *test-source* layer — already captured in the backlog, so **no prompt edit needed** (encoding a rule on a self-caught instance would over-fit, which Step 3.6 forbids); I triaged #1175 P2/v0.3.4 (Step 3 hygiene). **Distillation check:** `dev-cycle.md` Step-3 self-review still at **6 bolded principles** (< ~8 threshold), each catching a distinct failure class → not due. Honest no-change row to avoid churn — the right move when the hardest milestone item just landed via the rules working. | — | #1096 reclassified RESOLVED in GOAL.md (criterion #1 now code-complete); #1175 triaged P2/v0.3.4; pre-registered "attended-verify-or-caveat" escalation retired (Dev proved it live). |
| 2026-06-29 (01:22Z) | QA | **Stale untracked DLL → silent false verdict on native fixes (real, structural — not over-fit).** QA's @01:20Z #1096 verify hit it: `naturo_core.dll` is **not git-tracked**, so QA's Step-0 `git reset --hard origin/develop` left the **OLD pre-fix DLL** (122880 B) in the worktree — and Step 0 only copies the DLL **when absent**, so a stale binary is never refreshed. Verifying a native/recognition fix (the entire #1096 JAB case, and every future `core/`-touching moat fix #932/#933/#934) against a stale DLL is a **false PASS/FAIL** — the DLL *is* the thing under test. QA handled it correctly this once by **downloading the canonical CI-built `naturo-core-dll` from the merged run** (md5 7036e02…) before measuring + ran a discriminating negative control (pre-fix DLL → delta 0, fix DLL → delta 40) — but that relied on this agent's diligence; the operating doc did not encode it, so the next QA verifying a DLL fix could trust the stale binary. Same "false verdict from a stale install" class as the #969 editable-install sibling already in Step 2.4 (a *different* mechanism: untracked binary vs egg-link path → not redundant). Evidence: loop-state `## QA 2026-06-29T01:20Z` lines 7117–7119. | `qa-cycle.md` Step 2.4: added the **stale-`naturo_core.dll`** trap to the harness-lies enumeration (beside #969) + directive — for native (`core/`/DLL) fixes, **deploy the canonical CI-built DLL from the merged run** (not the worktree's untracked binary or Dev's local build) before trusting a recognition result. One surgical change, right home, no checklist bloat (Step-2.4 list 3→4 lies; not a numbered self-review). | QA can no longer silently verify a native fix against a stale DLL → no false recognition-moat verdicts; the #1096-class diligence is now doc-guaranteed, not agent-dependent. |
| 2026-06-29 (01:52Z) | — | **No change — no new evidence; two completed cycles, both the existing rules SUCCEEDING.** Since the 01:22Z review, the loop self-serviced one v0.3.2 quality bug end-to-end: **(1) Dev @01:33Z merged PR #1176 (`fixes #1173`, `find --limit` non-positive silently returns `count:0`)** — and it is the operating doc working, not a weakness: a **hardest-first** pick (Dev articulated why the easy additive v0.3.2 wins were unavailable — #932 code-complete, #1060/#1170 human-gated, #763 needs interactive login — and took the no-silent-failure/validation-order class instead of an easy win, resisting 避实就虚); **centralised the check up front** (platform-invariant, before the GUI gate, reusing the registered INVALID_INPUT envelope); ran the **Step-3.5 adversarial verifier (PASS** — reproduced the pre-fix silent `success:true/count:0`, proved INVALID_INPUT post-fix across tree/selector/image/ai + platform-invariance); and **correctly did NOT trip the public-API guardrail** (tightening validation on an existing working flag = bug fix, not new surface — sound discernment, the same gate that *did* fire on #1170/#1171). **(2) QA @01:40Z verified+closed #1173** (cited merged `54e4ba6` per Rule 1; evidence table; inclusive boundary `--limit 1` accepted = not over-rejected) and **lateral-audited the whole find numeric-param family** (depth/limit/threshold all reject non-positive + accept boundaries) → **filed nothing false**, zero intrusive input. **Test-quality audit (Step-3.6 dimension):** #1173's new 10-case test asserts the **INVALID_INPUT category + `recoverable` + the inclusive `--limit 1` boundary** — that is the fix's BEHAVIOR, not envelope shape; not over-mocked (a CLI validation test where the envelope *is* the observable behavior), not tautological, and it pins the boundary so it fails for the right reason. Sound — no tech-debt to file. **One thin spot, handled by NOT encoding it (avoiding over-fit):** Dev's log notes "one early stray write to the main checkout was removed" — a single, self-caught, reverted Rule 4/Rule 10 worktree-boundary slip with no lasting effect and no pattern across cycles; encoding a rule on a self-caught one-off is exactly the over-fit Step 3.6 forbids, and Rules 4/10 already cover it. **Distillation check:** `dev-cycle.md` Step-3 self-review still at **6 bolded principles** (< ~8 threshold), each a distinct failure class → not due. Honest no-change row to avoid prompt-churn — the right move when two cycles just demonstrated the rules holding (consistent with the 00:22Z/00:57Z streak, not oscillating). | — | — |
| 2026-06-29 (02:22Z) | — | **No change — no new evidence; two completed cycles, both the existing rules SUCCEEDING.** Since the 01:52Z review the loop self-serviced one recognition-fidelity bug end-to-end with NO human input: **(1) Dev @02:23Z merged PR #1177 (`fixes #886`, `keyboardShortcut` 100% null because the UIA C++ backend never queried `UIA_AcceleratorKey/AccessKeyPropertyId`)** — operating doc working, not a weakness: a **hardest-first** pick that *articulated why* the v0.3.2 Dev-actionable+hermetic queue was exhausted (#932/#1096 code-complete+verified, #1060/#1169 human-gated PRs, #863 env, #763 interactive-login) and fell to the NEXT milestone's HARDEST item — a SOUL "Never Lie"/silent-output-contract gap (a field null across 87k records), **not** an easy additive win (resisting 避实就虚); **re-proved the native-build "block" is gone** (rebuilt `naturo_core.dll` via MSVC 14.44 + CMake — the stale-DLL-trap rule from 01:22Z exercised cleanly); ran the **Step-3.5 adversarial verifier with a discriminating negative control** (built the fix DLL itself; PRE-fix → all 4 fixture buttons null = bug reproduced; POST-fix → `AcceleratorKey "Ctrl+S"`, AccessKey-only `"O"`, both-set `"Ctrl+P"` accelerator-wins — desktop test FAILS on the pre-fix DLL, so not tautological); the verifier **FOUND a sibling leak** (the MCP `_snapshot.py` uiMap builder dropped the field the CLI path now carries) → **fixed in the same sweep** (recurring-class closure across UIA see/find + MCP, not a point fix); and **correctly did NOT trip the public-API guardrail** (populating an ALREADY-documented output field by honoring the existing contract = bug fix, no new flag/param/symbol/error-code — the same gate that *did* fire and hold on #1170/#1171). **(2) QA @02:15Z verified #972** (the P0 input-safety guard — function-level matcher checks with **zero live keystrokes**: dual-signal activation, blocks `;|&&||$()\`><`/`rm`/`del`/`format`/`sudo`, word-boundary warmup/deleted pass; left human-only #972 for Ace, did NOT close) and ran a **live recognition** pass (Notepad `find --all` → 44 elements, Chinese UI rendered with no mojibake); **caught a harness-lie** (`--selector '//Button'` via Bash returned SELECTOR_NOT_FOUND because Git Bash/MSYS2 mangles leading `//`→`/`; re-run with `MSYS2_ARG_CONV_EXCL='*'` matched — naturo correct) and **filed nothing false**, zero intrusive input, cleaned up its own procs. **Test-quality audit (Step-3.6 dimension):** #886's new `test_keyboard_shortcut_886.py` desktop test uses an **owned WPF fixture that deterministically sets the UIA Accelerator/Access keys** and FAILS on the pre-fix DLL — that is the fix's BEHAVIOR (not envelope shape), discriminating (negative control), not tautological; the companion hermetic MCP test mocks at the **snapshot-builder boundary** (the carrier under test), not the field itself, so it's cross-platform-green without neutralizing the assertion. Sound on both axes — no tech-debt to file. **No thin spot to encode** (the one harness-lie QA hit is exactly the `msys-double-slash` class already in QA memory; over-fit forbidden). **Distillation check:** `dev-cycle.md` Step-3 self-review still at **6 bolded principles** (< ~8 threshold), each a distinct failure class → not due. Honest no-change row (consistent with the 00:22Z/00:57Z/01:52Z streak — two cycles just demonstrated the rules holding; churning prompts for its own sake is forbidden). | — | — |
