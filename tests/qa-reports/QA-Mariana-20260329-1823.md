# QA Report — Round 66
**QA-Mariana** | 2026-03-29 18:23 UTC | Commit: fda8530

## Summary
- CI Desktop Tests: skipped (no new commits since fda8530)
- Issues verified: #612 (PASS — type/press --app focus fix), #168 (SKIP — PR #607 not merged)
- E2E tests: Calculator (PASS — 7x8=56 verified via screenshot), Notepad (PASS — type+press+capture flow)
- Regression: 7/8 passed, 1 failed (TC-0012), 2 retired (TC-0020, TC-0021)
- New test cases created: TC-0030 (backslash escape in type)
- Test cases retired: TC-0020 (5 passes, #533 closed), TC-0021 (5 passes, #563 closed)
- New issues created: #619 (type backslash escape), #620 (app quit false failure)
- Total active test cases: 18
- Tests run: 8

## Phase Details

### Phase 1 — Verification
- **#612** (type/press --app raw SetForegroundWindow): VERIFIED. Typed "Hello from QA" in Notepad via `type --app notepad`, pressed Ctrl+A via `press --app notepad`. Both operations properly focused the target window. Screenshot confirmed.
- **#168** (Unified clipboard command): SKIPPED. PR #607 still open/unmerged. `naturo clipboard` command not available on HEAD.

### Phase 2 — E2E Testing
- **Calculator**: Launched, inspected elements, clicked 7->multiply->8->equals. Display showed 56. JSON output valid. Note: stale element refs from previous `see` call produce silent click misses (expected behavior).
- **Notepad**: Launched via `start notepad.exe`, typed "Enterprise RPA test 2026", pressed Ctrl+A+Delete. All operations confirmed via screenshots.

### Phase 3 — Exploratory Testing
- Empty string: proper error "TEXT argument is required", exit 1
- Nonexistent app: proper error "App not found", exit 1
- Negative depth: proper error "--depth must be between 1 and 50", exit 1
- JSON error output: valid JSON with error code, message, suggested_action
- Exit codes: 0 for success, 1 for errors, 2 for usage errors
- Flag combinations (--app + --depth + -j, --app + --on + -j): all working

### Phase 4 — Enterprise RPA Dev Simulation
10-step Notepad workflow: launch -> inspect -> click -> type header -> enter -> type body -> enter -> type data -> Ctrl+Shift+S -> cancel.

**Bug found**: `naturo type` interprets `\r`, `\n`, `\t` as escape sequences but has no `\\` escape for literal backslash. Windows file paths like `C:\Users\test\report.txt` get corrupted. Filed as #619.

**Bug found**: `app quit` exits 1 with error about parent PID, but app IS actually closed. False failure breaks scripted workflows. Filed as #620.

### Phase 5 — Regression Results
| TC | Title | Result | Consecutive |
|----|-------|--------|-------------|
| TC-0003 | Chinese app name matching | PASS | 1 |
| TC-0004 | Calculator E2E | PASS | 20 |
| TC-0012 | PID targeting | FAIL | 0 |
| TC-0014 | Scripted Notepad workflow | PASS | 14 |
| TC-0015 | App quit unsaved changes | PASS | 6 |
| TC-0016 | UWP app name matching | PASS | 1 |
| TC-0020 | Click nonexistent app | PASS -> RETIRED | 5 |
| TC-0021 | Escape sequences | PASS -> RETIRED | 5 |

TC-0012 FAIL: `see --pid 61828` returned Settings instead of Calculator — both UWP apps share ApplicationFrameHost.exe PID.

### Phase 6 — Test Case Evolution
- **Created**: TC-0030 (type backslash escape missing, source: #619)
- **Retired**: TC-0020 (5 passes, #533 closed), TC-0021 (5 passes, #563 closed)

## Simulated User
**Enterprise RPA Dev** (Hour 18, index 2): Automating legacy Win32/WPF apps. 10-step workflow survived steps 1-10 without failure. Key friction: backslash interpretation in type command makes file path automation unreliable.

## Top 3 Risks
1. **v0.3.1 has 4 open P0 bugs** (#608, #609, #611, #613) — coordinates wrong on 4K, AI Vision broken, click not foregrounding
2. **type command corrupts Windows paths** (#619) — enterprise users typing file paths will hit this immediately
3. **app quit false failures** (#620) — any scripted workflow using exit codes will break on successful app closes
