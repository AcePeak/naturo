# QA Report — Round 77
**QA-Mariana** | 2026-04-01 09:12 | Persona: First-time User

## CI Desktop Tests
- Desktop/integration: 1 failed, 1 passed (UIA detection — `test_detect_notepad_has_uia`)
- E2E: 2 failed, 8 passed, 3 xfailed
- Failures are known issues (#729, #749 — UWP Notepad on Chinese Windows)
- Commit: cf607cb

## Phase 1 — Dev Fix Verification
- **#729** (CI desktop tests fail): Partial progress. `--app notepad` now works (48 elements). `--app a1` still fails. Cannot verify.
- **#702** (AI vision dedup): Blocked on AI provider config — cannot exercise without API key.

## Phase 2 — E2E Testing
- **Calculator** (计算器): see ✅, click ✅, 7×8=56 verified via screenshot ✅, JSON valid ✅, app quit ✅
- **Notepad**: see ✅, click ✅, type "Hello from QA-Mariana round 77" ✅, press Ctrl+A/Delete ✅, app quit ✅
- **App ID filter** (`--app aN`): ❌ FAILS for all apps — filed #752

## Phase 3 — Exploratory Testing
- Empty string type: correct error ✅
- Special chars (`<>&"'` etc.): typed with extra `\` before `!` — minor escaping issue
- Error paths: `--app nonexistent`, `--id e99999`, `--depth -1` all return proper errors ✅
- JSON: see, list apps, list screens all produce valid JSON ✅
- Exit codes: success=0, failure=1 ✅
- `--app` + `--id` combination: works ✅

## Phase 4 — First-time User Simulation
- `naturo` bare command: shows helpful usage ✅
- `naturo see`: works on foreground window ✅
- `naturo see --help`: shows `--app` with examples ✅
- `naturo app launch notepad` → `naturo see --app notepad` → `naturo type "Hello World"`: intuitive flow ✅
- **Friction found**: `list apps` shows "ID" column (a1, a2) but `--app a1` fails. User must discover `--app-id` flag separately. Filed #752.

## Phase 5 — Regression Test Cases
- **9/11 passed**
- TC-0003 ✅, TC-0004 ✅, TC-0008 ✅, TC-0011 ✅, TC-0015 ✅, TC-0018 ✅, TC-0031 ✅, TC-0034 ✅, TC-0035 ✅
- TC-0036 ❌ (quit Chinese app — wrong PID targeting, related to #749)
- TC-0038 ❌ (UWP Notepad enumeration — known #749)

## Phase 6 — Test Case Evolution
- **Created**: TC-0039 (app ID filter fails with --app but works with --app-id) → #752
- **Total active test cases**: 23

## Phase 7 — Issues Filed
- **#752** (P1): `--app aN` (app ID from list apps) silently fails, must use `--app-id aN`

## Top 3 Risks
1. **UWP Notepad enumeration (#749)** — intermittently invisible on Chinese Windows, blocks multiple test cases and CI
2. **App ID filter discoverability (#752)** — first-time users will hit this immediately when following list apps → see workflow
3. **Zombie Notepad processes** — 11+ unkillable UWP processes accumulate, suggesting deeper process lifecycle management issues
