# QA Report — Round 91
**Agent**: QA-Mariana
**Date**: 2026-04-02 09:13 UTC
**Milestone**: v0.3.2
**Simulated User**: AI Agent Builder (persona 1)

## CI Desktop Tests
- Desktop: 1 failed (known #785 — Calculator UIA), 1 passed, 8 skipped (commit 168a98f)
- E2E: 9 passed, 3 xfailed, 1 xpassed

## Phase 1 — Dev Verification
- No `status:done` issues to verify this round.

## Phase 2 — E2E Desktop Testing
- **Notepad**: PASS — see/click/type/press/capture/quit all verified with screenshots. Typed "QA Round E2E Test - Phase 2 - 2026-04-02 09:00" confirmed in screenshot.
- **Calculator**: FAIL — `app launch calculator` reports PID 103936, CalculatorApp.exe runs (confirmed via tasklist), but completely invisible to `naturo list apps` and `naturo see --app calculator`. Known #785.
- JSON validation: `naturo see --app notepad -j` produces valid JSON.
- App cleanup: Previous round left 10 Notepad tabs open. Cleaned up before testing.

## Phase 3 — Exploratory Testing
- Empty string type: correct error "TEXT argument is required", exit 1
- Click nonexistent app: correct error, exit 1
- Type --id (invalid flag): correct usage error, exit 2
- See --depth -1: correct error "must be between 1 and 50", exit 1
- JSON error output: valid JSON with error code, exit 1
- Special characters `<>&"'\` typed correctly into Notepad (verified with screenshot)
- Exit codes: all correct (0 success, 1 error, 2 usage)

## Phase 4 — AI Agent Builder Simulation
Tested MCP server end-to-end as an AI agent would:
1. `naturo mcp tools` — 64 tools listed
2. `naturo mcp start` (stdio) — initialize handshake works, returns server info
3. `list_apps` tool call — returns valid JSON with app list
4. `capture_screen` — returns base64 PNG in JSON-RPC response
5. `click` with invalid coords (-100,-100) — returns success:true (bug, matches #787)
6. Nonexistent tool call — returns `isError: true` with clear message
7. Full workflow: `launch_app` → `see_ui_tree` → `type_text` — all functional, text confirmed in screenshot
8. MCP `type_text` response is minimal (`{"success": true}`) — no text echo or routing info unlike CLI

## Phase 5 — Regression Results
| TC | Title | Result |
|----|-------|--------|
| TC-0003 | Chinese app name matching | PASS (26) |
| TC-0010 | MCP agent workflow E2E | PASS |
| TC-0011 | App filter cross-process | FAIL (0) — #789 |
| TC-0023 | MCP launch missing PID | RETIRED (6, #575 closed) |
| TC-0035 | Press standalone modifier | PASS (18) |
| TC-0038 | UWP Notepad enumeration | PASS (14) |
| TC-0041 | JSON exit code on failure | PASS (6) |
| TC-0042r | Type -E newline dropped | not run |
| TC-0043 | Calculator UWP invisible | FAIL (0) — #785 |
| TC-0044 | Click coords out-of-bounds | FAIL (0) — #787 |
| TC-0042e | JSON stderr duplicate | PASS |

**Summary**: 7/10 passed, 3 failed, 1 retired

## Phase 6 — Test Case Evolution
- **Retired**: TC-0023 (MCP launch missing PID) — #575 closed, 6 consecutive passes
- **No new test cases** — all bugs found already tracked

## Phase 7 — Issues
No new issues filed. All bugs confirmed are already tracked:
- #785 (Calculator invisible)
- #787 (click out-of-bounds)
- #789 (cross-process title matching)

**[QA-Mariana]**
