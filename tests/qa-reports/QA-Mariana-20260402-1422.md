# QA Report — Round 96
**Agent**: QA-Mariana
**Date**: 2026-04-02 14:22
**Commit**: 16b0024 (develop)
**Persona**: Scripter/Automator (index 6)

## CI Desktop Tests
- **Desktop + Integration**: 130 passed, 4 failed, 1 skipped, 2 xfailed, 2 xpassed (commit 16b0024)
- **E2E**: 9 passed, 2 xfailed, 2 xpassed
- **Known failures** (all tracked):
  - `test_detect_calculator_has_uia` — Calculator only reports vision, not UIA (#785)
  - `test_detect_calculator_best_method` — Same root cause (#785)
  - `test_capture_screen_unicode_path` — naturo_core.dll File I/O error (#777)
  - `test_capture_window_unicode_path` — Same root cause (#777)

## Phase 1 — Dev Verification
No `status:done` issues to verify this round.

## Phase 2 — E2E Desktop Testing (Notepad)
| Step | Result | Notes |
|------|--------|-------|
| `see --app notepad` | PASS | Full element tree returned, 43 elements |
| `click e3` (text editor) | PASS | Click registered at correct coords |
| `type` text | PASS | "QA-Mariana E2E Round 96 Test" appeared correctly, screenshot verified |
| JSON valid | PASS | `see -j \| python -m json.tool` succeeds |
| `click e14` (File menu) | **FAIL** | Click reported success but menu did not open (#786) |
| `app quit notepad` | PASS | Window closed |
| Calculator launch | **FAIL** | Launched but invisible to list/see (#785) |

## Phase 3 — Exploratory Testing
| Test | Result | Notes |
|------|--------|-------|
| Empty string type | PASS | Correct error, exit 1 |
| Nonexistent app click | PASS | Correct error, exit 1 |
| Invalid element ID | PASS | Clear error message, exit 1 |
| Special chars app name | PASS | Error, exit 1 |
| Negative depth | PASS | Clear validation error, exit 1 |
| JSON error output (see) | PASS | Clean JSON to stdout, no stderr |
| JSON error output (click) | **FAIL** | Duplicate human-readable error to stderr (#783) |
| JSON error output (type) | **FAIL** | Same stderr leak (#783) |
| JSON error output (press) | **FAIL** | Same stderr leak (#783) |
| `--app notepad` matching | **FAIL** | Matched Chrome with "notepad" in title (#789) |

## Phase 4 — Scripter/Automator Simulation
Wrote and executed a full 5-step automation script:
1. `naturo app launch notepad` — PASS
2. `naturo see --app notepad -j | python parse` — PASS (tree structure requires recursive traversal)
3. `naturo click $EDITOR_ID --app notepad` — PASS
4. `naturo type "Script automation test - piping works!"` — PASS, screenshot verified
5. `naturo capture --app notepad -o /tmp/verify.png` — PASS

**Scripter UX findings:**
- JSON `see` returns a tree, not a flat list — harder to script with (need recursive traversal)
- `click`/`type`/`press` emit duplicate stderr in JSON mode, breaking `2>&1 | jq` pipes (#783)
- `set -e` correctly catches JSON mode failures (exit 1)
- Piping JSON to python works reliably

## Phase 5 — Regression Test Cases
16 of 26 active test cases run this round:

| TC | Test | Result |
|----|------|--------|
| TC-0003 | Chinese app name matching | PASS |
| TC-0004 | Calculator basic flow | FAIL |
| TC-0011 | App filter cross-process | FAIL |
| TC-0014 | Scripted notepad workflow | PASS (29 consecutive) |
| TC-0015 | App quit silent failure | FAIL (regression from 11 passes) |
| TC-0031 | Notepad menu click targeting | FAIL |
| TC-0035 | Press standalone modifier | PASS (23 consecutive) |
| TC-0038 | UWP Notepad enumeration | PASS (16 consecutive) |
| TC-0040 | Capture unicode path DLL | FAIL |
| TC-0041 | JSON mode exit code zero | PASS (11 consecutive) |
| TC-0042r | type -E newline dropped | FAIL |
| TC-0042e | JSON stderr duplicate error | FAIL |
| TC-0043 | Calculator UWP invisible | FAIL |
| TC-0044 | Click coords out-of-bounds | FAIL |
| TC-0046 | see error exit code zero | PASS |
| TC-0047 | Press app focus mismatch | FAIL |

**Summary**: 6/16 passed, 10/16 failed, 0 retired

## Phase 6 — Test Case Evolution
- No new test cases created (all findings map to existing issues)
- No test cases eligible for retirement
- TC-0015 regressed (had 11 passes, now back to 0)

## Phase 7 — Issues
No new issues filed. All bugs found are already tracked:
- #781: Commented with 11-round re-test evidence showing fix appears in place
- #783, #784, #785, #786, #787, #789, #807: All confirmed still open

## Top 3 Risks
1. **UWP app detection unreliable (#785)**: Calculator and other UWP apps invisible after launch — affects any user testing UWP apps
2. **--app filter matches by title substring (#789)**: Any window with target app name in title gets matched — silent wrong-window targeting
3. **JSON mode stderr leak (#783)**: click/type/press emit plaintext errors to stderr in JSON mode, breaking script piping — directly affects the Scripter/Automator persona
