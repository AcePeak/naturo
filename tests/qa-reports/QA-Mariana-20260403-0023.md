# QA Round 103 Report
**Agent**: QA-Mariana
**Date**: 2026-04-03 00:23
**Milestone**: v0.3.2
**Persona**: First-time User (index 0)

## CI Desktop Tests
- **5 passed, 1 failed, 18 skipped, 1 xfailed, 1 xpassed** (desktop+integration, commit 2308b68)
- **9 passed, 3 xfailed, 1 xpassed** (E2E, commit 2308b68)
- Failed: `test_capture_screen_unicode_path` — known #777 (naturo_core.dll Unicode path)

## Phase 1 — Dev Verification
- No `status:done` issues to verify

## Phase 2 — E2E Testing
- **Notepad**: PASS — launch → see → click text area → type "QA Round 103 E2E Test" → verify (screenshot confirmed 21 chars) → Ctrl+A → Delete → verify empty → close
- **Calculator**: FAIL — `naturo app launch calculator` reports success but Calculator UWP never becomes visible. Known #785.
- JSON validation: `naturo see --app notepad -j | python -m json.tool` — valid

## Phase 3 — Exploratory Testing
- Error paths: `click --app nonexistent`, `type --id e99999`, `see --depth -1` all return non-zero exit codes with clear messages
- Special characters: `<>&"'test` typed correctly (screenshot verified)
- `get` command: Retrieved text value correctly from Notepad Document element
- JSON stderr duplicate (#783): Still present — click -j emits human-readable error to stderr alongside JSON stdout

## Phase 4 — First-time User Simulation
- `naturo` (no args): Shows clear help with tagline "See, click, type, automate." — good first impression
- `naturo see`: Works immediately, shows element tree with helpful tip
- `naturo app launch notepad` + `see --app notepad` + `click e3` + `type "text"`: Intuitive flow, works as expected
- **Friction point**: UWP Notepad restores previous session tabs — first-time user might be confused by leftover content after `app quit` + `app launch`
- **Observation**: `focused_pid` in routing output consistently doesn't match target `pid` — suggests keystrokes may route to wrong window (#807)

## Phase 5 — Regression Test Cases
| Test Case | Result | Consecutive |
|-----------|--------|-------------|
| TC-0041 JSON exit code | PASS | 17 |
| TC-0046 see exit code non-JSON | PASS | 8 |
| TC-0003 Chinese app name | PASS | +1 |
| TC-0038 UWP Notepad enum | PASS | +1 |
| TC-0040 Unicode path capture | PASS | +1 |
| TC-0035 Standalone modifier | PASS | +1 |
| TC-0043 Calculator UWP | FAIL | 0 |
| TC-0042 type -E newline | FAIL | 0 |
| TC-0044 Click out-of-bounds | FAIL | 0 |
| TC-0042exp JSON stderr dup | FAIL | 0 |

**Summary**: 6/10 passed, 0 retired. 14 additional test cases reviewed but not directly executed.

## Phase 6 — Test Case Evolution
- No new test cases created (all findings map to existing issues)
- No retirements (auto-retire conditions not met)

## Phase 7 — Issues Filed
- No new issues (all bugs found are already tracked: #785, #784, #787, #783, #807)

## Top 3 Risks
1. **Calculator UWP completely broken on this machine** — cannot launch or test Calculator at all. Blocks TC-0004 and TC-0043.
2. **type -E newline still dropped** (#784) — multiline text input broken, impacts scripting use cases.
3. **focused_pid mismatch persistent** (#807) — press/type routing may send keystrokes to wrong window when target is not focused.
