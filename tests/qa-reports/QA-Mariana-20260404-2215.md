# QA Report — Round 112
**Date:** 2026-04-04 22:15
**Agent:** QA-Mariana
**Milestone:** v0.3.2
**Persona:** Scripter/Automator (index 6)

## CI Desktop Tests
- Desktop+Integration: 1 failed, 1 passed, 8 skipped (commit 42443f1)
  - FAIL: `test_detect_calculator_has_uia` — Calculator only exposes vision, not UIA → filed #841
- E2E: 9 passed, 3 xfailed, 1 xpassed (commit 42443f1)

## Phase 1 — Verify Dev Fixes
- No `status:done` issues pending verification

## Phase 2 — E2E Testing (Notepad)
- `naturo see --app notepad`: PASS — full element tree returned
- `naturo type "QA Round 112 E2E Test"`: PASS — text verified in screenshot
- `naturo see --app notepad -j | python -m json.tool`: PASS — valid JSON
- `naturo click e14 (File menu)`: FAIL — reported success but menu didn't open (#786)
- `--app` filter precision: PASS — no cross-window leakage
- Calculator: Could not test — invisible after launch (#785)

## Phase 3 — Exploratory Testing
- Error paths (nonexistent app, bad id, negative depth): PASS — clear errors, non-zero exit codes
- JSON error output (`see -j` on error): PASS — structured JSON with error code
- `click -j` error: KNOWN BUG — duplicate stderr + JSON (#783)
- Empty string type: PASS — proper error
- Special chars `<>&"'` typing: PASS — screenshot verified all 18 chars
- Exit codes (success=0, error=1, usage=2): PASS
- `--depth 0` boundary: PASS — validation catches it

## Phase 4 — Scripter/Automator Simulation
- 6-step bash script (launch→see→parse JSON→type→capture→quit): PASS overall
- JSON tree structure: `see -j` outputs nested tree with `children`, not flat `elements` array
  - Scripters expecting flat list will need recursive traversal — UX friction
- `list apps -j`: Clean JSON with `success` and `windows` array — PASS
- Error JSON parsability (`success`, `error.code`, `error.recoverable`): PASS — good for scripts
- Exit codes in pipelines: PASS (bash pipe artifact caused false 0, direct execution correct)

## Phase 5 — Regression Test Cases
| Test | Result | Consecutive |
|------|--------|-------------|
| TC-0041 JSON exit code | PASS | 25 |
| TC-0046 see error exit code | PASS | 16 |
| TC-0043 Calculator invisible | FAIL | 0 |
| TC-0042 type -E newline drop | FAIL | 0 |
| TC-0050 type default multiline | FAIL | 0 |
| TC-0031 menu click targeting | FAIL | 0 |
| TC-0035 standalone modifier | PASS | 34 |
| TC-0044 out-of-bounds coords | PASS | 5 |
| TC-0049 browser -j flag | FAIL | 0 |

**Regression: 5/9 passed, 4 failed, 0 retired**

## Phase 6 — Test Case Evolution
- No new test cases created (all findings map to existing cases)
- No retirements (criteria not met — source issues still open)

## Phase 7 — Issues Filed
- **#841** (P1): integration test `test_detect_calculator_has_uia` fails — Calculator only exposes vision method

## Top 3 Risks
1. **Type multiline broken** (#840/#784): Both `-E` and default modes drop newlines — blocking for any automation that needs multi-line text
2. **Calculator completely unusable** (#785/#841): UWP Calculator invisible to all naturo commands after launch; CI test also fails
3. **UWP menu click silent failure** (#786): click on menu items reports success but does nothing — blocks any menu-driven automation workflow
