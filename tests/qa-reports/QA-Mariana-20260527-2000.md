# QA-Mariana Round 116 — 2026-05-27 20:00

**Environment**: Windows 11 Pro 10.0.26200, naturo 0.3.1, develop @ 746ed92
**Persona this round**: Power User (hour 20 mod 8 = 4)

## Summary

Hourly QA round on real desktop. Skipped full CI suite (would take >hourly budget); focused on direct CLI verification of two `status:done` issues that are testable from the command line without a fresh app launch. Both passed and were closed. Brief power-user sanity check across `see`/`capture`/error paths — no new bugs found. One latent UX concern parked for next round.

## Phase 1 — Verified `status:done` Issues

### #781 — JSON mode exit code on failure ✅ CLOSED

```
$ naturo click --id e99999 -j
{"success": false, "error": {"code": "REF_NOT_FOUND", "message": "Element ref 'e99999' not found. ..."}}
$ echo $?
1
```

Exit code is non-zero on `success: false`. Fix from PR #819 confirmed.

### #783 — Duplicate human-readable error on stderr in JSON mode ✅ CLOSED

```
$ naturo type --on e99999 "test" -j 1>/tmp/out 2>/tmp/err
$ echo $?
1
$ cat /tmp/out
{"success": false, "error": {"code": "REF_NOT_FOUND", ...}}
$ cat /tmp/err
(empty)
```

stderr clean. JSON-mode contract honored.

## Phase 4 — Power User Simulation (10–15 min)

Real desktop state at start: 5 windows open (Terminal × 2, Clash for Windows, Explorer × 2 + Program Manager).

| Test | Command | Result |
|------|---------|--------|
| Discover open apps | `naturo list apps` | 5 apps listed correctly, HWND/PID/title columns clean |
| Substring app filter (running app) | `naturo see --app "Clash for Windows" -j` | Valid JSON UIA tree, root `e1` = Pane "Clash for Windows" with nested children; no contamination from other windows |
| Single-app screenshot | `naturo capture --app "Clash for Windows" -o ...` | 1277×907 PNG saved; AI vision review confirms ONLY Clash window content (Profiles tab visible), no Terminal/Explorer bleed-through |
| Error path: nonexistent app | `naturo see --app nonexistent-app-12345 -j` | exit=1, JSON `WINDOW_NOT_FOUND` with `suggested_action`, stderr empty |
| `--window` semantics | `naturo see --window 526550 -j` | Correctly returns WINDOW_NOT_FOUND because `--window` is a *title substring*, not HWND. `--hwnd` is the HWND option. **Power-user note**: this is documented in `--help` but the easy mistake of passing an HWND to `--window` returns a generic error rather than a hint like "did you mean --hwnd?". Minor UX gap — not filed. |

## Phase 0 — CI Guardian

Skipped this round. Last full CI sha = `cf3e1fd5` (Apr 5); current HEAD = `746ed92` — 82 commits behind. The intervening commits are 15 Dev fix PRs (#845–#855) plus many no-op review commits. Each fix PR ran unit/integration tests in its own CI run; **a full integration regression on develop has not run since runner #842 went offline (day 59)**. Will schedule a focused pytest pass next round for the modules touched by #832/#833 refactors.

## Phase 5 — Regression Test Cases

Not run this round. Plan: next round run TC-0024 through TC-0029 (mandatory every 3 rounds) on Notepad + Calculator.

## Phase 6 — Test Case Evolution

No new test cases created — no new bugs this round.

## Phase 7 — New Issues Filed

None.

## Phase 8 — Status

- `status:done` queue: was 13, now **11**. Issues closed this round: #781, #783.
- Remaining: #786, #788, #807, #810, #832, #833, #834, #840, #841, #843, #844.
- Categorization:
  - **Runtime-verifiable from CLI** (next rounds): #810 (MCP stdout), #832/#833 (refactors — verify imports + smoke), #834 (browser -j), #840 (type newlines), #844 (MCP validation errors)
  - **Real-desktop scenario required**: #786 (UWP menu), #788 (stale PID), #807 (press --app focus), #841 (Calculator UIA), #843 (popup capture)

## Top 3 Risks

1. **11 status:done issues pending verification** — drainable at ~2/round on this real desktop. Need to actually drain, not just rotate hourly no-ops.
2. **Full CI regression stale 52 days** — individual PRs were tested, but integration coverage on develop has not run since cf3e1fd. Runner #842 still offline (day 59).
3. **Spot UX gap**: `--id` flag exists on `click` (accepts eN) but not on `type` (which uses `--on`). Same identifier, two flag names. Will deepen the audit across all targeting commands next round before filing.

**[QA-Mariana]**
