# QA Round 123 — Chinese User Persona

**Agent**: QA-Mariana
**Date**: 2026-05-28 03:21 UTC
**Persona**: Chinese User (hour % 8 = 3)
**Runner**: NATUROBOT (non-interactive session — NO_DESKTOP_SESSION, blocked by #863)

## Summary

Round 123 was constrained by #863 (no interactive desktop in QA session). Full Phase 1 verification of the 5 SendInput-dependent `status:done` issues remains blocked. Within those constraints:

- Phase 0 CI: 138 desktop/integration + 13 e2e tests **all SKIPPED** with "No interactive desktop session available" — same #863 root cause. Last-CI-sha advanced to `099c2bb`.
- Phase 1: Posted partial-verification comments on the 3 untouched `status:done` issues (#807, #788, #786). Discovered **3 broken Windows unit tests** introduced by PR #820 — filed as **#870** (P2, missing `skipif(sys.platform=='win32')` decorators).
- Phase 4 (Chinese User persona): Confirmed CLI parses Chinese/emoji args without crash. Discovered **window-targeting flag matrix inconsistency** while trying `naturo list windows --window 记事本` — filed as **#871** (P2).
- Phase 5 regression: 5 testable cases re-run; TC-0046 and TC-0041 PASS (+1 consecutive); TC-0052/#864, TC-0054/#866, TC-0055/#867, TC-0057/#869 still FAIL. No regressions, all known bugs persist.
- Phase 6: Created TC-0058 (window-targeting flag matrix, sources from #871).
- Phase 7: Filed **#870** and **#871**.

## Phase 0 — CI Guardian

- Last-CI-sha: `cf3e1fd` → `099c2bb` (advanced)
- Code files changed since last run: 30 (CLI, backends/windows, MCP, browser commands)
- `pytest -m "desktop or integration"`: **138 skipped, 0 failed, 0 passed** (all skip with "No interactive desktop session available")
- `pytest -m "e2e"`: **13 skipped, 0 failed, 0 passed** (same reason)
- Targeted unit tests for the 5 `status:done` PRs:
  - #807 / PR #845: `test_cli_press.py` — **66/66 PASS**
  - #788 / PR #820: `test_stale_pid_routing*.py + test_cli_interaction_common.py + test_resolve_hwnd_session.py` — **88 passed, 3 FAILED** (broken tests, filed #870)
  - #786 / PR #815: `test_winui_menu_click_786 + test_click_uwp_identity + test_click_app_foreground` — **16/16 PASS**

**CI Desktop Tests**: 0 passed, 0 failed, 151 skipped, **3 latent unit failures** (commit `099c2bb`). Filed #870.

## Phase 1 — `status:done` issue verification

| Issue | Title | Action |
|-------|-------|--------|
| #807  | press --app hotkey to wrong process | Partial verify (code + 66 unit tests pass); runtime blocked by #863 |
| #788  | type --app routes to stale PID | Partial verify (code OK); **3 unit tests broken on Windows** → filed #870; runtime blocked by #863 |
| #786  | click eN on UWP Notepad menu | Partial verify (code + 16 unit tests pass); runtime blocked by #863 |
| #840  | type drops literal newlines | Previously partial-verified (no change this round) |
| #843  | capture --app excludes popup menu | Previously partial-verified (no change this round) |

**None can be closed without #863 unblock.** Comments posted on #807, #786, and #788 (with #870 cross-reference).

## Phase 2 — Desktop E2E

**Blocked by #863.** `naturo list apps`, `naturo see`, `naturo capture`, `naturo app launch` all return NO_DESKTOP_SESSION. Explored CLI surface and help text only.

Confirmed `naturo mcp tools` lists **64 tools** without needing desktop — usable smoke test.

## Phase 3 — Exploratory edge cases

Confirmed (all already filed bugs, no fix landed):
- `see --depth -1` → exit 1, plain text (correct)
- `see -b nonexistent` → exit 2, Click usage error (correct)
- `see-app` (typo) → exit 2, Click did-you-mean (correct)
- `see --depth 0` / `--depth 100` → exit 1 (custom validation)
- `type "x"` → exit 2 + Usage banner (#866 still active)
- `naturo screenshot` → "Did you mean 'snapshot'?" (#867 still active)
- `naturo desktop list -j` → emits install prompt before JSON (#869 still active)

## Phase 4 — Chinese User persona

Test surface focused on Chinese encoding and CJK arg parsing (desktop ops blocked).

| Probe | Result |
|-------|--------|
| `naturo see --app 记事本` | NO_DESKTOP_SESSION (correct routing of Chinese arg) |
| `naturo see --app 记事本 -j` | Enveloped error JSON, exit 1 |
| `naturo capture -o "...测试图片.png"` | NO_DESKTOP_SESSION (correct) |
| `naturo type "你好世界"` | exit 2 + Usage banner (#866) |
| `naturo type "😀🎉"` | exit 2 + Usage banner (#866) — emoji parses fine |
| `naturo app launch 记事本` | "Cannot launch GUI applications in a non-interactive session" (correct) |
| `naturo list windows --window 记事本` | **exit 2 — "No such option: --window. Did you mean --pid?"** → led to **#871** |
| `naturo find foo --window notepad` | "No such option: --window" — same root cause |
| `naturo menu-inspect --window notepad` | "No such option: --window" — same root cause |

**Verdict**: Chinese arg parsing itself is solid. The bug surfaced by this persona is a CLI flag-matrix inconsistency (not encoding) — filed as #871.

## Phase 5 — Regression test cases run

| TC | Title | Result | Consecutive |
|------|---------|--------|------|
| TC-0041 | `-j` exit code on failure | PASS | 33 |
| TC-0046 | see --app non-JSON exit code | PASS | 24 |
| TC-0052 | --id flag rejected by 7/8 commands | FAIL (#864 unchanged) | 0 |
| TC-0053 | see -j envelope inconsistency (error branch) | PARTIAL (success branch blocked by #863) | 0 |
| TC-0054 | input cmds exit 2 + Usage banner | FAIL (#866 unchanged) | 0 |
| TC-0055 | hidden snapshot typo leak | FAIL (#867 unchanged) | 0 |
| TC-0057 | deps prompt leaks into -j JSON | FAIL (#869 unchanged) | 0 |

Skipped (require desktop session blocked by #863): TC-0003, TC-0005, TC-0008, TC-0011, TC-0014, TC-0015, TC-0018, TC-0024, TC-0026, TC-0027, TC-0029, TC-0031, TC-0032, TC-0033, TC-0035, TC-0038, TC-0040, TC-0042-explore, TC-0043, TC-0044, TC-0045, TC-0047, TC-0049, TC-0050, TC-0051, TC-0056, TC-0058.

**Regression**: 2/5 testable pass, 3 known-fail (already filed), 1 partial. **No new regressions.**

## Phase 6 — Test case evolution

Created:
- **TC-0058** `exploratory/window-targeting-flag-matrix.yaml` — covers #871

No retirements this round (no closures observed).

## Phase 7 — Issues filed

- **#870** `bug: 3 unit tests added by PR #820 fail on Windows — missing skipif/platform guard` (v0.3.2, P2)
- **#871** `bug: window-targeting flags (--window/--hwnd/--pid) missing from find, menu-inspect, list-windows, get/set, and others — inconsistent matrix` (v0.3.4, P2)

## Top 3 Risks

1. **#863 (P0 ship gate)** is unresolved for 2 days and blocks 5 `status:done` issues from runtime verification. Without console-session QA access, v0.3.2 cannot ship.
2. **#870 newly discovered**: 3 unit tests have been silently failing on Windows since PR #820 merged (2026-04-04 — 53 days ago). Self-hosted runner (#842) offline for 59 days means Windows-only test failures go unnoticed. Cloud VM (#860) still unassigned.
3. **CLI contract debt is accumulating**: #864, #865, #866, #867, #869, #871 — six v0.3.4 CLI consistency bugs filed in the last week. The interactive-shell surface needs a coherent flag spec before #92 (semver API stability) can land.

## Environment

- Windows 10.0.26200
- naturo 0.3.1
- Python 3.12.10, pytest 9.0.2
- Runner: NATUROBOT
- Session: SSH (non-interactive — NO_DESKTOP_SESSION)
- Worktree: `../naturo-qa-mariana` on `develop`

**[QA-Mariana]**
