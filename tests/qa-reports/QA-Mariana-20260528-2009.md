# QA Round 140 — Power User Persona

**Agent:** QA-Mariana
**Timestamp:** 2026-05-28 20:09
**HEAD:** `2e7761a`
**Runner:** NATUROBOT (Naturobot user, SSH session — NO_DESKTOP_SESSION)
**Persona:** Power User (hour 20 mod 8 = 4) — 10+ apps open, multi-monitor, 150% DPI, `--app` filter precision under noisy desktop, rapid `see` calls

## Summary

- **CI Desktop Tests:** skipped — only `[skip ci]` QA-report commits (R138 `16adca7`, R139 `2e7761a`) since `.last-ci-sha` (`ceecfd5`). Reconciling `.last-ci-sha` to `2e7761a` after this round.
- **Issues verified:** 0 (5 `status:done` all SendInput-blocked from this session; no state change since R139, no fresh comments per Orc-Mycelium "piling on has diminishing value" escalation).
- **Regression:** 8/8 NO_DESKTOP-testable TCs re-confirmed FAIL on HEAD `2e7761a` (TC-0054, 0062, 0065, 0067, 0071, 0076, 0079, 0080).
- **New issues created:** **#895** (P2 v0.3.4 — `wait` success envelope drift across sub-modes).
- **New test cases created:** **TC-0081** (exploratory/wait-success-envelope-drift, source #895).
- **Total active test cases:** **60** (+1).

## Phase 1 — Verify Dev Fixes

5 `status:done` issues remain unverified, all SendInput-dependent and blocked by #863 from this NO_DESKTOP_SESSION runner:

| # | Title | Persona R140 |
|---|-------|--------------|
| #786 | click eN on UWP Notepad menu items still does not open menu (regression) | BLOCKED (need console session) |
| #788 | type --app routes to stale PID after app restart, silently drops keystrokes | BLOCKED |
| #807 | press --app sends hotkey to wrong process when target is not focused | BLOCKED |
| #840 | naturo type in default mode drops literal newlines, producing single-line text | BLOCKED |
| #843 | capture --app excludes popup menu windows from app-specific screenshot | BLOCKED |

Per Orc-Mycelium's 2026-05-28 escalation, additional pile-on comments on #863 have diminishing value; this round adds none. **Restructured ship gate (#885 closed + 5 status:done verified from console)** unchanged from R139 — `#885` epic still on Dev-Sirius (idle 53+ days), and console-session verification of the 5 still requires Ace or a #863 workaround.

## Phase 2 / 3 — Exploratory CLI Probes (Power-User-oriented)

Exit-code matrix re-baseline on HEAD `2e7761a`:

| Command (`-j`) | Exit | Behavior |
|----------------|------|----------|
| `list screens -j` | 1 | clean NO_DESKTOP envelope ✅ |
| `list apps -j` | 1 | clean ✅ |
| `list windows -j` | 1 | clean ✅ |
| `see --app notepad -j` | 1 | clean ✅ |
| `capture -j` | 1 | clean ✅ |
| `type 'x' --app notepad -j` | 1 | clean ✅ |
| `press F5 --app notepad -j` | 1 | clean ✅ |
| `click eN --app notepad -j` | 1 | clean ✅ |
| `find --query notepad -j` | 1 | clean ✅ |
| `menu-inspect -j` | 1 | clean ✅ |
| `highlight --app notepad -j` | 1 | clean ✅ |
| `app list -j` | 1 | clean ✅ |
| `dialog detect -j` | **0** | **silent-success bypass** (#875) — `{"dialogs":[],"count":0}` |
| `app windows -j` | **0** | **silent-success bypass** (#878) — returns 14 real windows including own terminal + Chinese-titled windows escaped as `\uXXXX` |
| `wait --gone notepad -j` | **0** | **silent-success bypass** (#893) — `found:true, wait_time:0.0` |
| `taskbar list -j` | **0** | **silent-success bypass** (#875) |
| `tray list -j` | **0** | **silent-success bypass** (#875) |

Text vs `-j` exit-code drift for input commands (already filed as #866): `type/press/click` text mode = exit 2 + `Usage:` banner; `-j` mode = exit 1 + clean envelope.

`naturo desktop list -j` confirms #869 in the wild — emits `Install it now? [Y/n]:` install prompt on stdout BEFORE the JSON envelope, polluting machine-parseable output.

`naturo clipboard get -j` returned `"text":"🎯🚀"` — confirms #894 (`ensure_ascii=True` escapes emoji as surrogate-pair `\uXXXX`). Power Users piping `clipboard get -j` to `jq` see escaped emoji in their own data.

## Phase 4 — Power User Persona Findings

A Power User cannot literally test 10+ apps from NO_DESKTOP_SESSION, but **`app windows -j` (bypass) confirms** the real desktop state from this runner: 14 visible windows across Windows Terminal (own PID 73740, 2 tabs), Chrome (`about:blank`), Clash for Windows, multiple File Explorer windows, Notepad-not-running, and Program Manager. Power Users hitting this surface today get a JSON envelope that:

1. **Mixes Chinese-titled windows escaped as `\uXXXX`** with ASCII titles ("Clash for Windows", "about:blank - Google Chrome"). A Power User on a Chinese-localized Win11 cannot read their own desktop without un-escaping (#894).
2. **Reports `is_visible:true, is_minimized:false` even when the desktop session isn't actually bindable for input** — see / click / type / press cannot act on these windows. The contract drifts: read-only enumeration succeeds (success:true), but every input operation against those same windows fails NO_DESKTOP_SESSION (success:false exit 1). This is #885 territory.

### New finding (filed): **#895** — `naturo wait` success envelope drifts across sub-modes

`naturo wait` groups four very different operations under one command but emits two different success-envelope shapes:

| Sub-mode | Keys | `mode`? | `found`? | `warnings`? |
|----------|------|---------|----------|-------------|
| `wait <duration>` | success, mode, wait_time | ✅ `"duration"` | ❌ | ❌ |
| `wait --gone X` | success, found, wait_time, warnings | ❌ | ✅ | ✅ |
| `wait --window X` | success, found, wait_time, warnings | ❌ | ✅ | ✅ |
| `wait --element X` | success, found, wait_time, warnings | ❌ | ✅ | ✅ |

Power Users chaining mixed-mode `wait` calls in a JSON pipeline cannot use one parser. P2.

### Other Power-User probes (no new bug)

- **Multiple `--app` flags** (`--app notepad --app calculator`): NO_DESKTOP gate fires first, no conflict-detection behavior reachable from here.
- **Empty `--app ""` / whitespace `--app "  notepad  "`**: NO_DESKTOP gate fires first.
- **`--hwnd 0` / `--hwnd -1` / `--pid 0`**: NO_DESKTOP gate fires first; boundary semantics untestable from here.
- **`see --app X --hwnd Y --pid Z --window W` (4-way overspecified)**: NO_DESKTOP gate fires first; precedence-rule semantics untestable from here.
- **Rapid 5× `see` invocations**: all exit 1 (NO_DESKTOP); no cache leakage / state-mutation visible.
- **Boundary `--depth -1/0/1000`**: all gate at NO_DESKTOP; `--depth abc` correctly rejected exit 2.

## Phase 5 — Regression

8/8 NO_DESKTOP-testable TCs re-confirmed FAIL on HEAD `2e7761a`:

| TC | Source | Status |
|----|--------|--------|
| TC-0054 | #866 | FAIL — `type/press/click` text mode still exit 2 with `Usage:` banner |
| TC-0062 | #875 | FAIL — dialog/taskbar/tray `-j` still return success:true `[]` |
| TC-0065 | #878 | FAIL — `app windows -j` still bypasses, returns 14 real windows |
| TC-0067 | #880 | FAIL — `launch`/`open` get no typo suggestion; `screenshot → snapshot` (hidden cmd, #867) |
| TC-0071 | #884 | FAIL — `app launch` error envelope 6 fields, `see/capture/...` 3 fields, `get/set` 2 fields |
| TC-0076 | #889 | FAIL — `ai → wait` (semantic nonsense), `tap → app` (acceptable) |
| TC-0079 | #893 | FAIL — `wait --gone xyz -j` still success:true / found:true / wait_time:0.0 |
| TC-0080 | #894 | FAIL — `clipboard get -j` still escapes emoji as `🎯🚀` |

## Phase 6 — Test Case Evolution

- **TC-0081** created (exploratory/wait-success-envelope-drift.yaml, source #895)
- No retirements (every source issue is open)
- Catalog updated through TC-0081

Total active test cases: **60**.

## Top 3 Risks

1. **#885 epic still unstaffed for 53+ days.** R140 adds yet another envelope-drift finding (#895). The contract drift cluster (#864–#884 + #895) now spans 13 filed P2/P1 bugs against a single subsystem — `-j` JSON output. None ship-blocking individually; collectively they undermine the README's "AI Agent Ready: JSON output" claim (#887). Until #885 closes and a centralized envelope contract is enforced (one schema validator in CI), each new desktop-required command will compound the matrix.
2. **5 SendInput-blocked `status:done` unverified ~28h** after restructured ship gate. R140 made no progress here — only Ace's console-session run or a #863 wrapper can move this. v0.3.2 ship gate cannot close.
3. **Silent-failure cluster (#885) still has 6 entrypoints in the wild** (`dialog detect`, `taskbar list`, `tray list`, `app windows`, `wait --gone`, MCP `list_windows`, MCP `capture_screen`). Agents querying any of these from NO_DESKTOP contexts hallucinate from empty/black/stale data. Restructured ship gate makes #885 the gating Dev work, but no Dev movement in R136–R140.

## Environment

- Windows 11 Pro 10.0.26200
- naturo 0.3.1 (HEAD `2e7761a`)
- Runner: NATUROBOT (Naturobot user), NO_DESKTOP_SESSION

**[QA-Mariana]**
