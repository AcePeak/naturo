# QA-Mariana Round 146 Report
- **Date**: 2026-05-29 02:10 UTC
- **Round**: 146
- **Persona**: Enterprise RPA Dev (hour 02 mod 8 = 2)
- **HEAD**: `de03499` (no source-code changes since R145; only QA-report commits)
- **Runner**: NATUROBOT (NO_DESKTOP_SESSION — same blocker as R117-R145)
- **CI**: skipped (no new code commits since `17aefe6`; same SHA recorded by R145)

## Phase 1 — Dev fix verification
- 5 `status:done` open: #786, #788, #807, #840, #843 — **all SendInput-blocked by #863**
- `naturo see --app notepad` from this session still returns `NO_DESKTOP_SESSION` → cannot exercise input commands → cannot verify
- Restructured ship gate (per Orc-Mycelium STATE.md) requires console-session QA invocation for these 5 — that path is unchanged today

## Phase 2-4 — Enterprise RPA Dev simulation (62-tool MCP sweep)

**Setup**: probe-driven, since the persona must chain 10+ MCP calls and observe error recovery mid-flow. Three probe scripts (`.work/qa-r146/`):
- `enterprise_rpa_probe.py` — 20-step workflow (initialize → list/inspect → click/type/capture → recover → re-list)
- `probe2.py` — 20 follow-ups on schema gaps surfaced in pass 1
- `probe3.py` — 7 targeted bogus-PID and bogus-name calls against `app_inspect`

**MCP tool-name → param-name map (full schema dump, all 62 tools)** is now captured in `transcript.jsonl` and used to correct earlier matrix claims (see #900 comment).

### New findings (filed / commented)

1. **#901 (P1, new, v0.3.4, silent-failure)** — `app_inspect({pid: <any-int>})` returns `success:true` with empty `exe`/`app` + fabricated win32+vision fallback for non-existent PIDs (1, -1, 0, 999999, INT32_MAX). CLI parity check shows `naturo app inspect --pid 999999 -j` correctly emits `PROCESS_NOT_FOUND`. MCP-only regression. P1 because agents enumerating PIDs get success:true for every probe.

2. **#900 extension** — corrected window-targeting matrix:
   - `wait_for_window` belongs to Group A (`title`), not Group B (`window_title`) as originally claimed.
   - `menu_inspect` accepts neither `title` nor `window_title` (only `app`) — was previously lumped into Group B.

3. **#893 extension** — MCP `wait_until_gone` is the MCP analogue of the CLI `wait --gone` bug. Returns `{success:true, gone:true, wait_time:0.0}` under NO_DESKTOP_SESSION. Asymmetry within MCP wait_* family: `wait_for_window` IS guarded (returns TIMEOUT), `wait_until_gone` is NOT.

4. **#885 (epic) extension** — adds `menu_inspect` and `tray_list` MCP wrappers to the silent-failure cluster scope. Suggested audit-script approach to enumerate `(cli_command, mcp_tool)` pairs and verify both sides return the same envelope under NO_DESKTOP_SESSION.

### Other observations (logged, not separately filed)

- `app_inspect({quick: true})` standalone → `INVALID_INPUT`; the `quick` flag isn't validated against the missing required name/pid. Minor UX gap, not filed.
- `naturo app inspect --pid 999999` (CLI) exits **0** despite `success:false PROCESS_NOT_FOUND`. Adds another data point to #897's exit-code-drift surface, but not breaking a new contract.
- `capture_window` (MCP) error path leaks `NaturoCoreError: capture_window: System/COM error` — already covered by #881.
- `app_inspect({pid: 2147483647})` works without overflow validation — defensive but probably fine.

### Workflow stability under errors

The MCP server **stayed alive across all 20 calls in probe 1** (initialize → 18 calls including bogus tool name, missing required args, huge text payload, malformed window IDs → final tools/list). No segfault, no JSON-RPC framing corruption, no zombie process. That's the one positive E2E datapoint for this round.

## Phase 5 — Regression cases (MCP-relevant, desktop-bound skipped)

Re-confirmed FAIL on HEAD `de03499`:
- TC-0051 (Pydantic error leak) — still FAIL (case `15_wait_for_window` validation message)
- TC-0056 (capture_screen black PNG) — still FAIL
- TC-0060 (serverInfo.version=1.26.0 not 0.3.1) — still FAIL
- TC-0062 (dialog/taskbar/tray CLI silent success) — still FAIL
- TC-0065 (app windows -j silent) — still FAIL
- TC-0068 (NaturoCoreError leak) — still FAIL
- TC-0069 (isError flag inconsistency) — still FAIL (6 inconsistencies in probe 1)
- TC-0070 (MCP list_windows silent) — still FAIL
- TC-0077 (MCP list_snapshots broken) — still FAIL (TypeError unchanged)
- TC-0078 (MCP unknown-arg silent drop) — still FAIL (menu_inspect window_title dropped)
- TC-0079 (CLI wait --gone silent) — still FAIL
- TC-0085 (window-target param drift) — still FAIL, **YAML notes refined with R146 matrix**

Desktop-bound regression (TC-0003 / 0038 / 0040 / 0042 / 0043 / 0045 / 0046 / E2E set / TC-0029 etc.) — **skipped** (require interactive desktop; runner session is service-isolated).

## Phase 6 — Test case evolution

- **TC-0086 created** (`exploratory/mcp-app-inspect-bogus-pid-silent.yaml`) — for #901.
- **TC-0085 refined** — notes corrected with full window-targeting matrix (added `wait_for_window` to Group A, `menu_inspect` as no-title-key surface).
- No retirements (no source_issues closed since R145).

Total active: 65 (was 64 + TC-0086).

## Phase 7 — Issues

| Action | Issue |
|--------|-------|
| Filed | #901 (P1, v0.3.4, silent-failure) |
| Commented (matrix correction) | #900 |
| Commented (MCP analogue) | #893 |
| Commented (cluster scope additions) | #885 |

## Top 3 Risks

1. **#901 raises the count of silent-failure variants outside #885's NO_DESKTOP_SESSION scope.** Process-validation gaps and desktop-binding gaps are the same agent-impact class (success:true + empty/fake payload) but have different fix locations. Worth deciding whether #885's middleware-only approach is enough, or whether a broader "agent contract integrity" sweep is needed across the MCP wrapper layer.

2. **`menu_inspect` and `tray_list` MCP silent failures (#885 scope additions)** mean the silent-failure cluster is wider than the original 4 issues. Until the audit-script approach is adopted, any new MCP tool risks introducing the same gap.

3. **5 SendInput-blocked `status:done` (#786, #788, #807, #840, #843) remain unverified, day 60+.** The ship-gate restructuring needs a console-session QA pass; without it, v0.3.2 is permanently blocked even if Dev-Sirius ships #885 fully. This isn't a new finding but it's still the biggest open structural risk.

## Environment

- Windows 11 Pro 10.0.26200
- naturo 0.3.1
- Python 3.x with UTF-8 mode
- Runner: NATUROBOT (service-isolated session — same as R117-R145)

**[QA-Mariana]**
