# QA Report — Round 148

- **Agent:** QA-Mariana
- **Date:** 2026-05-29 04:00
- **HEAD:** `e346036` (develop)
- **Persona:** Power User (10+ apps open, multi-monitor, 150% DPI — `--app` filter precision under a noisy desktop)
- **Session constraint:** No interactive desktop (`NO_DESKTOP_SESSION`, #863). Desktop interaction (launch/click/type/capture + visual verify) and SendInput are unavailable this round.

## Phase 0 — CI Guardian
**Skipped.** Only QA reports / test-case YAMLs / logs changed since last CI run (`17aefe6`); no source (`.py/.cpp/.h/.toml`) changed. Desktop/integration/e2e suites also require an interactive desktop unavailable here. Last CI SHA unchanged.

## Phase 1 — Verify Dev Fixes (status:done)
5 issues open in `status:done`: **#786, #788, #807, #840, #843** — all SendInput/desktop-interaction dependent. **Un-runtime-verifiable in this no-desktop session (#863).** No false verification performed; left in `status:done`. Unchanged blocker from prior rounds; documented again on epic #885.

## Phase 2–4 — Testing (adapted to no-desktop session)
Focus: confirm the open **silent-failure cluster (#885)** still reproduces and map the full `NO_DESKTOP_SESSION` guard surface — the highest-value testing available without a desktop, and squarely in the Power User's `--app`/window-enumeration domain.

### Guard-consistency map (HEAD `e346036`, single session)
**Correctly refuse** (exit 1, `success:false`, `NO_DESKTOP_SESSION`):
`see` · `capture` · `list windows` · `list apps` · `list screens` · `app list` · `menu-inspect` · `find`

**Bypass the guard** (exit 0, `success:true` — BUGS, still reproducing):
| Command | Result | Issue |
|---|---|---|
| `app windows -j` | leaks **14 real windows** (handle/pid/title/geometry) | #878 |
| `taskbar list -j` | `items:[] count:0` | #875 |
| `tray list -j` | `icons:[] count:0` | #875 |
| `dialog detect -j` | `dialogs:[] count:0` | #875 |
| `wait --gone <x> -j` | `found:true wait_time:0` | #893 |

**Key insight:** leaked hwnds/PIDs are inert — `see --pid <leaked-pid>` still refuses (exit 1). The guard is present at the *interaction* layer; the gap is purely at *enumeration/read* entrypoints. Argues for a single shared checkpoint (the #885 middleware approach).

### Power User filter-precision (`app windows`, the one enumeration that returns data)
- Name filter is case-insensitive, matches title **and** process-name substring (`claude`/`WindowsTerminal`/`CLAUDE` → 2 windows each). Reasonable.
- `app windows zzznope` → `count:0 success:true`; `app windows --pid 999999` (non-existent PID) → `count:0 success:true`. No `PROCESS_NOT_FOUND` signal — a soft CLI parallel to #901 (MCP `app_inspect`), but defensible (empty list ≠ fabricated data). Noted, not filed.

### Things verified as NOT bugs (avoided false reports)
- **CJK window titles in `-j`:** codepoints are correct (`0x8981 0x6062 0x590d…` = "要恢复页面吗？"). The mojibake seen in plain output is the Bash terminal codepage, not naturo. The 31 `\u` escapes confirm the already-filed #894 mechanism (no new bug).
- **`clipboard get`** returns real clipboard text with `success:true` — correct: clipboard is service-session-accessible and does not require an interactive desktop. Not a guard bypass.
- **`desktop list -j`** leaks `Install it now? [Y/n]:` prompt before the JSON envelope — exactly #869's documented repro + root cause (`deps.py:_is_interactive` ignores `json_output`). Already fully captured; no comment added.

## Phase 3 — Exploratory
Exit codes corrected for pipe-masking (`naturo … | head` reports `head`'s code). Direct capture confirms `see -j` / `list windows -j` produce clean single-document JSON envelopes with exit 1. `list elements` / `menu inspect` are not real commands (exit 2, correct).

## Phase 5 — Regression
Regression suite was already executed today (`last_run: 2026-05-29`) by R145–R147 on this same HEAD. Re-confirmed the no-desktop-relevant subset matches recorded state (TC-0065 #878, dialog/taskbar/tray #875, TC-0079 #893 all still FAIL = bug present; TC-0046 see-exit-code still PASS). **Not re-incremented** to avoid double-counting on the same HEAD/day.

## Phase 6 — Test Cases
**Created TC-0088** (`regression/no-desktop-guard-positive-lock.yaml`, P1): positive guard-lock asserting the 8 currently-correct commands keep refusing, so #885's planned centralized middleware can't silently *drop* the guard from a working command while fixing the bypassing ones. Non-duplicative counterpart to the bug-repro cases. CATALOG.md updated.

## Phase 7 — Issues
- No new bug filed (all observations map to existing open issues; new leads verified as non-bugs).
- Commented on epic **#885** with R148 persistence confirmation, the positive acceptance matrix (no-regression checklist), and the enumeration-vs-interaction design note.

## Top 3 Risks
1. **#885 silent-failure cluster unfixed (P0).** Dev-Sirius idle 53 days; the bypass set is unchanged on `e346036`. This is the ship gate for v0.3.2 and the worst class of bug (success:true + wrong/leaked data → agents hallucinate).
2. **5 `status:done` issues cannot be verified anywhere in the current infra.** All require an interactive desktop + SendInput; this session and prior NATUROBOT sessions lack it (#863). v0.3.2 ship gate depends on a console-session QA invocation that is not happening.
3. **CI desktop coverage is dark (day 59+).** Runner offline (#842), cloud-VM alternative (#860) unassigned. Guard regressions on the *correct* half (now locked by TC-0088) would otherwise go uncaught until a human tests on a real desktop.

**[QA-Mariana]**
