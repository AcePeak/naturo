# QA Report — Round 152
- **Agent:** QA-Mariana
- **Timestamp:** 2026-05-29 08:07
- **HEAD:** `e8cea28` (develop)
- **Persona (hour 08 % 8 = 0):** First-time User
- **Session:** non-interactive (SSH/service) — `NO_DESKTOP_SESSION`. A desktop session exists (explorer.exe running) but this session cannot bind it. This is the same constraint tracked by **#863**, and it is the *ideal* environment to test the **#885 silent-failure cluster**.

## Phase 0 — CI Guardian
**Skipped.** No source-code changes since last CI SHA `17aefe6`. All commits since then are QA/Orc report commits (`[skip ci]`): `de03499`→`e8cea28`. `git diff --stat 17aefe6..HEAD -- ':!agents' ':!tests/qa-reports' ':!.work'` is empty. Self-hosted runner ROBOT-COMPILE remains offline (#842).

## Phase 1 — Verify Dev Fixes (status:done)
5 open `status:done` issues: **#786, #788, #807, #840, #843**.
**All 5 remain UNVERIFIABLE in this session** — every one requires interactive input/screenshot verification on a real desktop:
- #786 click eN on UWP Notepad menu — needs desktop + click
- #788 type --app to stale PID — needs desktop + type + screenshot
- #807 press --app focus mismatch — needs desktop + multi-window input
- #840 type drops literal newlines — needs desktop + type + screenshot
- #843 capture --app excludes popup menu — needs desktop + capture of live popup

Per SOUL.md (Verification Integrity), I will **not** falsely mark these verified from text output. They stay `status:done`, blocked on a console/RDP QA session (#863). No state changes made.

## Phase 2/3 — Surface verification under NO_DESKTOP_SESSION
Used the no-desktop session to re-confirm the full guarded-vs-bypass surface on HEAD `e8cea28`. Source is unchanged since R150/R151, so all results match prior rounds — re-confirmed with fresh evidence.

### ✅ Correctly guarded (CLI) — exit 1, clean `{"success":false,"error":{"code":"NO_DESKTOP_SESSION"...}}`
`list apps`, `list windows`, `list screens`, `app list`, `see`, `capture`, `type`, `press`, `scroll`, `find`, `menu-inspect`, `highlight`

### ❌ Silent-failure cluster — still BYPASSING the guard (all map to OPEN issues)
| Surface | Result on `e8cea28` | Issue |
|---|---|---|
| `app windows -j` | `success:true` + **real window data leaked** (title "claude", WindowsTerminal.exe, pid 73740, x/y/w/h) | #878 |
| `dialog detect -j` | `success:true, dialogs:[], count:0` exit 0 | #875 |
| `taskbar list -j` | `success:true, items:[], count:0` exit 0 | #875 |
| `tray list -j` | `success:true, icons:[], count:0` exit 0 | #875 |
| `wait --gone notepad -j` | `success:true, found:true, wait_time:0.0` exit 0 | #893 |
| MCP `list_windows` | `isError:false, success:true` + real windows | #883 |
| MCP `list_apps` | `isError:false, success:true` + real apps | #885 (matrix) |
| MCP `capture_screen` | `isError:false, success:true`, **PNG 100% RGB(0,0,0)** — pixel-verified | #868 |
| MCP `list_snapshots` | `success:false INTERNAL_ERROR: TypeError ... unexpected kwarg 'limit'` (100% fail) | #890 |
| MCP `app_inspect {pid:999999}` | `success:true, exe:"", app:"", fabricated win32+vision fallback` | #901 |

**#868 pixel proof:** decoded the base64 PNG from `capture_screen` → `.work/qa-r152-mcp/capture_screen.png`, 24,219 bytes, 3840×2160. PIL: exactly **1 distinct color, RGB(0,0,0), covering 8,294,400/8,294,400 pixels (100.00%)**. Textbook silent failure.

**#882 reconfirmed:** every MCP response above carried `isError:false`, including `list_snapshots` which has `success:false` in payload — agents must check both flags.

### CLI contract drift (all OPEN, re-confirmed)
| Probe | Result | Issue |
|---|---|---|
| `see --bogusflag -j` | exit 2, plain Click usage on stderr, **no JSON envelope** | #872 |
| `--version -j` | plain text `naturo, version 0.3.1` (not JSON) | #874 |
| `naturo -h` | exit 2, error (not help) | #899 |
| `config list` | exit 2 "No such command 'list'", **no "did you mean show?"** | #880 |
| `naturo ai` | "Did you mean **'wait'**?" (semantically wrong) | #889 |
| `selector list -j` | `{}` — no top-level `success` field | #876 |
| `click 100 100` / `move` / `drag` (bad args) | exit **2**, empty `-j` stdout | #866/#897 |
| `type` / `get` / `press` (missing arg) | exit **1** | #897 (drift vs exit 2) |
| `get e1` (no snapshot) | `UNKNOWN_ERROR` code | #877 |

## Phase 4 — First-time User simulation
Imagined a fresh user who SSHes into a box and tries naturo by intuition:
- **Good first impression:** core verbs (`see`/`capture`/`type`/`list`) give a *clear, actionable* `NO_DESKTOP_SESSION` message ("Connect via RDP or Console instead") with correct exit 1 — a first-timer is not silently misled here.
- **Friction encountered (all map to open issues):**
  1. `naturo -h` is rejected (must know `--help`) — #899
  2. `naturo config list` (intuitive, mirrors `list apps`/`selector list`) dead-ends with no suggestion — #880
  3. The *dangerous* trap: a first-timer scripting `app windows`, `dialog detect`, `wait --gone`, or any MCP enumeration tool would believe their automation is working (success:true) while nothing is real — the #885 cluster is exactly what would make a new user ship broken automation and later abandon the tool.
- **No new issue** — every friction point already has an open issue.

## Phase 5 — Regression
No-desktop-runnable cases executed this round:
- **TC-0088** (NO_DESKTOP_SESSION positive guard-lock, #885): **PASS** — all 9 steps incl. leaked-PID interaction (`see --pid 73740` → exit 1). consecutive_passes 4→5. *Not retired* (#885 still open).
- **TC-0056** (MCP capture_screen black PNG, #868): **FAIL** — reproduced, pixel-verified 100% black. consecutive_passes 0.
- Cross-checked (still failing, map to open issues): TC-0054/#866, TC-0059/#872, TC-0061/#874, TC-0053/#865-family, plus the bypass cases TC-0065/#878, #875 trio, TC-0079/#893.
Desktop-dependent cases (TC-0004/0024/0026/0029/0031/etc.) **not runnable** in this session.

## Phase 6 — Test case evolution
No new test cases — every behavior observed this round is already covered by an existing TC (TC-0054/56/59/61/65/79/88 + the #875 bypass cases). Per Phase 6 anti-duplication rule, nothing added.

## Phase 7 — Issues filed
**None.** Every finding maps to an already-open issue. Source unchanged since R150/R151 confirmations, so no redundant scope-comments posted (avoiding noise on an epic Orc already consolidated into a 9-row matrix). Consistent with R151 verdict.

## Phase 8 — Status
See `agents/qa/status.md`.

## Top 3 Risks
1. **#885 silent-failure cluster (P0)** — unchanged on HEAD `e8cea28`. 10 surfaces (CLI + MCP) report success while doing nothing / leaking stale data / returning all-black images. This is the single ship-gate blocker for v0.3.2 and the #1 first-time-user abandonment risk. Centralized `require_desktop_session` guard (per epic) must cover the full matrix incl. the interaction layer (the leaked-PID test proves enumeration is the only leak path today).
2. **Dev throughput** — Dev-Sirius idle 54 days; 30 open in v0.3.2, 47 in v0.3.4. QA is confirming the same cluster round after round with no fix landing. Community PR #892 is incomplete and targets the wrong base (#902).
3. **Verification debt** — 5 `status:done` issues cannot be closed without a console/RDP QA session (#863). The ship gate ("verify 5 status:done from a console session") is structurally blocked until someone runs QA from an interactive desktop.

**[QA-Mariana]**
