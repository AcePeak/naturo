# QA Report — Round 153
- **Agent:** QA-Mariana
- **Date:** 2026-05-29 10:00
- **Persona:** Enterprise RPA Dev (hour 10 → index 2)
- **HEAD tested:** `cf1cfb7` (v0.3.1)
- **Session:** NO_DESKTOP_SESSION (SSH/service — no interactive desktop; explorer.exe running)

## Summary
Source is unchanged since the last CI commit `17aefe6` (verified `git diff 17aefe6..cf1cfb7 -- naturo/ core/ tests/` = empty; intervening commits are all `[skip ci]` QA reports). This round re-confirmed the **#885 silent-failure cluster** from the Enterprise RPA Dev angle and produced a new end-to-end artifact: the cluster doesn't just fail per-surface — it **silently corrupts a single multi-step workflow**. No new issues (all evidence maps to open issues).

## Phase 0 — CI Guardian
**CI Desktop Tests: skipped** (no source change since `17aefe6`; HEAD `cf1cfb7` differs only by QA report commits). `.last-ci-sha` left at `17aefe6`.

## Phase 1 — Verify Dev Fixes (status:done)
5 open `status:done` issues, ALL input/capture commands requiring an interactive desktop — **unverifiable in this session**, blocked by #863 (SendInput/desktop interaction unavailable in agent session). No close/reject (would be falsification per SOUL.md):
- #786 click eN on UWP Notepad menu — needs click + interactive menu
- #788 type --app stale PID — needs type + live app restart
- #807 press --app focus mismatch — needs press + multi-window focus
- #840 type drops literal newlines — needs type + editor inspection
- #843 capture --app excludes popup menu — needs capture + interactive popup

These 5 remain the console-session ship-gate verification backlog (#773).

## Phase 2–3 — Desktop E2E / Exploratory (constrained by NO_DESKTOP_SESSION)
Could not run interactive E2E. Ran the guard-surface matrix instead (see Phase 4).

## Phase 4 — Enterprise RPA Dev Simulation (signature finding)
Ran a 10-step unattended batch. The same NO_DESKTOP_SESSION root cause produced **3 different exit codes** and **3 mid-flow `success:true` steps on fabricated data**:

| Step | Command | exit | payload | Verdict |
|---|---|---|---|---|
| 1 | `app launch notepad` | 1 | plain "non-interactive session" | ✅ fail-fast |
| 2 | `app windows -j` | **0** | success:true, 14 real windows | ❌ #878 |
| 3 | `see -j` | 1 | success:false NO_DESKTOP_SESSION | ✅ |
| 4 | `wait notepad` | **2** | "notepad is not a valid float" | UX nit (use `--window`) |
| 5 | `type` | **2** | NO_DESKTOP_SESSION | ❌ #866 (see=1, type=2) |
| 6 | `capture` | 1 | NO_DESKTOP_SESSION | ✅ |
| 7 | `dialog detect -j` | **0** | success:true, dialogs:[], count:0 | ❌ #875 |
| 8 | `list windows -j` | 1 | success:false NO_DESKTOP_SESSION | ✅ |
| 9 | `app quit notepad` | 1 | "Application not found" (misleading) | minor |
| 10 | `wait --gone notepad -j` | **0** | success:true, found:true, wait_time 0.0 | ❌ #893 |

**Impact:** an RPA harness guarding on exit code (`set -e`) OR on `success:true` sails past steps 2/7/10 on fabricated results — empty dialog list, fake "app gone" confirmation, real window list for an app this flow never owned. Posted as sharpening evidence to epic #885.

## MCP cluster reconfirmation (cf1cfb7)
- `list_windows` → isError:false, success:true, 14 windows (#883)
- `app_inspect pid=99999999` → isError:false, success:true, exe:'' app:'' (#901)
- `capture_screen` → success:true, 3840×2160 PNG, **100% black** (extrema ((0,0),(0,0),(0,0)), 1 distinct color), isError:false (#868 + #882)
- `list_snapshots` → success:false leaking `TypeError: SnapshotManager.list_snapshots` (#890 + #881)

## Phase 5 — Regression
- **TC-0088** (NO_DESKTOP guard positive-lock) — **PASS** (all 8 refusing commands + leaked-PID step exit 1 / NO_DESKTOP_SESSION). consecutive_passes 5→6. Not retired (#885 open).
- **TC-0056** (MCP capture_screen silent-black, #868) — **FAIL** (100% black PNG, success:true). consecutive_passes stays 0.

## Phase 6 — Test Case Evolution
- Created **TC-0090** (`exploratory/rpa-workflow-exit-code-consistency.yaml`) — locks end-to-end cross-surface exit-code/success consistency for #885.

## Phase 7 — Issues
**No new issues.** All evidence maps to open: #878 #875 #893 #866 #883 #901 #868 #882 #890 #881. Posted cross-surface matrix to epic #885.

## Phase 8 — Status
- CI Desktop Tests: skipped (no new source since `17aefe6`)
- Issues verified: none (5 status:done console-session-blocked, #863)
- Regression: TC-0088 PASS (6 consecutive), TC-0056 FAIL
- New test cases: TC-0090
- New issues: none

## Top 3 Risks
1. **#885 silent-failure cluster (P0)** — unchanged 60+ days at the surface level; this round shows it corrupts whole workflows, not just single calls. Ship-blocker.
2. **No desktop CI / runner offline (#842, day 60)** — input/capture fixes (#786/#788/#807/#840/#843) cannot be verified; ship-gate verification backlog frozen.
3. **Contract drift across v0.3.4 (#866/#868/#882/#890 etc.)** — exit codes, JSON envelopes, and isError flags inconsistent; agents/scripts cannot reliably distinguish success from guard-bypassed fake success.
